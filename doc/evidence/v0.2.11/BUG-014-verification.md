# BUG-014 复验核对记录（核对记录型，CLAUDE.md §5.3 第 4 条口径）

流程/台账类缺陷，无自动化复现命令可挂机械测试 log；本记录为独立 qa 出具的核对记录，
按 doc/bugs.md 登记的复现命令逐条核对，供 orch 依本记录裁定 BUG-014 状态。

## 核对对象

- doc/bugs.md BUG-014 条目（登记的复现命令：「用 SPEC-2.1 的子句逐条比对 doc/feature-matrix.md
  全部 FM 行的交付物与场景列，检查是否存在无人认领的子句」）
- doc/spec.md SPEC-2.1（顶栏）、SPEC-2.2（主区）、SPEC-2.2a（事件流面板列表行）、SPEC-2.4①（基础分类过滤）全文
- doc/feature-matrix.md 全部 FM 行（重点 FM-07、FM-10、FM-14）
- doc/testplan.md M2-13、M2-18、M2-19、M2-22、M3-01 行
- doc/evidence/v0.2.10/M2-18.log、doc/evidence/v0.2.10/M2-19.log
- e2e/topbar-brand-clock.spec.ts、e2e/smoke.spec.ts（读导出/断言文本做对接核实，未改动）

## 逐条结论

### 1. SPEC-2.1 逐子句认领核对（登记的复现命令，逐条执行）

| SPEC-2.1 子句 | 认领 FM 行 | 认领场景 | 结论 |
| --- | --- | --- | --- |
| 品牌名「Worlens」（M2） | FM-10（交付物明写「品牌名 Worlens」，引 SPEC-2.1） | M2-18（✅） | 有主，闭环 |
| 顶栏（48px） | FM-10（交付物引 SPEC-2.1，未单列「48px」字样但 SPEC-2.1 整条被引用覆盖） | M2-18（✅，行文显式含「顶栏高度为 48px」判据） | 有主，闭环 |
| UTC 时钟（M2，实时刷新，格式 HH:MM:SS UTC） | FM-10（交付物明写「UTC 时钟」，引 SPEC-2.1） | M2-18（✅，行文显式含格式与实时刷新两项判据） | 有主，闭环——即 BUG-014 原始缺口，现已认领 |
| 加密行情 ticker（M3，SPEC-5.7） | FM-14（交付物明写「SPEC-2.1-ticker」，M3 里程碑） | 场景列现为「（M3 开卡登记）」——与 FM-12/13/15 等其余 M3 行同一占位约定，M3 尚未开卡，非静默蒸发 | 有主但未落场景，属正常的「里程碑未到」占位，与其余 M3 行一致，未见异常 |

结论：BUG-014 登记现象（UTC 时钟无 FM 行认领）核实已消除，四个子句均可在 feature-matrix 找到明确认领行；
其中三个（品牌名/48px/UTC 时钟）已有 ✅ 场景闭环，ticker 子句因里程碑未到暂无场景，与仓库内其余 M3 行占位约定一致，不构成新的「静默蒸发」。

### 2. UTC 时钟子句闭环三方互指核对

- doc/feature-matrix.md FM-10 行：交付物列原文「src/ui/（品牌名 Worlens · **UTC 时钟** · 六 category 开关；**SPEC-2.1**, 2.4①, 8.1 分类子集）」，场景列含 **M2-18**。
- doc/testplan.md M2-18 行：状态 ✅，描述显式引用「SPEC-2.1『UTC 时钟…格式 HH:MM:SS UTC』」与「SPEC-2.1『UTC 时钟（M2，实时刷新…）』」两处子句原文，证据列 = `doc/evidence/v0.2.10/M2-18.log`，复跑列 = `make e2e TEST=topbar-brand-clock`。
- doc/evidence/v0.2.10/M2-18.log：首行为可复跑命令 `make e2e TEST=topbar-brand-clock`，含 `# Generated by scripts/evidence.py` 机械生成头注（非手写），实际输出 4 个用例全 ok（品牌名、48px、UTC 时钟格式、UTC 时钟实时刷新），尾行 `[regress] RESULT: PASS (exit 0)`。

三者互指一致：FM-10 认领 → 指向 M2-18 → M2-18 状态与证据路径/复跑命令对应 → 证据文件内容与 M2-18 行文判据逐条对应。闭环成立。

（旁证，非本条判定依据）：e2e/topbar-brand-clock.spec.ts 当前工作区版本比 doc/evidence/v0.2.10/M2-18.log 抓取时多一条测试（「顶栏不含行情 ticker 占位文本，BUG-026 复验」），经核对为另一未提交改动（`git diff --stat` 显示该文件 +10 行未提交，doc/bugs.md 工作区未提交改动同步将 BUG-026/027 置 CLOSED 并已生成 doc/evidence/v0.2.11/BUG-026.log），与本条 UTC 时钟判据无关，M2-18 场景行文本身已明确注明「加密行情 ticker 为 M3 范围…不入本场景判据」，不构成 M2-18 证据与判据的不一致。

### 3. SPEC-2.2 / SPEC-2.2a / SPEC-2.4① 认领核对（M2 开卡时一并核对，登记要求「只列不改」）

**SPEC-2.4①**（基础分类过滤前移 M2、首屏可见、不进设置面板）：认领于 FM-10，场景 M2-19（✅，doc/evidence/v0.2.10/M2-19.log 4 用例全 ok，逐条引用 SPEC-2.4①/2.2/8.1）。有主，闭环。

**SPEC-2.2**（主区：全屏地球 canvas；右侧悬浮事件流面板 300px 可折叠）：
- 「右侧悬浮事件流面板（宽 300px，可折叠）」：认领于 FM-07，场景 M2-13——**当前状态 🔲**（testplan 行文注明「判据经 REV-012 裁准的 spec 修改而变（v0.2.8），原 ✅ 系旧契约下取得，回退重测」），有主但未闭环，非蒸发，待重测。
- 「全屏地球 canvas」：**未发现任何场景以 SPEC-2.2 为引用对此子句做显式判定**。唯一涉及 canvas 尺寸的场景 M0-02（e2e/smoke.spec.ts）只断言 canvas 非零尺寸（`width>0`、`height>0`），既不断言占满视口（未见 innerWidth/innerHeight/viewport 相关比对），场景行文与测试文件注释也均未引用 SPEC-2.2。FM-07 交付物列虽整体引用了「SPEC-2.2」，但其场景列（M2-10/11/12/13/14/22、M3-01）均聚焦标记层/面板，无一条覆盖「canvas 全屏」这一具体判据。**判定：该子句无场景显式认领，列为遗留发现**（性质与 BUG-014 原始现象相同——spec 子句被 FM 行笼统引用但未被任何场景真正接住）。

**SPEC-2.2a**（列表行三要素、severity 环境式编码、排序邻近度、空状态）：认领于 FM-07。
- 分类色圆点+标题+相对时间、空状态「暂无事件」、不变量 A（色相不随 severity 变）、不变量 C（severity 不作显式行元素）：认领场景 M2-13——**当前状态 🔲**（同上，回退重测），有主未闭环。
- 排序按距 now 时间邻近度升序：认领场景 M2-22（✅，doc/evidence/v0.2.8/M2-22.log）。有主，闭环。
- severity 三档单调递增编码（不变量 B）：认领场景 M3-01——**显式 🔲**，testplan 行文明确「归 D24/D25 视觉批次，非 M2 硬门槛；D25 定通道与取值前保持 🔲，严禁标 ✅（REV-012 §3.6 硬条件）」，属已知、有意的阻断（非蒸发）。

## 遗留漏洞

1. **SPEC-2.2「全屏地球 canvas」子句无场景显式认领**（与 BUG-014 原始问题同一模式，发生在 M2 而非 M1/M3；本记录按登记要求「只列不改」，是否登记新 BUG 或补场景由 orch 定）。
2. M2-13（承载 SPEC-2.2 面板结构与 SPEC-2.2a 列表行/空状态/不变量 A、C）当前 🔲，为 REV-012 spec 修订后的正常回退重测，非蒸发，但尚未闭环——若 M2 签核前不重测通过，SPEC-2.2/2.2a 对应子句实质仍未验证。
3. M3-01（SPEC-2.2a severity 编码）显式 🔲 待 D25 定值，为已知设计阻断（REV-012 §3.6 硬条件），非本次新发现，随附列出以呼应「顺带核对」要求的完整性。

以上 2、3 两项均为「已认领但未验证完成」，与 BUG-014「无人认领」性质不同，不影响本条 BUG-014 复验结论，仅作如实列出。

## 复验结论

BUG-014 登记的核心现象（SPEC-2.1「UTC 时钟」无 FM 行认领）**核实已修复，三方互指闭环成立**：FM-10 行、M2-18 场景、doc/evidence/v0.2.10/M2-18.log 证据逐条对应一致；SPEC-2.1 其余三子句（品牌名/48px/ticker）核对同样有主，无异常。
「M2 开卡时一并核对 SPEC-2.x 其余子句」的顺带核查发现一处同类遗留（SPEC-2.2「全屏 canvas」未被任何场景显式认领），已如实列出、未改动任何文件。

建议 orch：BUG-014 本身依本记录可置 **CLOSED**；上述遗留漏洞 1 是否另开新缺陷登记，留待 orch 决定。

---
署名：qa（独立核对实例，与 BUG-014 修复方非同一实例）
日期：2026-07-21
