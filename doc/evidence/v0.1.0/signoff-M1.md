# signoff-M1 — 里程碑 M1（地球仪）完成签核

- 日期：2026-07-20
- 版本：v0.1.0（`version.json`：`{"version":"0.1.0","milestone":"M1"}`）
- 签核人：rev（独立实例，未参与 M1 的 arch / dev / qa 任务）
- 配套审查记录：`doc/review/REV-004.md`（逐项复核明细与遗留清单）
- 判据：CLAUDE.md §5.1 三条硬条件、§5.2 证据规则（含本轮新增的「✅ 的撤销」与「判据不得向实现看齐」）、§5.3、§7；`doc/spec.md` v0.1

---

## 结论：**有条件通过（CONDITIONAL PASS）**

CLAUDE.md §5.1 的三条硬条件经**独立复核**（未采信 `make next` 输出，亦未采信 qa/orch 交付汇报的转述）全部成立；证据链真实，未发现伪造证据、未挣得的 ✅、或判据被下调至与实现齐平的情形。

放行条件 C1~C3 为台账与收尾流程动作，须在打 tag `v0.1.0` 前闭合，**不涉及 src/tests 返工，不需要重跑任何场景，不影响已归档的证据与状态位**。

---

## 一、三条硬条件核对

### ① 该 M 的 testplan 场景全 ✅ —— 成立

M1 场景共 **13 条**：M1-01 ~ M1-12、M1-14，状态列全部 ✅，证据列与复跑列均非空且证据文件实际存在、首行为可复跑的 `make ` 命令。

无 M1-13：REV-002 §4 G-1 的二选一由 orch 取选项 (b)（SPEC-3.8 在 M1 豁免），豁免声明正式落纸于 `doc/design-prompt/M1-globe.md:17-23` 并附 M2 回补要求，属有据豁免而非静默跳过。

### ② `make regress` 100% PASS 且证据归档 —— 成立

`doc/evidence/v0.1.0/regress_summary.txt`：lint PASS / unit PASS / e2e PASS，`[regress] RESULT: PASS (exit 0)`。

**回归是否跑在最终代码之上（独立时序验证）**：log 内时间戳为 UTC、文件 mtime 为本地时（UTC+10），对齐后——

| 项 | 本地时 |
| --- | --- |
| 最后一次 `src/` 修改（`GlobeScene.ts`） | 2026-07-19 23:27:59 |
| 最后一次 `tests/` 修改（`atmosphere.test.ts`） | 2026-07-19 23:12:17 |
| 最后一次 `e2e/` 修改（`day-side-no-lights.spec.ts`） | 2026-07-20 02:47:37 |
| **全量回归产出** | **2026-07-20 02:54:19** |

回归晚于全部源码与测试改动，覆盖交付的最终代码。另逐条核对 13 个场景证据的 mtime 均晚于其被测源文件，无一例外。

### ③ rev 签核记录 —— 本文件

---

## 二、抽查明细

### 2.1 证据链真实性

- 13 个证据文件首行均为可复跑的 `make ` 命令，第二行均为 `# Generated by scripts/evidence.py on ..., source log: ...`，来源 log 与场景匹配，无 BUG-004 式的 `--lint` 污染。
- `doc/spec.md` sha256 = `ad662e8c111ba6e9ddfa1fd4fccb58b072bc38e46df472d925354ab1e1382041`，与 `doc/spec.sha256` 逐字一致，spec 未被悄改；§0 修改记录 v0.1 条目与 REV-002 P1~P5 裁决对应。
- BUG-008 修复后的截图前置拦截已见实效：M1-05 / M1-07 / M1-08 / M1-14 四个含「截图」「视觉」字样的场景，证据目录中均有对应 PNG。

### 2.2 截图人工判读（签核人亲自执行，非转述）

| 截图 | 判读 |
| --- | --- |
| `M1-07-atmosphere-glow.png` | **达标**。球缘可见明显蓝色辉光带，色相符合 SPEC-3.4 主色 `#4a90d9`；峰值位于球缘、向外递减至背景黑 |
| `M1-05-greenwich-calibration.png` | **达标**。白色校准标记精确落在几内亚湾，非洲轮廓无东西镜像、无南北翻转，符合 SPEC-3.6 |
| `M1-08-starfield.png` | **达标**。星点遍布背景；左侧 20% 采样区为纯背景加星点，与几何计算一致 |
| `M1-14-day-side-gain-1_5.png` / `-6_0.png` | **达标**。两张昼半球目视完全无差别，符合 SPEC-3.3「昼半球不显示灯光」 |

### 2.3 重点复核项（详见 REV-004 §2）

| 复核项 | 结论 |
| --- | --- |
| M1-08「星空随相机旋转」判据强度 | **成立，强于 qa 自评**。采样区（画布左 20%）经几何复算位于大气壳外缘之外 5.6% 画布宽处，区内除星点无他物；用例 2（地球自转时该区逐字节不变）与用例 1（相机转动时该区变化）构成对照/处理组，可拦截「星空挂相机」「星空挂地球」「星空未渲染」三类错误实现 |
| M1-07 重登记质量 | **达标**。REV-003 §1.4 的底线要求（截图 + 人工判读）与可选强化（径向像素采样：峰值显著性、蓝色主导、≥90% 步进非增、末端 <30% 峰值）均已交付；判据措辞未被修改 |
| M1-14 证伪能力 | **可采信**。且不依赖那次运行时变异实验——`mix(night*g, day, k)` 在 k≡1 时对 g 的偏导解析为 0，叠加式实现的昼侧 delta = `night_raw × 4.5`，取样窗口按「夜侧响应最大」主动挑选（对错误实现最不利），与容差 3 之间裕度充分。经 mtime 核验，`src/globe/shaders/earth.ts` 在 M1-14 工作时段内未被改动，qa 未越「不改 src/」边界 |
| BUG-010（OPEN） | **不阻塞**。失效方向为假红非假绿，不动摇既有 ✅；7 次复跑未复现且被全量回归覆盖。须在 M2 内闭合，**不得通过放宽 spec 判据消除** |
| BUG-007（FIX_READY） | **不阻塞**（列为 C2）。属流程缺陷无 SPEC 依据；其数据损坏后果已被机械确认消除（`docs-check` 的 `parse_table` 现对畸形行 `sys.exit` 拒收，本次运行未报任何分列异常）；修复规则已落纸 `.claude/skills/dispatch/SKILL.md:35-37` |
| REV-002 / REV-003 执行清单 | **全部闭合**。REV-002 §7 十项、REV-003 §5 六项逐条核对通过；REV-003 §1.5 的规则补强建议已被 CLAUDE.md §5.2 采纳且更严密 |

### 2.4 代码与测试审查

- **spec 一致性**：SPEC-3.1~3.6、4.1~4.5、6.2、7.1~7.5 逐条核对，实现与 spec 无偏差。SPEC-7.5 的时间归一化用 `pow(0.95, frames)` 而非线性近似，处理正确；sunDir 保持在模型空间、不随自转变换，REV-002 标记的最易错点未踩。
- **过度设计**：未见新增抽象、未见空壳预留，与 REV-002 §3 门禁通过的设计一致。唯一残留是 REV-002 O-3（`uPower`/`uIntensity` 宜收敛为 GLSL 常量），非阻塞。
- **断言质量**：未发现「照抄实现行为」。`tests/starfield.test.ts`、`tests/earth-night-gain.test.ts` 显式只断言 spec 下限而拒绝对实现取值做等值断言，为正面样例。e2e 普遍采用同一仪器下的相对比较，不做跨环境绝对像素等值断言。
- **未发现「判据向实现看齐」**：M1-06/M1-07/M1-14 的判据只被拆分与加强，SPEC-3.3 第二句、SPEC-3.4 衰减句均未掉出场景之外。
- **DEV-only 钩子不进生产产物：独立验证通过**。签核人亲自执行 `npm run build` 后 grep 产物 `dist/assets/index-*.js`，`__globeDebug` / `addCalibrationMarker` / `globeDebug` **0 命中**。REV-002 §6 R-4 的抽验要求由此闭合。

---

## 三、放行条件（须在打 tag `v0.1.0` 前闭合）

- **C1**：执行 `/closeout` 填 `doc/status.jsonl` 首行 summary 与 `doc/log.md` 首块四问，使 `make docs-check` 转绿。当前 FAIL（两条 bump TODO 未填），属收尾未执行的正常中间态，非 §5.1 硬条件。
- **C2**：BUG-007 由**独立 qa（≠ 修复人）**关单。若受阻于「流程类缺陷无测试 log 可作证据源」的规则空白，须由 orch 先明确关单口径，**不得挂一份与该缺陷无关的 log 冒充复验证据**。
- **C3**：将 REV-004 §4 的 NEW-1（证据文件无法覆盖判据跨单测与 e2e 两层的场景，M1-07 / M1-08 各兑现一次）登记入 `doc/bugs.md`；NEW-2~NEW-5 按性质登记或列入 M2 开卡清单。**只要求登记留痕，不要求在 M1 内修完**。

### 关于 NEW-1 的定性（明确不构成回退理由）

M1-07 与 M1-08 的行文各声明了一部分单测判据，而其归档证据只含 e2e 一层（`evidence.py --scen` 只接受单一 log 源）。经判定**不构成「未挣得的 ✅」，不要求回退状态位**：涉及的四条断言均真实存在、真实通过，并被条件 ② 的全量回归覆盖，**无任何 spec 子句因此掉出全部场景之外**——与 REV-003 §1 所处理的「判据零覆盖」有性质区别。缺陷在于复跑列不忠实（证据粒度），须登记留痕并在 M2 首个卡内修复工具后对这两个场景重跑 evidence 补齐。

---

## 四、遗留风险

完整清单见 `doc/review/REV-004.md` §5（R-1 ~ R-11）。须由 M2 承接的 spec 回补义务三条，因其最易静默蒸发，在此重申：

1. **SPEC-3.4「不遮挡标记」**——M1 结构性不可验（无标记层），M2 FM-07 须回补真实断言，**且不得再用材质代理断言**（REV-003 §2.4 裁定继续有效）。
2. **SPEC-3.8**（60fps 目标 / ≥200 标记用 instancing）——M1 已正式豁免，M2 FM-07 须回补。
3. **SPEC-7.4**（点击飞行 800ms + 详情卡 + 列表联动）——须在 M2/M3 建实际 testplan 行，不能只停留在 feature-matrix 的「（M2 起点登记）」占位。

另新发现 **SPEC-2.1 的「UTC 时钟」当前不落在任何 FM 行、也无任何 testplan 场景**（`src/App.tsx` 中连占位都无）。SPEC-2.x 未列入 M1 范围，**不构成 M1 缺陷、不影响本签核**，但须由 orch 指定承接里程碑，否则会以「无人认领」的方式滑到 v1.0.0 之后。

---

## 五、签核声明

本签核人已就 CLAUDE.md §5.1 三条硬条件逐条独立复核，就前序裁决 REV-002 / REV-003 的执行清单逐项核对，亲自判读全部视觉场景截图，并独立验证了回归时序、spec pin、生产产物无 DEV 钩子三项。

据此签核：**里程碑 M1（地球仪）有条件通过**，放行条件 C1~C3 闭合后即可 commit、push 并打 tag `v0.1.0`。

签核人：rev
记录：`doc/review/REV-004.md`
