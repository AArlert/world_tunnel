# signoff-M2 — 里程碑 M2（事件数据层）完成签核（重签核）

- 日期：2026-07-21
- 版本：v0.2.23（`version.json` 里程碑 M2）
- 签核人：rev（全新独立实例，未参与 M2 任何 arch / dev / qa 任务）
- 前次签核：`doc/evidence/v0.2.6/signoff-M2.md` + `doc/review/REV-011-M2-signoff.md`（结论：驳回，主因 FM-09/10/11 零场景）
- 判据：CLAUDE.md §5.1 三条硬条件、§5.2 证据规则、§5.3 缺陷闭环、§7 单一事实源
- 核对方法：三条硬条件与证据链一律独立复跑/复算，不采信 `make next` 齐备提示（BUG-036：该提示无时效校验）、不采信 qa/orch 交付汇报转述

---

## 结论：通过（PASS）

里程碑 M2（事件数据层）三条硬条件全部成立，REV-011 全部整改项均已兑现，无 M2 阻塞缺陷。予以签核。

---

## 一、三条硬条件核对

### ① 该 M 的 testplan 场景全 ✅ —— 成立（含里程碑范围完整性）

- **已登记场景侧**：M2 场景共 25 条（M2-01 ~ M2-25，编号连续），`doc/testplan.md` 逐行核对状态列全部 ✅，证据/复跑两列均非空。抽查 8 个证据文件（M2-13/18/19/20/21/23/24/25）：首行为可复跑 `make` 命令且与 testplan 复跑列逐一对应；次行为 `# Generated by scripts/evidence.py on 2026-07-21, source log: ...`；尾行 `[regress] RESULT: PASS (exit 0)`，log 内 passed 计数正常、无 FAIL 标记。
- **里程碑范围侧（REV-011 驳回主因，本次已闭合）**：feature-matrix M2 七行（FM-05~11）现全部有已 ✅ 场景，无零场景 FM 行——

  | FM 行 | M2 场景 | 状态 |
  | --- | --- | --- |
  | FM-05 数据核心 | M2-01,02,03,04,09 | ✅ |
  | FM-06 T1 信源 | M2-05,06,07,08 | ✅ |
  | FM-07 事件标记+面板 | M2-10,11,12,13,14,22 | ✅ |
  | FM-08 矢量默认风格 | M2-15,16,17 | ✅ |
  | FM-09 缓存优先启动 | M2-20,21,25 | ✅ |
  | FM-10 顶栏+基础分类过滤 | M2-18,19 | ✅ |
  | FM-11 性能预算基线 | M2-23,24 | ✅ |

  （FM-07 场景列另引 M3-01~06，属 M3 视觉批次，不计入 M2 完成判据；M3-06 为 REV-013 C-1 占位场景，🔲，属 M3 范围，不阻塞 M2。）

- REV-011 缺口 G-A~G-F 全部有 ✅ 场景承接，且断言从 SPEC 推导、非照抄实现（见 §三抽查）。

### ② `make regress` 100% PASS 且证据归档 —— 成立（新鲜证据，独立复核时序）

`doc/evidence/v0.2.23/regress_summary.txt`：lint PASS / unit PASS / e2e PASS，`[regress] RESULT: PASS (exit 0)`，内部时间戳 2026-07-21T12:21:30（UTC）。

**新鲜性独立核验（针对 BUG-036「齐备提示无时效校验」，本人核对的是证据本身而非提示）**：

| 项 | 本地时（UTC+10） |
| --- | --- |
| regress_summary.txt mtime | 2026-07-21 22:21:33 |
| test-results/e2e_all.log mtime | 2026-07-21 22:21:30 |
| test-results/unit_all.log mtime | 2026-07-21 22:19:46 |
| commit 6ac20a6（M2 最终代码）提交时刻 | 2026-07-21 22:19:34 |
| 最新 e2e/src/tests 改动 mtime | 2026-07-21 21:36:03（day-night-calibration.spec.ts） |

回归产出晚于 commit 6ac20a6 与全部源码/测试改动，覆盖 M2 最终代码，系本轮显式重跑的新鲜证据（非历史轮次遗留）。test-results 三份 log 尾行均 `[regress] RESULT: PASS (exit 0)`，全库 grep 无非良性 FAIL 标记。`doc/evidence/v0.2.23/` 当前为工作树未跟踪目录（提交后重跑所致，符合签核前显式重跑口径）。

### ③ rev 签核记录 —— 本文件

---

## 二、REV-011 整改项复核表

| REV-011 整改项 | 兑现情况 | 复核依据 |
| --- | --- | --- |
| 1. 开卡补齐 FM-09/10/11（G-A~G-F） | 已兑现 | 三行均有 ✅ 场景（见 §一①表）；G-A/B 由 M2-18、G-C 由 M2-19、G-D 由 M2-20、G-E 由 M2-21、G-F 由 M2-23/24 承接，全 ✅ |
| 2. 裁定 BUG-027 品牌名归属冲突（以 SPEC-2.1 为准纳入 FM-10） | 已兑现 | BUG-027 CLOSED；orch 依 REV-011 整改项 2 以 SPEC-2.1（M2）为准，未走 §7 改里程碑标注；M2-18 断言品牌名 `toHaveText('Worlens')`、M0-02 页面标题改判据回退重测 ✅ |
| 3. BUG-010 逾期义务（M2 内闭合，不得放宽 spec 判据消除） | 已兑现 | BUG-010 CLOSED（doc/evidence/v0.2.22/BUG-010.log 机械证据 + 三轮全并发绿）；治理仅改 e2e/ 采样机制，未动 src 与 spec 判据（见 §三 commit 6ac20a6 审查） |
| 4. 修 make next 的 feature-matrix 层盲区 | 已兑现 | BUG-028 CLOSED；scripts/docs.py next_action() 新增「该 M 的 FM 行是否均有场景」前置检查，独立 qa 正反两向核对 PASS（doc/evidence/v0.2.22/BUG-028-033-verification.md） |

---

## 三、抽查明细

### 3.1 证据链真实性（成立）

- 抽查 M2-13/18/19/20/21/23/24/25 共 8 个证据文件：首行复跑命令与 testplan 复跑列一致、次行 evidence.py 署名、来源 log 与场景名匹配、无 BUG-004 式 `--lint` 污染、尾行 PASS、无 FAIL。
- `make docs-check` 返回 `[docs-check] OK`（含幽灵引用/列数/状态一致性校验）。
- `doc/spec.md` sha256 = `c20122b50f60aa6a90af6c191100f066612d7c30739f1bbdefb6a3b92e133758`，与 `doc/spec.sha256` 逐字一致，spec 未被悄改，修改记录与 pin 一致。

### 3.2 FM-10 核心断言强度（REV-011 驳回缺口，逐条指回 SPEC）

- **M2-18（e2e/topbar-brand-clock.spec.ts）**：品牌名 `toHaveText('Worlens')`（SPEC-2.1，闭合 G-A/BUG-027）；顶栏高度 `toBe(48)`（SPEC-2.1）；UTC 时钟格式 `/^\d{2}:\d{2}:\d{2} UTC$/`（SPEC-2.1）；与浏览器 UTC 秒数 ±10s 环绕比对以区分 UTC 与本地时（判别力真实）；>1s 两次采样取值不同证明实时刷新（SPEC-2.1，闭合 G-B/BUG-014）；顶栏不含 ticker 占位（BUG-026）。期望值全部标注从 SPEC-2.1 推导、显式声明「不把实现取值当期望」。
- **M2-19（e2e/category-filter.spec.ts）**：六开关 `toHaveCount(6)` 且首屏可见（SPEC-2.4①）；data-category 集合等于 SPEC-3.7 六值枚举；默认全开、可切换；关闭某类后其标记与列表行两处同步消失、重开恢复（SPEC-2.4①/8.1/2.2，闭合 G-C）；集合语义非互斥。诚实标注 conflict/news/launch/flight 无 M2 信源（GDELT/opensky 属 M3）故仅验开关 DOM 存在，disaster/humanitarian（GDACS）验真实过滤——属正确范围界定，非覆盖缺口。

### 3.3 commit 6ac20a6 e2e 采样机制审查（log.md 0.2.23 问题①，本人兜底）

逐 spec 复核 8 个改动 + globeDebug 新增 helper，确认采样机制修改**未降低 SPEC 推导的断言强度**：

- **idle-spin（M1-11/SPEC-7.5）**：速率量测由单 5s 均值窗改 9 段短窗中位数，±20% 容差（[0.96,1.44]°/s）逐字未改，仅把估计量从含卡顿单窗均值换成抗离群中位数——判据强度不降（更抗离群）。
- **drag-inertia（M1-09/SPEC-7.1）**：`earthRotY.toBeCloseTo(before, 10)` 精度未改，采样点由 t5（~8.2s，逼近 SPEC-7.3 10s 空闲阈值受空闲自转干扰）前移至 t2（拖拽+惯性已收敛、距阈值有余量）——判据完整保留、去混淆。
- **marker-breathing（M2-21/SPEC-3.11）**：外部 20 次往返采样改页面内 rAF 逐帧记录（injectAndRecordBreathing），采样密度=真实帧率；下游断言逐条完整（旧标记 ≥2 中间态+单调非增+终态 α≤0.05；新标记 ≥2 中间态+单调非减+终态 α>0.95；既有标记全程 α>0.9）——仅提高采样分辨率，判据未改。
- **marker-severity-tri-channel（M3-03/SPEC-3.7）**：findColorInRegion→findColorInRegionStable（重试等渲染追上，minCount 下限不变、真读不到则重试耗尽照常失败）+ 每枚采样前重置空闲计时钉零自转——期望值未改。
- **panel-marker-linkage（M2-14/SPEC-7.4）**：基线 dot 面积改轮询到渲染可见为止，`>0` 前置自检与联动因果断言保留——等异步链路追上，非放宽。
- **day-night-calibration（M1-05）/zoom-range（M1-10）**：仅放宽超时预算 + 恢复零自转前置条件，SPEC-3.2/7.2 断言未动。
- **globeDebug.ts**：findColorInRegionStable（重试不吞真失败）、injectAndRecordBreathing/waitBreathingTrace（读 three.js 标准 instanceMatrix/instanceAlpha、按 lat/lon 方向识别、不读实现过渡常量）——均为采样密度/驱动方式改进，无判据侵蚀。

---

## 四、全部非 CLOSED 缺陷逐条阻塞判断（10 条，独立复核 orch 预判）

| BUG | 状态 | 阻塞 M2 | 理由 |
| --- | --- | --- | --- |
| BUG-011 | OPEN | 否 | evidence.py 跨单测+e2e 两层证据工具缺口；断言真实存在且被全量回归覆盖、无 spec 子句掉出场景（REV-004 已定性不撤 ✅）。工具债 |
| BUG-012 | OPEN | 否 | M1-04/M2-17 层级标注（标单测实为 e2e）；判据已被 e2e 真实覆盖，属表述与实测对齐、非覆盖缺口 |
| BUG-013 | OPEN | 否 | tests 头注过期失真（M1-06/07），文档卫生，不涉状态位/判据 |
| BUG-015 | OPEN | 否 | evidence.py 无法机械关单流程/规则类缺陷——工具+规则空白，不涉 M2 场景 ✅。工具债 |
| BUG-016 | FIX_READY | 否 | M2-02 flight 子句已按 REV-007 F-1 改挂 M3 FM-12，M2 无 opensky 源，正确延后；复验属 M3 开卡事项 |
| BUG-017 | OPEN | 否 | SPEC-5.6 OpenSky CORS 属 M3 FM-12 契约缺陷，M2 无 opensky 源，不在 M2 范围 |
| BUG-029 | OPEN | 否（状态滞后，宜关） | SPEC-2.2「全屏地球 canvas」子句现已由 M2-13 场景显式承接（描述含「canvas 占满视口…SPEC-2.2…补齐 BUG-029」）且 M2-13 ✅——子句不再无人认领、无静默蒸发。BUG-029 仍挂 OPEN 属台账滞后，建议 orch 核对 M2-13 断言后置 CLOSED |
| BUG-031 | OPEN | 否 | SPEC-3.7 常驻脉冲被用户否决，属走 §7 的动效批（D27）spec 变更提案，M3+；M2-10 ✅ 立于现行 SPEC-3.7，若日后改则 M2-10 须同步重登 |
| BUG-032 | OPEN | 否 | 近景失真三件：③网格默认隐藏（M2 相关部分）已兑现（v0.2.15，M2-15 结构断言确认网格 visible=false）；①②海岸线/大气 LOD 属视觉第二批，M3+ |
| BUG-036 | OPEN | 否 | make next 判据②③无时效校验，工具债；本轮已显式重跑 REGRESS 规避，条件②由新鲜证据独立核验成立（见 §一②） |

**结论**：10 条非 CLOSED 缺陷无一阻塞 M2 三条硬条件，与 orch 预判一致。**移交 orch 处置项**：BUG-029 状态滞后——子句已被 M2-13 ✅ 承接，建议核对后置 CLOSED（非签核阻塞，仅台账卫生）。

---

## 五、未越权声明

本次仅只读分析 + 书面记录，只写本文件（doc/evidence/signoff-M2.md）。未改 src/tests/e2e/spec/台账、未动任何状态位、未提交 git。BUG-029 状态滞后仅以移交项形式提示 orch，未自行改动。

---

## 六、签核声明

本签核人已就 CLAUDE.md §5.1 三条硬条件逐条独立复核（未采信 make next 齐备提示、未采信交付汇报转述）：独立复算 regress 证据新鲜性（针对 BUG-036）、逐字比对 spec pin、抽查 8 个证据文件真实性、逐条审查 commit 6ac20a6 全部 8 个 e2e 采样改动确认断言强度未降、逐条核对 REV-011 四项整改与 10 条非 CLOSED 缺陷阻塞性、并交叉核对 feature-matrix 确认 M2 全部 FM 行有 ✅ 场景（REV-011 驳回主因已闭合）。

据此签核：**里程碑 M2（事件数据层）通过（PASS）**，可打 tag `v0.2.23`（或 orch 收尾版本）。遗留非阻塞项 BUG-029 台账状态滞后，移交 orch 核对后关单。

签核人：rev
日期：2026-07-21
