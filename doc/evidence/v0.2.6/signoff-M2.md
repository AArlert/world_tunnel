# signoff-M2 — 里程碑 M2（事件数据层）完成签核

- 日期：2026-07-21
- 版本：v0.2.6（`version.json`：`{"version":"0.2.6","milestone":"M2"}`）
- 签核人：rev（独立实例，未参与 M2 的 arch / dev / qa 任务）
- 配套审查记录：`doc/review/REV-011-M2-signoff.md`（核对方法与过程明细）
- 判据：CLAUDE.md §5.1 三条硬条件、§5.2 证据规则、§5.3、§7；`doc/spec.md` v0.2.4

---

## 结论：**驳回（REJECT）**

M2 里程碑**尚未完成**，不予签核。

已登记的 17 条 M2 testplan 场景（M2-01~17）全部 ✅、证据链真实、回归 100% PASS 且归档——这三项**均成立**，证据侧无造假、无未挣得的 ✅、无判据向实现看齐。但**里程碑范围不完整**：feature-matrix 定义的 M2 七行（FM-05~11）中，**FM-09 / FM-10 / FM-11 三行从未开卡**——零 testplan 场景、其中 FM-10 零实现——所承接的多条 M2 强制 SPEC 子句（SPEC-2.1 品牌名 Worlens + UTC 时钟、SPEC-2.4① 六 category 过滤、SPEC-3.10 性能基线、SPEC-3.11 缓存优先启动、SPEC-3.8 60fps）目前**零 ✅ 覆盖**。

`make next` 提示"跑签核"是**误报**：它只机械核对**已登记**场景全 ✅，看不见"从未开卡"的 feature-matrix 行——正是 REV-002 §4 G-1 / BUG-014 反复警示的"spec 子句无人认领而静默蒸发"模式，只是这次发生在 feature-matrix 层、且被 `make next` 的盲区放大成"可签核"的假象。orch 自己的 `doc/log.md` [0.2.6] 块与 `doc/status.jsonl` 首行均把这三项列为待办的"M2 收尾三件"，与本判定一致——REV-011 签核卡系在 FM-09/10/11 交付前**提前派发**。

本结论**不是**台账收尾类"有条件通过"（M1 的 C1~C3 那种不涉 src/tests 返工的动作）；FM-09/10/11 需要实打实的 dev 实现 + qa 场景登记 + 证据归档。故取**驳回**：补齐三行、重跑 regress、再行签核。

---

## 一、三条硬条件核对

### ① 该 M 的 testplan 场景全 ✅ —— **字面成立，里程碑范围不成立**

**已登记场景侧（成立）**：M2 场景共 **17 条**（M2-01 ~ M2-17），状态列全部 ✅，证据列与复跑列均非空、证据文件实际存在、首行为可复跑 `make` 命令、次行为 `# Generated by scripts/evidence.py`，来源 log 与场景匹配，无 BUG-004 式 `--lint` 污染；抽查 17 个 log 无一含 FAIL 标记。

**里程碑范围侧（不成立）**：feature-matrix M2 七行中三行零场景——

| FM 行 | 交付物 / SPEC 锚点 | 场景 | 实现 |
| --- | --- | --- | --- |
| FM-05 数据核心 | SPEC-5.0/6.1/6.2/6.3 | M2-01,02,03,04,09 ✅ | 有 |
| FM-06 T1 信源 | SPEC-5.1/5.2/5.3/5.5 | M2-05,06,07,08 ✅ | 有 |
| FM-07 事件标记+面板 | SPEC-2.2/3.7/3.8/3.4/7.4 | M2-10,11,12,13,14 ✅ | 有 |
| FM-08 矢量默认风格 | SPEC-3.2 重写/3.3 | M2-15,16,17 ✅ | 有 |
| **FM-09 缓存优先启动** | **SPEC-3.11, 8.4** | **（M2 开卡登记）— 零场景** | 部分（cache.ts round-trip 有，启动 wiring + 呼吸式过渡未验证） |
| **FM-10 顶栏+基础分类过滤** | **SPEC-2.1（品牌名 Worlens · UTC 时钟）, 2.4①（六 category 开关）, 8.1 分类子集** | **（M2 开卡登记）— 零场景** | **零**（grep src 无 UTC 时钟代码、无分类过滤控件；顶栏品牌名仍为「World Tunnel」而非「Worlens」） |
| **FM-11 性能预算基线** | **SPEC-3.10 基线, 3.8（60fps）** | **（M2 开卡登记）— 零场景** | 无基线量测（M2-11 已显式把 60fps 量测挂 FM-11 承接，见其行文与 design-prompt/M2-globe.md 检查点 6） |

三行的 M2 归属有据（非本签核人臆断）：SPEC-2.1 正文标「品牌名 Worlens（M2）· UTC 时钟（M2）」、SPEC-2.4①「基础分类过滤前移至 M2」、SPEC-3.10「M2 建基线量测」；design-prompt/M2-globe.md §2.3（行 21/23/24）与 M2-data.md §17-18 均把这些交付物显式改挂 FM-09/10/11；product-decisions D6/D9 亦确认为 M2 承诺。**无任何降级/移出 M2 的决议**——是待办，非已豁免。

### ② `make regress` 100% PASS 且证据归档 —— 成立（但跑在不完整的代码上）

`doc/evidence/v0.2.6/regress_summary.txt`：lint PASS / unit PASS / e2e PASS，`[regress] RESULT: PASS (exit 0)`；对应 `test-results/`：lint PASS、unit **19 files / 159 tests** passed、e2e **34** passed。

**回归时序（独立核验，覆盖最终代码）**：内部时间戳 UTC 02:42:38 = 本地（UTC+10）12:42:38，与文件 mtime 对齐——

| 项 | 本地时 |
| --- | --- |
| 最新 `src/` 修改（`markers.ts`） | 2026-07-21 06:31:39 |
| 最新 `tests/` 修改（`markers.test.ts`） | 2026-07-21 05:12:32 |
| 最新 `e2e/` 修改（`panel-marker-linkage.spec.ts`） | 2026-07-21 06:39:27 |
| **全量回归产出（e2e_all.log）** | **2026-07-21 12:42:38** |

回归晚于全部源码/测试改动约 6 小时，覆盖交付的最终代码。**须提示**：此 PASS 只证明"当前（缺 FM-09/10/11 的）代码库"通过回归，不构成里程碑完整性的证据。

### ③ rev 签核记录 —— 本文件 + `doc/review/REV-011-M2-signoff.md`

---

## 二、抽查明细

### 2.1 证据链真实性（成立）

- 17 个证据文件首行均为可复跑 `make` 命令，次行均为 `# Generated by scripts/evidence.py on ..., source log: ...`；来源 log 与场景名逐一对应，无 `--lint` 污染。
- `doc/spec.md` sha256 = `42b9602b1abe4ff954d8d0781f4ac6584b830306db6828f4a634fd88d693f9a3`，与 `doc/spec.sha256` 逐字一致，spec 未被悄改；`make docs-check` 返回 `[docs-check] OK`（含幽灵引用/列数/状态一致性校验）。
- §0 修改记录 v0.1~v0.2.4 六条与各裁决（REV-002/005/006/007/008/009/010）对应，无孤条。

### 2.2 视觉场景截图人工判读（签核人亲自执行，非转述）

声明了「截图/视觉」判据、经 BUG-008 前置拦截把关的两条场景均附 PNG——

| 截图 | 判读 |
| --- | --- |
| `M2-10-marker-category-colors.png` | **达标**。六标记颜色可辨且互异，与 SPEC-3.7 色表一一对应：左上红（disaster）、上中橙（conflict）、右上金黄（humanitarian）、左下蓝（news）、下中紫（launch）、右下青（flight）；各标记带脉冲环 |
| `M2-15-vector-earth-day.png` | **达标**。深色底 + 青蓝海岸线 + 30° 网格 + 蓝色大气辉光；无卫星纹理，非洲/欧洲/南美轮廓正确、无镜像翻转（SPEC-3.2a/3.2②/3.6） |
| `M2-15-vector-earth-night.png` | **达标**。整体较昼图更暗，海岸线仍可辨（SPEC-3.2a 夜面「夜端暗于昼端」）；辉光强度差异细微，机械断言由 e2e 承接、截图为佐证 |

### 2.3 附带发现（非本次场景判据，但截图暴露、与新登记缺陷印证）

三张 M2 场景截图的顶栏均显示「**World Tunnel　行情 ticker（M3）**」、面板显示「事件流（M2）」——印证 BUG-026（里程碑代号泄漏进 UI）、BUG-027（对外名仍为内部代号而非 Worlens），且直观佐证 FM-10 顶栏尚未落地。

---

## 三、驳回缺口清单（须补齐后重签）

| 缺口 | 承接 FM | 未覆盖 SPEC 子句 | 现状 |
| --- | --- | --- | --- |
| G-A 顶栏品牌名 | FM-10 | SPEC-2.1「品牌名 Worlens（M2）」 | 仍为「World Tunnel」（BUG-027） |
| G-B UTC 时钟 | FM-10 | SPEC-2.1「UTC 时钟（M2，实时刷新，HH:MM:SS UTC）」 | 无代码（BUG-014 FIX_READY 但 FM-10 未开卡，复验前置未满足） |
| G-C 六 category 过滤 | FM-10 | SPEC-2.4①「基础分类过滤前移 M2，首屏可见控件」、SPEC-8.1 分类子集 | 无控件、无场景 |
| G-D 缓存优先启动 wiring | FM-09 | SPEC-3.11「冷启动即渲染缓存事件、不空网络等待」 | 零场景；cache round-trip 有，启动接线未验证 |
| G-E 呼吸式过渡 | FM-09 | SPEC-3.11「首轮刷新后旧标记渐隐/新事件渐亮，不整屏闪烁」 | 零场景（M2-globe.md 只保证接口不堵死，未实现） |
| G-F 性能预算基线 | FM-11 | SPEC-3.10「M2 建基线量测（首包体积/冷启动秒数）」、SPEC-3.8 60fps | 零场景、无量测 |

补齐路径：qa 先登记 FM-09/10/11 对应 testplan 场景 →（顶栏/过滤属重大 UI，按需 arch design-prompt → rev 门禁）→ dev 实现 + 自检 → qa 测试 PASS 后 `make evidence` → 全量 `make regress`（REGRESS=1 重新归档）→ 重新签核。

---

## 四、全部非 CLOSED 缺陷逐条阻塞判断

台账现有 14 条非 CLOSED 缺陷，逐条独立判断其是否阻塞 M2 签核：

| BUG | 状态 | 是否阻塞 M2 | 理由 |
| --- | --- | --- | --- |
| BUG-010 | OPEN | 否（但为逾期义务） | flaky e2e（idle-spin/day-night-cal/zoom-range），失效方向为假红非假绿，不动摇任何 ✅（同 M1 签核定性）。**注**：M1 签核曾要求「须在 M2 内闭合」，至今未闭合，属逾期义务，非签核阻塞项本身 |
| BUG-011 | OPEN | 否 | 证据跨单测+e2e 两层的工具缺口；影响 M2-10（e2e+单测辅助）证据粒度，但断言真实存在且被全量回归覆盖、无 spec 子句掉出场景（同 M1 NEW-1 定性）。工具债，留痕 |
| BUG-012 | OPEN | 否 | testplan M1-04/M2-17 层级标注（标「单测」实为 e2e）——表述与实测对齐问题，判据本身已被 e2e 真实覆盖，非覆盖缺口 |
| BUG-013 | OPEN | 否 | tests 头注过期失真（M1-06/07），文档卫生问题，不涉状态位/判据 |
| **BUG-014** | FIX_READY | **是** | SPEC-2.1 UTC 时钟改挂 FM-10（M2），但 FM-10 从未开卡——「复验待 qa 于 M2 开卡时出具」的前置条件从未满足。属 G-B 缺口，M2 强制子句零覆盖 |
| BUG-015 | OPEN | 否 | evidence.py 无法机械关单流程/规则类缺陷——工具+规则空白；不涉 M2 场景 ✅，但制约 BUG-007 类闭环口径。留痕 |
| BUG-016 | FIX_READY | 否 | M2-02 flight 过期子句已按 REV-007 F-1 改挂 M3 FM-12，M2 无 opensky 源，正确延后；复验属 M3 开卡事项 |
| BUG-017 | OPEN | 否 | SPEC-5.6 OpenSky CORS 属 M3 FM-12 契约缺陷，M2 无 opensky 源，不在 M2 范围 |
| BUG-022 | OPEN | 否（质量风险） | 脉冲环 AdditiveBlending 密集重叠饱和成白团。**不推翻 M2-10 ✅**（M2-10 判读的是单标记六分类色，其断言仍真）；但确为 SPEC-3.7「分类色唯一色语义」在重叠态的真实劣化，与聚合方案（D23）联动，宜随 FM-07 质量收口或 M3 处理 |
| BUG-023 | OPEN | 否（高产品风险） | USGS 无震级下限、M<2 微震淹没。**不推翻 M2-05 ✅**（字段映射/severity/回填断言仍真）；SPEC-5.1 未定入球门槛属 spec 级设计缺口，走 §7；严重伤害 D1 低噪核心，宜与 FM-10 分类过滤联动优先解决 |
| BUG-024 | OPEN | 否（spec 变更耦合） | 事件流 ts 倒序对未来事件反直觉。**不推翻 M2-13 ✅**（M2-13 断言的正是 SPEC-2.2a 现文的 ts 倒序）；属 spec 级设计缺陷走 §7，若 SPEC-2.2a 改则 M2-13 须同步重登 |
| BUG-025 | OPEN | 否（spec 变更耦合） | 事件流行无 severity 表达。**不推翻 M2-13 ✅**（M2-13 显式断言「行=三要素、不含 severity」与现 SPEC-2.2a 一致）；属 spec 增补走 §7，与 BUG-023 入球门槛联动设计 |
| BUG-026 | OPEN | 否 | 「行情 ticker（M3）」代号泄漏进 UI——src/ui 缺陷，无 M2 场景断言其不渲染，定为 M3 交付前修；属对外可见的小行为泄漏，低优先级。与 FM-10 顶栏重构可一并处理 |
| BUG-027 | OPEN | **是** | 对外名仍「World Tunnel」而非「Worlens」。**违反 SPEC-2.1「品牌名 Worlens（M2）」**——是 FM-10 的 M2 交付子句（G-A 缺口）。缺陷条目自评「可与 M5 打包合并」的归属与 SPEC-2.1 的 M2 归属冲突，须以 SPEC 为准纳入 FM-10 |

**阻塞项**：BUG-014、BUG-027（两者均并入 FM-10 缺口 G-B / G-A）。其余 12 条不阻塞三条硬条件，但 BUG-010（逾期义务）、BUG-022/023（产品质量）、BUG-024/025（spec 变更耦合）应随 M2 收尾一并跟踪。

---

## 五、给 orch 的整改项（非本签核人可自行执行）

1. **开卡补齐 FM-09 / FM-10 / FM-11**（阻塞签核）——按缺口清单 G-A~G-F，qa 登记场景 → dev 实现 → qa 证据 → 重跑 REGRESS=1 → 重新签核。
2. **裁定 BUG-027 归属冲突**：SPEC-2.1 把「品牌名 Worlens」定为 M2，而 BUG-027 条目自评延至 M5——以 spec 为准应纳入 FM-10（M2）；若确要延后，须走 §7 修改 SPEC-2.1 的里程碑标注，不得口头默认。
3. **BUG-010 逾期义务**：M1 签核要求 M2 内闭合，至今 OPEN；M2 收尾前处置，**不得通过放宽 spec 判据消除**。
4. **修 `make next` 盲区（建议，非阻塞）**：当前 `make next` 只核对已登记场景，看不见"零场景的 M2 feature-matrix 行"，导致本次误报可签核。建议里程碑核对纳入"该 M 的 FM 行是否均有场景"，堵住 feature-matrix 层的静默蒸发（BUG-014 同源）。

---

## 六、签核声明

本签核人已就 CLAUDE.md §5.1 三条硬条件逐条独立复核（未采信 `make next` 输出、亦未采信 qa/orch 交付汇报转述），亲自判读全部视觉场景截图、独立验证回归时序与 spec pin、逐条核对全部非 CLOSED 缺陷的阻塞性，并交叉核对 feature-matrix / design-prompt / product-decisions 确认 FM-09/10/11 的 M2 归属无降级决议。

据此签核：**里程碑 M2（事件数据层）驳回**，须补齐 FM-09 / FM-10 / FM-11（缺口 G-A~G-F，含阻塞缺陷 BUG-014 / BUG-027）、重跑并归档全量回归后重新提交签核。

签核人：rev
记录：`doc/review/REV-011-M2-signoff.md`
