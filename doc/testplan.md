# testplan — 场景真值表

状态位：🔲 计划 / ⚠️ 部分 / ❌ 失败（须挂 BUG 编号）/ ✅ 通过（仅 evidence.py 可置）。
**先登记后写码**；「证据」「复跑」两列为脚本专属，人和 agent 不手填。
场景描述要写成「激励 + 必须成立的判据」，引用 spec 条目编号（SPEC-x.y）。

| 编号 | 里程碑 | 场景描述 | 状态 | 证据 | 复跑 |
| --- | --- | --- | --- | --- | --- |
| M0-01 | M0 | 单测链路：geo.latLonToVector3 对北极/赤道本初子午线/东经90°三个已知点输出正确坐标（SPEC-6.2 约定），证明 vitest→log→evidence 链路可用 | ✅ | doc/evidence/v0.0.1/M0-01.log | `make test` |
| M0-02 | M0 | e2e 链路：dev server 启动后页面标题含品牌名「Worlens」（SPEC-2.1；原判据「World Tunnel」为 M0 期内部代号文案，2026-07-21 orch 依 SPEC-2.1 与 REV-011 整改项 2 同步，原 ✅ 证据系旧文案下取得，回退重测），canvas 出现且尺寸非零，无 pageerror；截图入证据 | ✅ | doc/evidence/v0.2.10/M0-02.log | `make e2e TEST=smoke` |
| M0-03 | M0 | 质量门禁链路：eslint 0 警告 + tsc --noEmit 0 错误 | ✅ | doc/evidence/v0.1.0/M0-03.log | `make lint` |
| M1-01 | M1 | 天文-太阳赤纬锚点（单测）：年积日 N 按 SPEC-4.1「1 月 1 日 N=1，按 UTC 日期计，闰年 2 月 29 日顺次计入」取值，代入公式计算春分(3-20)/夏至(6-21)/冬至(12-21)对应的太阳赤纬 δ，输出应分别 ≈0°/+23.44°/−23.44°（容差 ±1°，SPEC-4.4） | ✅ | doc/evidence/v0.1.0/M1-01.log | `make test TEST=solar` |
| M1-02 | M1 | 天文-均时差幅度与正午经度（单测）：年积日 N 取 SPEC-4.1 定义（SPEC-4.2 同一起点），按 SPEC-4.2 计算全年均时差 EoT，其绝对值幅度 ≤17 分钟；取 EoT≈0 的日期（如 4-15 前后）在 UTC 正午（h_UTC=12）代入 SPEC-4.3 经度公式，太阳直下点经度 ≈0°（容差 ±1°，SPEC-4.4） | ✅ | doc/evidence/v0.1.0/M1-02.log | `make test TEST=solar` |
| M1-03 | M1 | 天文-经度归一化、sunDir 坐标约定与更新频率（单测）：SPEC-4.3 经度结果归一化落在 (−180°, 180°] 区间（含跨 ±180° 边界用例）；由直下点 (lat, lon) 按 SPEC-4.5 生成的 sunDir 为单位向量，且分量遵循 SPEC-6.2 坐标约定（北极 (90,·)→+Y，(0,0)→+Z，(0,90°E)→+X，与 geo.latLonToVector3 同一约定）；注入两个相差 >1 分钟的时刻，uSunDir 必须发生变化（SPEC-4.5「每帧按当前时刻更新，可降频至 1 次/分钟」的更新频率下限校验） | ✅ | doc/evidence/v0.1.0/M1-03.log | `make test TEST=sun` |
| M1-04 | M1 | 地球几何与相机初始参数（单测）：SphereGeometry 分段 ≥64、球半径 1.0；相机 fov=45°、初始距离 3.2（SPEC-3.1） | ✅ | doc/evidence/v0.1.0/M1-04.log | `make e2e TEST=globe-init` |
| M1-05 | M1 | 昼夜混合与晨昏线校准（视觉，需附截图）：自定义 shader 按 `t=dot(N,sunDir)` 混合昼/夜纹理，晨昏线在 t∈[-0.1,+0.1] 区间呈现 smoothstep 软过渡带（SPEC-3.2）；给定已知 sunDir，格林尼治 (lat 0, lon 0) 标记点须落在昼纹理非洲西侧几内亚湾位置（SPEC-3.6） | ✅ | doc/evidence/v0.1.0/M1-05.log | `make e2e TEST=day-night-calibration` |
| M1-06 | M1 | 夜景灯光增益下限（单测）：shader uniform `uNightGain` 默认值 ≥1.5（SPEC-3.3「亮度增益 ≥1.5」的机械化断言）。昼半球不叠加灯光的判据见 M1-14 | ✅ | doc/evidence/v0.1.0/M1-06.log | `make test TEST=earth-night-gain` |
| M1-07 | M1 | 大气菲涅尔辉光（视觉截图 + 单测）：球缘可见主色 `#4a90d9` 的菲涅尔边缘辉光，由球缘向外衰减（SPEC-3.4，截图判据）；「不遮挡标记」经机械代理断言——大气材质满足 `AdditiveBlending` + `depthWrite=false` + `transparent`（SPEC-3.4 推论；M1 无标记层，不可直接验证标记不被遮挡，该项验证顺延至 M2 标记层场景） | ✅ | doc/evidence/v0.1.0/M1-07.log | `make e2e TEST=atmosphere-glow` |
| M1-08 | M1 | 星空背景（单测+视觉，需附截图）：程序化点星 ≥1500 颗分布于半径 ≥40 球壳（单测校验数量与分布半径）；相机旋转时星空随相机运动、地球自转时星空图案不随之旋转（e2e 对比两组截图验证，SPEC-3.5） | ✅ | doc/evidence/v0.1.0/M1-08.log | `make e2e TEST=starfield` |
| M1-09 | M1 | 拖拽旋转与惯性衰减（e2e）：拖拽作用于**相机**（绕球心的方位角/仰角，地球本体不因拖拽转动，SPEC-7.1）——水平方位角绕 Y 轴无限制（可累计超 360°），垂直仰角被限制在纬度 ±85° 以内不可拖出；释放拖拽后相机方位角/仰角速度按阻尼系数 ≈0.95/帧（以 60fps 为基准，SPEC-7.5）衰减，逐帧减速趋于停止（SPEC-7.1） | ✅ | doc/evidence/v0.1.0/M1-09.log | `make e2e TEST=drag-inertia` |
| M1-10 | M1 | 缩放范围限制（e2e）：滚轮/双指缩放使相机距离被限制在 [1.8, 6] 区间内，向内/向外超界输入均不能突破该范围（SPEC-7.2） | ✅ | doc/evidence/v0.1.0/M1-10.log | `make e2e TEST=zoom-range` |
| M1-11 | M1 | 空闲自转与输入打断（e2e）：无任何输入持续 10s 后地球本体开始约 0.02°/帧（以 60fps 为基准，SPEC-7.5）的缓慢自转，作用于地球本体绕 +Y 轴，相机与星空不动（SPEC-7.3）；自转期间任意拖拽/滚轮输入须立即使自转停止（SPEC-7.3） | ✅ | doc/evidence/v0.1.0/M1-11.log | `make e2e TEST=idle-spin` |
| M1-12 | M1 | 几何 uv 与 SPEC-6.2 坐标一致性（单测）：`createEarthGeometry()` 生成几何体上，uv≈(0.5,0.5) 处顶点位置 ≈ `latLonToVector3(0,0)`；uv≈(0.75,0.5) 处顶点 ≈ `latLonToVector3(0,90)`；uv.y=1 处顶点为 +Y 北极——证明纹理 u 方向向东递增且无南北翻转（SPEC-3.6 + SPEC-6.2） | ✅ | doc/evidence/v0.1.0/M1-12.log | `make test TEST=earth-geometry` |
| M1-14 | M1 | 昼半球不叠加夜景灯光（e2e，真实 WebGL 渲染）：取样点不可写死——须由当前 `uSunDir` 反算，或先转 `earthGroup.rotation.y` 把昼区转进视野，以避免直下点扫经导致可见半球全为夜侧的偶发红；在昼半球内部（t = dot(N, sunDir) 明显大于 SPEC-3.2 过渡带上界 +0.1）取样点的最终颜色不随 `uNightGain` 变化，而夜半球取样点随之变化——证明灯光项在纯昼端点的混合权重为 0、昼夜为混合而非叠加（SPEC-3.3 第二句 + SPEC-3.2 混合定义）。禁止断言 shader 源码字符串、禁止移植 GLSL 混合公式再对该移植断言（同义反复） | ✅ | doc/evidence/v0.1.0/M1-14.log | `make e2e TEST=day-side-no-lights` |
| M2-01 | M2 | GeoEvent 归一化模型字段与坐标一致性（单测）：任意一条归一化后的 GeoEvent 须满足 SPEC-6.1 定义的字段与类型约束——`id` 形如 `{source}:{原始id}` 且全局唯一；`category` 属六值枚举之一；`severity`∈{1,2,3}；`urls` 为数组，除 flight 外长度 ≥1；`lat`/`lon` 为 WGS84 度数、`ts` 为 epoch ms、`source` 属枚举字符串集合；由其 lat/lon 生成的三维坐标须遵循 SPEC-6.2 约定（与 `geo.latLonToVector3` 同一约定：北极 (90,·)→+Y，(0,0)→+Z，(0,90°E)→+X）。GDACS/LL2 字段来源已由 SPEC-5.3/5.5 精确 pin（REV-008 §6.5），本场景对四源归一化按对应条目做精确字段断言，不限于结构不变量 | ✅ | doc/evidence/v0.2.2/M2-01.log | `make test TEST=geoevent-model` |
| M2-02 | M2 | 同 id 更新去重与过期计时基准（单测，v0.2.3/REV-009 §1 裁决一改写过期半判据）：同一 `id` 的事件再次出现时覆盖 ts/severity/summary 且不产生新标记（去重键为 id，SPEC-6.3 第一句，不变）；过期计时基准为事件最后一次被 upsert 的墙钟时刻 `lastSeen`、而非事件时间 `ts`（SPEC-6.3①）——事件在其 `lastSeen` 之后连续超过期窗（48–72h 区间内、具体值可配，不预设精确数字）未被任何源再次 upsert 才移除；只要每轮仍被 upsert 即续期留屏，即便 `ts` 陈旧、即便 `ts` 早于过期窗也不影响存活（关键断言：陈旧 ts 但持续被 upsert 的事件不被 sweepExpired 清扫）；`load` 回填不触发清扫，且保留其携带的持久化 `lastSeen`、不重置为当前时刻（冷启动语义，SPEC-6.3①）。SPEC-6.3① flight-60s 子句不在本场景：M2 无 opensky 源，依 REV-007 F-1 裁决改挂 M3 FM-12（opensky provider 场景）承接，见 BUG-016 | ✅ | doc/evidence/v0.2.2/M2-02.log | `make test TEST=store` |
| M2-03 | M2 | 独立轮询与限流预算（单测）：不同数据源的调度按各自独立的轮询周期触发，一源的轮询节奏或限流预算不与另一源共享或相互干扰（SPEC-5.0「每源独立轮询与限流预算」） | ✅ | doc/evidence/v0.2.0/M2-03.log | `make test TEST=scheduler` |
| M2-04 | M2 | HTTP 失败退避、条件请求与故障隔离（单测）：单源连续请求失败时重试间隔按「基础间隔×2^n」指数增长且不超过 30 分钟上限（SPEC-5.0 退避条款）；对声明支持 ETag/Last-Modified 的源发起请求时附带对应的条件请求头（SPEC-5.0 条件请求条款）；任一源持续故障（如网络错误/5xx）不得导致其他源的轮询停止、也不得影响渲染主循环（SPEC-5.0 故障隔离条款） | ✅ | doc/evidence/v0.2.0/M2-04.log | `make test TEST=backoff` |
| M2-05 | M2 | USGS provider 字段映射、severity 三档与启动回填（单测）：真实 fixture（M2.5+ 显著性 feed 样本，头注抓取时间）归一化后 id=usgs:{feature.id}、title=M{mag} {place}、lat/lon 与 geometry 一致、ts=properties.time、urls=[properties.url]（SPEC-5.1 映射条款）；构造输入验证 severity 边界 mag=4.5/6.0 分别落 2/3 档、[2.5,4.5) 内一点（如 3.0）落 1 档（SPEC-5.1 severity 条款，与 feed 门槛正交）；provider.poll 依 ctx.firstRun 请求 2.5_day.geojson，其余轮询请求 2.5_hour.geojson（SPEC-5.1 端点与启动回填条款）。判据经 REV-012 裁准的 spec 修改而变（v0.2.8），原 ✅（doc/evidence/v0.2.1/M2-05.log）系旧契约下取得，回退重测；输出字段满足 SPEC-6.1 结构不变量 | ✅ | doc/evidence/v0.2.8/M2-05.log | `make test TEST=usgs` |
| M2-06 | M2 | NASA EONET provider 字段映射、坐标降维与 severity 默认值（单测）：真实 fixture 归一化后 id=eonet:{event.id}、summary=categories[0].title、urls=sources[].url 全数组映射（SPEC-5.2 映射条款）；坐标取 geometry 数组中时间最新一条，Point 型直取该点 [lon,lat]（真实 fixture 校验），Polygon/MultiPolygon 型取该 geometry 全部坐标点经纬度包围盒中心 ((minLon+maxLon)/2,(minLat+maxLat)/2)（fixture 无真实多边形样本，改构造 geometry 校验，SPEC-5.2 坐标降维条款）；severity 恒默认 2（SPEC-5.2）；输出字段满足 SPEC-6.1 结构不变量 | ✅ | doc/evidence/v0.2.1/M2-06.log | `make test TEST=eonet` |
| M2-07 | M2 | GDACS provider 字段映射、eventid 分组坐标与 UTC 时间解析（单测）：真实 fixture 归一化后按 eventid 分组产出唯一事件（唯一 eventid 数＝产出条数，SPEC-5.3 分组条款）；坐标取该 eventid 全部 Point 中心点要素经纬度包围盒中心，单点退化为该点本身，Polygon/LineString 几何细节不参与取坐标（真实 fixture 单点与多点分组均覆盖，SPEC-5.3 坐标条款）；title=name、summary=htmldescription、urls=[url.report]（SPEC-5.3 字段条款）；ts=datemodified 须显式按 UTC 解析（真实样本+构造无时区后缀输入验证，不依赖 Date.parse 本地时区解释，SPEC-5.3 UTC 解析条款）；severity 由 alertlevel Green/Orange/Red→1/2/3（真实 fixture 覆盖 Green/Orange，Red 档构造输入补齐，SPEC-5.3 severity 条款）；category 由 eventtype∈{DR,FL}→humanitarian，其余（EQ/TC等）→disaster（真实 fixture 四类均覆盖，SPEC-5.3 category 条款）；输出字段满足 SPEC-6.1 结构不变量 | ✅ | doc/evidence/v0.2.2/M2-07.log | `make test TEST=gdacs` |
| M2-08 | M2 | Launch Library 2 provider 字段映射、坐标数值化与 severity 时序档（单测，v0.2.3/REV-009 §2 裁决二补断言全区间）：真实 fixture 归一化后 id=ll2:{results[].id}、坐标取 pad.latitude/pad.longitude 字符串数值化（SPEC-5.5 坐标条款）；title=name、summary=mission.description、ts=net（SPEC-5.5 字段条款）；urls 取 infoURLs∪vidURLs 全数组并集，二者皆空时回落自链 url（真实 fixture 同时覆盖两种情形，SPEC-5.5 urls 条款）；severity 以 net 相对当前时刻的剩余时间（仅未来方向）分档验证——T-1h 内 3、T-24h 内 2、其余 1（真实 fixture+构造边界输入覆盖）；**net 已过去（net ≤ now）方向**构造 net=now−5min/now−30min/now−2h 及边界 net=now，severity 恒为 1（不取绝对值双向对称，SPEC-5.5 修订条款，援引 REV-009 §2.5，原方向性歧义 BUG-019 已消解，判据覆盖 spec 全区间）；输出字段满足 SPEC-6.1 结构不变量 | ✅ | doc/evidence/v0.2.2/M2-08.log | `make test TEST=ll2` |
| M2-09 | M2 | 真龄清扫——真实 fixture 长寿命事件续期不被误清（单测，v0.2.3/REV-009 §1 裁决一，承接 BUG-018）：`normalizeEonet` 对 `tests/fixtures/eonet_events.json` 归一化出的全量真实 open 事件，以统一 `now`（fixture 抓取时刻）依次 `upsertMany(evts, now)` 后立即 `sweepExpired(now)`，快照仍含全部事件、0 条被清（过期计时基准为 `lastSeen` 而非陈旧 `ts`，SPEC-6.3① 续期语义：只要事件仍被源返回并被 upsert 即续期留屏，即便其 `ts` 距抓取时刻已超 72h 窗口）；该 fixture 确有部分事件 `ts` 距抓取时刻超 72h（独立核验前提真实存在，非同义反复） | ✅ | doc/evidence/v0.2.2/M2-09.log | `make test TEST=real-age-sweep` |
| M2-10 | M2 | 事件标记分类色表与 severity 分级（e2e 视觉，需附截图；单测辅助校验颜色映射函数）：六 category 标记颜色精确匹配 SPEC-3.7 色表——disaster `#ff4d4f`、conflict `#ff7a45`、humanitarian `#ffc53d`、news `#40a9ff`、launch `#b37feb`、flight `#5cdbd3`；截图对比 severity 1/2/3 三档标记基础尺寸与脉冲光环幅度依级递增，且 severity 3 呈现持续脉冲环（连续两帧截图对比光环状态变化，证明为「持续」而非静态图形）（SPEC-3.7） | 🔲 | - | - |
| M2-11 | M2 | 标记层大规模渲染路径（单测）：构造 ≥200 条 GeoEvent 输入标记层构建函数，验证渲染路径采用 instancing（如单个 `InstancedMesh`）或点精灵（`Points`/`Sprite` 材质），而非逐事件创建独立 Mesh——断言场景图/渲染对象实例数不随标记数线性增长到与事件数相等（SPEC-3.8「标记 ≥200 个时用 instancing/点精灵，不逐事件建 Mesh」；60fps 目标量测由 FM-11 性能预算基线场景承接，本场景仅覆盖渲染路径结构判据，不做帧率断言） | ✅ | doc/evidence/v0.2.5/M2-11.log | `make test TEST=markers` |
| M2-12 | M2 | 大气不遮挡真实标记——机械像素断言（e2e，真实 WebGL 渲染，禁材质代理）：标记层就绪后，在球缘/大气可见带内放置真实事件标记，直接读取该标记像素位置的渲染输出颜色，验证标记自身分类色仍可分辨、未被大气辉光完全吞没覆盖为纯大气蓝 `#4a90d9`（回补 REV-004 R-1、REV-005 K-2 裁决——不得再用材质属性字符串代理断言，SPEC-3.4「不遮挡标记」）；一并沿球缘径向多点采样大气辉光强度，验证强度在球缘切线附近最大、向外单调衰减（SPEC-3.4「从球缘向外衰减」，承接 REV-004 R-6 机械化辉光峰值判据，去除对人工判读的依赖） | ✅ | doc/evidence/v0.2.5/M2-12.log | `make e2e TEST=atmosphere-marker-occlusion` |
| M2-13 | M2 | 今日刻痕抽屉基础结构（e2e）：抽屉冷启动即收起态，提供展开/收起双向切换控件，点击展开控件后抽屉进入展开态、再次点击收起控件恢复收起态（收起/展开状态双向切换，SPEC-2.2「抽屉冷启动即收起态，提供展开/收起双向切换控件」，R3/v0.3.6 重铸，原「面板初始宽度 300px…可折叠」语义由此承接）；展开态宽度约 300px、悬浮于 canvas 之上不挤占 canvas（DOM 量测，SPEC-2.2「展开态…展开宽度约 300px」+「展开态悬浮于 canvas 之上、不挤占 canvas」）；展开态内渲染的列表条目数与当前球面已加载事件标记数一一对应，体现抽屉展开即事件列表语义（SPEC-2.2「展开后呈现事件流列表（SPEC-2.2a 列表行）」）；主区地球 canvas 占满视口宽高（SPEC-2.2「主区为全屏地球 canvas，占满视口宽高」，补齐 BUG-029 此前无人认领子句）；展开态列表行三要素齐备（分类色圆点取对应 category **色相**——不变量 A：色相不随 severity 改变，为 M2 硬断言即刻可测；圆点明度/饱和可随 severity 呈球面标记镜像（SPEC-2.2a D25 落定，取 SPEC-3.7 分级值），其单调性验证归 M3-01/M3-03 视觉批次场景、不入本场景判据 + 标题 + 相对时间），不额外渲染 severity 的数值/文字/徽章（severity 仅环境式编码、不作显式行元素=不变量 C）；列表按「距 now 时间邻近度」升序（主键为 ts 与 now 的绝对时间差升序，等距未来先于过去、再按 id 升序；SPEC-2.2a REV-012 修订排序句，本场景排序判据不变）；空状态——本场景构造事件均无守望对象设定，无事件时应落入 SPEC-2.2a 无守望对象中性空态分支（非旧文案「暂无事件」）（SPEC-2.2a「无守望对象时…显示不预设守望叙事的中性空态」）：断言先锚「渲染文本非『暂无事件』+ 中性空态元素存在」，中性空态精确文案本体 gate on aes 定稿后补精确断言（REV-022 §4.1/遗留 6）。判据经 REV-022 裁准的 SPEC-2.2/2.2a 修改而变（v0.3.6），原 ✅（doc/evidence/v0.2.5/M2-13.log）系旧契约下取得，回退重测（REV-022 §4） | 🔲 | - | - |
| M2-14 | M2 | 列表↔标记高亮双向联动（e2e）：hover 或选中事件流面板中某一列表条目时，球面对应标记进入高亮态（如尺寸/发光度变化）；反向地，hover 或选中球面某一标记时，面板中对应列表条目进入高亮态——双向均须验证（SPEC-7.4「列表 hover/选中与球面标记高亮双向联动」）；点击触发的相机飞行与详情卡弹出不属本场景，已拆挂 M3 FM-14（见 feature-matrix FM-07/FM-14 备注）。**机位注记（R3 默认态生效后适用，REV-022 §4.1/§4.3）**：事件流面板默认收起为「今日刻痕」抽屉（SPEC-2.2），验证前须先展开抽屉使列表条目可见，双向联动判据本身不变（机位调整 re-verify，非判据降级，✅ 状态不动） | ✅ | doc/evidence/v0.2.5/M2-14.log | `make e2e TEST=panel-marker-linkage` |
| M2-15 | M2 | 矢量默认风格昼夜混合与视觉参数（视觉，需附截图）：未选卫星/付费风格包的默认状态下，地球以矢量海岸线/网格 + 昼夜明暗渲染，不加载卫星纹理（SPEC-3.2②「默认风格＝轻量矢量…免大纹理」）；晨昏线仍遵循跨风格公式 t=dot(N,sunDir)、过渡带 t∈[-0.1,+0.1] smoothstep 软过渡（SPEC-3.2①）；底面昼端色 `#1f4468`、夜端色 `#0d1827`（两端均 pin）、海岸线昼端色 `#6690b3`、**默认远景画面无经纬网格**（网格色/密度断言移 M3-06 占位场景，REV-013 C-1）（SPEC-3.2a v0.2.13 底面/海岸线/网格条款）；夜半球海岸线呈现自发光辉光，断言其存在与色相 `#3a5a72`、不断言强度精确值（SPEC-3.2a 夜面条款）；夜端整体亮度暗于昼端（SPEC-3.2a「QA 断言夜端暗于昼端即可」）；矢量风格夜面表达不依赖夜纹理、昼半球不叠加发光（SPEC-3.3 第二句）。判据经 REV-013 裁准的 spec 修改而变（v0.2.13），原 ✅（doc/evidence/v0.2.4/M2-15.log）系旧色值下取得，回退重测 | ✅ | doc/evidence/v0.2.15/M2-15.log | `make e2e TEST=vector-earth-style` |
| M2-16 | M2 | 矢量海岸线几何与坐标系对齐（单测）：矢量默认风格海岸线几何数据的顶点坐标须遵循 SPEC-6.2 坐标约定、与 `geo.latLonToVector3` 同一约定（北极(90,·)→+Y，(0,0)→+Z，(0,90°E)→+X）；格林尼治(lat 0, lon 0)在该几何上对应位置须落于非洲西侧几内亚湾方向而非镜像/翻转，作为矢量风格下 SPEC-3.6 经度对齐判据的等价验证（SPEC-3.6 + SPEC-3.2②）；本场景仅覆盖矢量自身几何判据，不承接卫星纹理切换的 M1-05/M1-14 交互项（该交互缺陷已挂起于 REV-010 §3.1 BUG-020） | ✅ | doc/evidence/v0.2.4/M2-16.log | `make test TEST=coastline-geo-alignment` |
| M2-17 | M2 | 卫星昼夜底图退出默认加载路径（单测）：应用默认风格状态下不请求/加载卫星纹理资源 `earth_day.jpg`/`earth_night.jpg`，该资源随付费天气风格包懒加载、不计入首包（SPEC-3.2③「卫星昼夜底图退出默认…并入付费天气风格包」+ SPEC-3.9「风格包资源懒加载：未解锁/未选用不下载，不计入首包」）；仅用户显式切换至卫星/天气风格时才触发加载 | ✅ | doc/evidence/v0.2.4/M2-17.log | `make e2e TEST=satellite-lazy-load` |
| M2-18 | M2 | 顶栏品牌名与 UTC 时钟（e2e）：顶栏渲染品牌名文本「Worlens」（SPEC-2.1「品牌名『Worlens』（M2）」）；顶栏高度为 48px（DOM 量测，SPEC-2.1「顶栏（48px）」）；顶栏渲染 UTC 时钟文本，格式为两位小时:两位分钟:两位秒 + 空格 + `UTC` 后缀（如 `08:15:32 UTC`，SPEC-2.1「UTC 时钟…格式 HH:MM:SS UTC」）；时钟为实时刷新——间隔 >1s 采样两次时钟文本，两次取值不同，证明随真实时间推进而非静态占位文本（SPEC-2.1「UTC 时钟（M2，实时刷新…）」）。加密行情 ticker 已删除（R2/D31，原 SPEC-5.7），顶栏永不含 ticker | ✅ | doc/evidence/v0.2.10/M2-18.log | `make e2e TEST=topbar-brand-clock` |
| M2-19 | M2 | 六分类过滤控件——首屏可见性与过滤生效（e2e）：首屏渲染六个分类开关控件，分别对应 SPEC-3.7 六值 category 枚举（disaster/conflict/humanitarian/news/launch/flight）（SPEC-2.4①「基础分类过滤（六 category 开关）」）；六开关无需打开设置面板等额外导航即可直接可见与操作，抽屉默认收起不影响六开关首屏呈现（SPEC-2.4①「作为首屏可见控件（不进设置面板）」+ SPEC-2.2 末句「顶栏…与基础分类过滤六开关…首屏呈现不由本条改动」，歧义1-A 六开关不退位）；该六开关即 SPEC-8.1 定义的「分类（category 集合）过滤」——独立的环境显示筛选、非守望对象（不产生守望命中、不进「或」守望集）——前移至 M2 的落地（守望对象三类「地方/人/主题守望」另归 M3 最小/M4 精化、与分类相区分，SPEC-8.1）——关闭某一分类开关后，该 category 的事件从球面标记与事件流面板列表中被过滤移除；重新开启后恢复显示；支持任意多个分类同时开启的子集组合而非互斥单选（集合语义，SPEC-8.1「分类（category 集合）」），验证过滤对球面标记与事件流面板列表两处同步生效（SPEC-2.4①「基础分类过滤」语义 + SPEC-2.2 事件流面板同源事件集；**机位注记：核对事件流面板列表条目须先展开「今日刻痕」抽屉**，抽屉默认收起态下列表不可见，SPEC-2.2）；空状态断言 ×2（关闭 disaster+humanitarian 两类后列表空；六类全关后列表空）——本场景构造事件均无守望对象设定，两处空列表均落入 SPEC-2.2a 无守望对象中性空态分支（非旧文案「暂无事件」）（SPEC-2.2a「无守望对象时…显示不预设守望叙事的中性空态」；全关场景对应 REV-022 §3 歧义3-A「分类全关空列表套用同一空态语义分支、不新增区隔特例」，因本场景无守望对象故落中性空态而非安好态）：断言先锚「渲染文本非旧文案『暂无事件』+ 中性空态元素存在」，中性空态精确文案本体 gate on aes 定稿后补精确断言（REV-022 §4.1/遗留 6）。判据经 REV-022 裁准的 SPEC-2.2a 空态子句修改而变（v0.3.6），原 ✅（doc/evidence/v0.2.10/M2-19.log）系旧契约下取得，回退重测（REV-022 §4） | 🔲 | - | - |
| M2-20 | M2 | 缓存优先启动——冷启动先渲染本地缓存事件、不空屏等网络（单测，数据层启动顺序）：预先经 `EventCache.persist` 写入若干条模拟「上次会话遗留」的事件记录（SPEC-8.4「事件缓存…升格为启动路径的一部分（SPEC-3.11）」——缓存回填须作为启动流程的必经步骤，不可被跳过/静默忽略）；令首轮网络请求人为长时间不 resolve（模拟网络尚未完成），随后调用数据层启动入口。断言：在网络首轮仍未完成期间，事件读口（store）中已出现源自缓存的事件——以此作为「渲染上次本地缓存事件（球面标记）」的机械化代理（标记层订阅同一 store 即渲染，故 store 提前出现缓存事件等价于标记提前上屏），证明缓存回填不依赖、不等待网络首轮完成（SPEC-3.11「冷启动即渲染上次本地缓存事件（球面标记），不空屏等待网络」）。另覆盖边界：本地无缓存，或缓存读取本身失败（如底层存储异常），启动流程均不得因此阻塞或抛出未捕获异常，网络轮询仍应照常启动推进（SPEC-8.4「仍不承诺离线数据完整性」——缓存不完整或读取失败不得使启动卡死）。SPEC-3.10「缓存事件上屏 ≤1s」的时长验收基准属 M5（本场景不做计时断言），M2 基线量测另挂 FM-11，不在本场景判据内 | ✅ | doc/evidence/v0.2.13/M2-20.log | `make test TEST=cache-first-start` |
| M2-21 | M2 | 呼吸式过渡——首轮刷新收敛时旧标记渐隐、新标记渐亮、无整屏闪烁（e2e 视觉，需附截图，连续多帧采样）：冷启动以一组缓存事件上屏后，令首轮网络刷新返回一组与缓存存在差异的事件结果（部分事件不再被返回／视为已过期或被替换，部分为新增或更新的最新事件）。在该轮刷新收敛过程中连续采样多帧（非仅刷新前/后两张静态截图）：①不再出现于最新结果中的旧标记，其可见状态（透明度/尺寸等）须随时间连续衰减至消失，而非单帧内瞬间消失（SPEC-3.11「已过期/被替换的旧标记渐隐熄灭」）；②新增/更新的最新事件标记，其可见状态须随时间连续增长至正常态，而非单帧内瞬间出现（SPEC-3.11「最新事件渐亮」）；③过渡期间未涉及增减变化的既有标记须保持连续可见，不因该轮刷新的整体重建出现全屏级别的瞬间清空/重绘闪烁（SPEC-3.11「不做整屏重绘闪烁」） | ✅ | doc/evidence/v0.2.13/M2-21.log | `make e2e TEST=marker-breathing-transition` |
| M2-22 | M2 | 事件流排序——时间邻近度升序（单测/组件测,mock now 确定性构造）:混合场景一条未来事件(ts=now+若干天)与一条过去事件(ts=now−几分钟)→ 过去者在上(直锁 BUG-024 首屏被最远未来占据);全未来退化(+1h/+12h/+9d → +1h 在顶,倒计时序);全过去退化(等价 ts 倒序,最新在上,零回归);等距 tie-break 未来先于过去、再 id 升序(排序判据不变,SPEC-2.2a REV-012 修订排序句,主键为 ts 与 now 的绝对时间差);空态断言——无事件时渲染不崩溃 + 渲染文本非旧文案「暂无事件」(本单测无守望对象上下文构造,依 SPEC-2.2a 无守望对象中性空态分支,不断言中性空态精确文案,文案本体 gate on aes 定稿,SPEC-2.2a「无守望对象时…显示不预设守望叙事的中性空态」)。判据经 REV-022 裁准的 SPEC-2.2a 空态子句修改而变(v0.3.6),原 ✅(doc/evidence/v0.2.8/M2-22.log)系旧契约下取得,回退重测(REV-022 §4) | 🔲 | - | - |
| M2-23 | M2 | 首包体积基线量测——构建产物体积统计 + 风格包资源代码分割隔离（构建产物度量，非 e2e）：对 `npm run build` 构建产物做体积统计，记录主入口（首包，不含卫星纹理/风格包资源）JS/CSS 总体积基线数值并写入证据（SPEC-3.10「首包 ≤2MB（不含可选纹理/风格包）」）；SPEC-3.10 明示「验收基准环境与量测方法由 M5 testplan 场景定义（M2 建基线量测）」，故本场景只要求量测流程可复跑、产出有限非负体积数值，不把 ≤2MB 门槛作为本场景 M2 通过条件，达标判定留待 M5；结构性断言（M2 可直接判定，不依赖验收环境）：卫星昼夜纹理 `earth_day.jpg`/`earth_night.jpg`（SPEC-3.2③）与风格包资源不出现在主入口 chunk 的静态引用/构建产物清单中，须位于独立的懒加载 chunk（SPEC-3.9「风格包资源懒加载：未解锁/未选用不下载，不计入首包」+ SPEC-3.10 同款「风格包与卫星/天气纹理一律懒加载，不计入首包」）；与 M2-17（运行时不请求判据）互补，本场景断言构建产物层面的物理隔离 | ✅ | doc/evidence/v0.2.20/M2-23.log | `make test TEST=build-budget` |
| M2-24 | M2 | 冷启动计时与帧率基线量测（e2e，非阈值门禁）：量测从页面导航开始到地球可交互（初始渲染完成、输入可响应）所耗时间，记录为「冷启动→可交互地球」基线数值（SPEC-3.10「中端手机冷启动→可交互地球 ≤3s」）；同批预置本地缓存事件，量测从启动到缓存事件（球面标记）首次出现于渲染输出所耗时间，记录为「缓存事件上屏」基线数值（SPEC-3.10「缓存事件上屏 ≤1s（SPEC-3.11）」，承接 M2-20 搁置项「M2 基线量测另挂 FM-11」）；并连续采样多帧计算帧率均值，记录为帧率基线数值（SPEC-3.8「桌面 Chrome 目标 60fps」，承接 M2-11 搁置项「60fps 目标量测由 FM-11 性能预算基线场景承接」）；SPEC-3.10 明示「验收基准环境与量测方法由 M5 testplan 场景定义（M2 建基线量测）」——本机/CI 非中端手机验收环境，三项数值均只要求量测流程可复跑、产出有限非负数值并入证据，不对数值做 ≤3s/≤1s/≥60fps 门禁断言，达标判定留待 M5 | ✅ | doc/evidence/v0.2.20/M2-24.log | `make e2e TEST=cold-start-perf` |
| M2-25 | M2 | 标记层首批 snap 对照增量呼吸淡入（单测，标记层透明度通道初值判据，承接 REV-014 §4-3 / BUG-030 复验，防新 pin 子句静默蒸发）：新建标记层（此前从未 setEvents，无任何前态在屏）首次调用 setEvents 传入非空事件集合，断言新分配槽位的标记透明度初值即为满值，未经任何过渡步进即已是终值——即「snap，不淡入」（SPEC-3.11 消歧句「当无任何前态在屏……时，首个非空事件批次直接上屏——标记透明度即置满、不淡入」+ SPEC-3.2①「资源加载完成后直接替换，无淡入过渡」）。对照：先以一批事件建立在屏前态，再次 setEvents 追加一枚新事件，断言该新增标记透明度初值为 0（非满值），须经推进真实毫秒累计的过渡步进才连续坡向满值，中途存在严格介于 0~1 的过渡态而非瞬间跳变（SPEC-3.11「已过期/被替换的旧标记渐隐熄灭、最新事件渐亮」呼吸过渡句 + D27「呼吸动效只用于增量：①冷启动加载缓存→首轮刷新完成后，旧点呼吸离场、新点呼吸登场；②运行中……仅对过时/新增的更新做呼吸登/离场」）。另登独立场景、不并入 M2-20（数据层启动顺序判据）/M2-21（有前态收敛过程判据），理由见 REV-014 §4-3 | ✅ | doc/evidence/v0.2.16/M2-25.log | `make test TEST=marker-initial-snap` |
| M3-01 | M3 | 事件流行 severity 单调编码——不变量 B(e2e 视觉):主通道=列表行标题文本明度:sev3 `#eef2f7` 亮于 sev2 `#c2ccd8` 亮于 sev1 `#8794a3`,三档 sRGB 亮度与对面板底 WCAG 对比单调递增、全档 ≥4.5:1 AA;行首分类色圆点镜像球面 severity 变换(色相=分类不随 severity,明度/饱和随 severity 取 SPEC-3.7 分级值)(SPEC-2.2a v0.2.13 D25 落定,REV-013 改动 9)。归 D24/D25 视觉批次,非 M2 硬门槛;判据已转可测,仍须实测证据方可 ✅。**机位注记(R3 默认态生效后适用,REV-022 §4.1)**:事件流面板默认收起为「今日刻痕」抽屉(SPEC-2.2),验证前须先展开抽屉读取列表行标题明度;本场景亦量 `.event-panel` 底色算 WCAG 对比,须展开抽屉后量**展开态**底色(而非收起态),severity 单调编码判据本身不变(机位调整 re-verify,✅ 状态不动) | ✅ | doc/evidence/v0.2.19/M3-01.log | `make e2e TEST=event-row-severity` |
| M3-02 | M3 | 昼夜半球柔和对比(视觉,需附截图):主判据——晨昏线两侧同纬度约 ±40° 对称采样点的底面亮度比落入 [1.8,2.6];副判据——昼半球内「次日点亮度 : 近晨昏线昼侧亮度」≥1.3:1(SPEC-3.2a 对比契约 + SPEC-3.2① 连续衰减,v0.2.13)。量测方法依 REV-013 硬条件 C-2 定死:亮度定义=gamma 编码 Rec.709 luma(0.2126R+0.7152G+0.0722B,对 0-255 sRGB 值直接加权、不线性化,与 aes 基线同法);采样几何为晨昏线两侧同纬度对称点;亮像素排除阈值在测试注释定稿;对照基线=改造前实测 1.11:1。归 D24/D25 视觉批次,非 M2 硬门槛 | ✅ | doc/evidence/v0.2.17/M3-02.log | `make e2e TEST=hemisphere-contrast` |
| M3-03 | M3 | 标记 severity 明度/饱和/发光分层(视觉,需附截图):同屏构造 sev1/sev2/sev3 标记——sev3 有持续脉冲辉光、sev2 静态柔光环无脉冲、sev1 无辉光(存在性硬断言);分级色按乘子规则容差断言(相对 sev3:sev2 为 S×0.82/L×0.93,sev1 为 S×0.60/L×0.82;断言取 REV-013 §3.1 两等价路径之一,ε 在测试注释定稿,建议每通道 ±6/255 量级);不变量 A:色相跨 severity 恒定(允 ±2° 取整漂移)(SPEC-3.7 v0.2.13 分层条款)。归 D24/D25 视觉批次 | 🔲 | - | - |
| M3-04 | M3 | 星空不盖过信息层(视觉):任意机位星点最大亮度不高于昼面海岸线 `#6690b3` 亮度(SPEC-3.5 v0.2.13 追加句)。独立场景,不回退既有 M1-08(其判据不变)。归 D24/D25 视觉批次 | ✅ | doc/evidence/v0.2.18/M3-04.log | `make e2e TEST=star-brightness-cap` |
| M3-05 | M3 | 面板不亮于球(视觉):事件流面板底色亮度不高于夜半球底面 `#0d1827` 亮度(SPEC-2.2 v0.2.13 追加句,R3/v0.3.6 平移为**抽屉展开态**同一 cap、语义与取值不变,M3-05 锚)。独立场景,不并入 M2-13。**机位注记(R3 默认态生效后适用,REV-022 §4.1)**:抽屉默认收起,量测对象明确为**抽屉展开态**底色,验证前须先展开抽屉再量(非收起态,机位调整 re-verify,✅ 状态不动)。归 D24/D25 视觉批次 | ✅ | doc/evidence/v0.2.18/M3-05.log | `make e2e TEST=panel-brightness-cap` |
| M3-06 | M3 | 近景 LOD 档网格显示(占位,视觉第二批):近景/调试档启用经纬网格时线色 `#1e3a5f`、密度经线/纬线各 30°、赤道与极区不加密(SPEC-3.2a v0.2.13 网格条款启用态子句;REV-013 硬条件 C-1 占位防蒸发;随相机距离的显隐规则待视觉第二批提案后补判据) | 🔲 | - | - |
| M3-07 | M3 | 信源→信任等级映射(单测,D22 最小信任层):GeoEvent `source`(SPEC-6.1 枚举)经 SPEC-5.10 表派生显示名+信任等级——`usgs`→「USGS」/「权威事件源」;`eonet`→「NASA EONET」/「权威事件源」;`gdacs`→「GDACS」/「权威事件源」;`ll2`→「Launch Library 2」/「权威事件源」;`opensky`→「OpenSky」/「权威事件源」;`gdelt`→「GDELT」/「新闻报道(待验证)」(SPEC-5.10 信源→显示名+信任等级表);分级为**信源级常量**、由 `source` 派生而不入模型——同一 `source` 值多次派生结果一致,且不要求/不断言 GeoEvent 对象新增 `tier` 类字段(SPEC-5.10「分级为信源级常量…不新增 GeoEvent 字段」,与 SPEC-3.7 category→颜色派生同构先例一致,REV-015 §2 通过)。关联 FM-14 | ✅ | doc/evidence/v0.3.4/M3-07.log | `make test TEST=trust` |
| M3-08 | M3 | 详情卡信源名+信任等级+时间语义+去重呈现(e2e,D22 最小信任层):点击球面标记或事件流列表条目弹出详情卡后,卡内可见该事件 `source` 对应的显示名与信任等级文案,且与 M3-07/SPEC-5.10 表一致(SPEC-2.3「信源名与信任等级(SPEC-5.10)」);卡内显示时间取该事件 GeoEvent `ts`(SPEC-2.3+6.1,相对+绝对并陈;不显示 `lastSeen`,SPEC-6.3①「lastSeen…不展示」+SPEC-2.3 时间语义句——仅断言取值来源为 `ts` 且相对/绝对两种呈现皆存在,**不得**断言 `ts` 即真实发布/更新时刻,该等价关系依赖尚未在 SPEC-5.4 落 pin 的 provider 映射,REV-015 R-1/R-2 强制口径);构造 `urls.length>1` 的事件,详情卡可呈现其链接条数(如「N 条链接/报道」),且呈现文案不得出现「N 个信源」或其他暗示跨源多信源汇集的表达(SPEC-2.3 去重呈现句 M-1 修正文本「呈现文案不得暗示『N 个信源』式的跨源多信源汇集」);`urls.length===1` 时不强制呈现计数(SPEC-2.3「可呈现其条数」非强制)。关联 FM-14 | 🔲 | - | - |
| M3-09 | M3 | 纠错反馈入口(e2e,D22 最小信任层):详情卡内存在一处可点击的轻量纠错反馈入口;点击后触发的目标为 `mailto:` 链接或指向仓库 issue 页面的外链(SPEC-2.3「点击打开 mailto: 或仓库 issue 链接」),链接目标/预填内容中包含该事件的 `id` 或标题等上下文信息(SPEC-2.3「预填事件 id/标题/信源以便定位」);点击该入口的过程中不产生任何指向应用自身或新增后端的网络请求(fetch/XHR),不发起任何服务端提交(SPEC-2.3「零服务器、不发起任何服务端提交」+SPEC-8.7「明示为阶段一唯一服务端组件…不承载任何产品功能」+§9「阶段一零服务器,唯一例外=匿名遥测心跳」)。关联 FM-14 | 🔲 | - | - |
| M3-10 | M3 | L0 不编码信源/分级(负向,e2e,D22 最小信任层):未打开详情卡时,球面标记本身与事件流面板列表行(SPEC-2.2a 三要素:分类色圆点/标题/相对时间)均不出现信源显示名或信任等级文本——列表行渲染内容中不包含 SPEC-5.10 显示名表任一取值(USGS/NASA EONET/GDACS/Launch Library 2/OpenSky/GDELT)或「权威事件源」/「新闻报道(待验证)」字样(SPEC-5.10「分级仅用于 L1 详情卡(SPEC-2.3);L0 球面标记与事件流列表行(SPEC-2.2a)不编码信源/分级」+D23「L0 球面(零点击)只编码位置/分类/新鲜度三变量…不编码信源」);同一事件打开详情卡后方可见上述文案(对照 M3-08),验证「仅 L1 呈现」的边界而非全局缺失。关联 FM-14 | 🔲 | - | - |
| M3-11 | M3 | 守望对象采集与本地持久化（单测，守望对象数据模型 + store 本地存，D31 R7 守望最小闭环）：三类守望对象均可被采集写入守望对象集合——地方（粗粒度地理关注/大区域，复用首启引导 SPEC-8.6 已采集、M3 不新增精细圆域交互）、人（一处地方 + 一个名字标签）、主题（关键词）（SPEC-8.1 守望对象三类 M3 最小形态 + SPEC-8.6①「守望对象采集：地方/人/主题」；采集引导 UI 流程归 M3-13/FM-15，本场景断言 store 侧数据模型与本地存）；「人」类结构含地方 + 名字标签两部分，名字纯本地永不上传——机械化断言：守望对象本地存写入/读回不触发任何对外网络请求（fetch/XHR）、名字标签不出现在任何请求 payload（SPEC-8.1「名字纯本地、永不上传」+ §9 阶段一零服务器）；多守望对象以集合承载、彼此为「或」关系（SPEC-8.1「多守望对象为『或』关系」，本场景仅断言集合承载，命中判定归 M3-12）；本地持久化——守望对象写入后模拟应用重启（重新初始化 store 从本地存回填），集合仍完整存活、可读回（SPEC-8.6「上述选择均本地持久化」+ SPEC-8.4「守望存 IndexedDB」M3 本地存闭环；IndexedDB 完整机制属 M4、本场景仅断言重启存活，机制属实现自由度）；M3 最小边界——精细圆域（中心+半径 km 精确输入）、守望持久化完整化属 M4，不在本场景（SPEC-8.1 M4 精化） | 🔲 | - | - |
| M3-12 | M3 | 仅守望命中过滤——两态切换 + 命中呈现/非命中隐藏 + 分类正交叠加（e2e，单测辅助守望匹配判定，D31 R7 守望最小闭环）：设定地方/主题守望对象后构造命中与非命中事件混合集，过滤模式提供「全部事件 / 仅守望命中」两态且可切换（SPEC-8.2 M3「过滤模式『全部事件 / 仅守望命中』两态」）；全部事件态命中与非命中均呈现，仅守望命中态仅命中事件经球面光柱（SPEC-3.7）与事件流面板列表（SPEC-2.2a）现有渲染呈现、非命中事件从球面与列表两处隐藏（SPEC-8.2 M3「仅守望命中模式下仅呈现命中事件…非命中事件隐藏」，隔离即强调、复用 SPEC-2.4① 同类显隐机制、不新增视觉轴）；守望命中判定（单测辅助）——地方守望按事件经纬度落于该粗粒度地理关注区域内判命中、主题守望按事件 title+summary 含关键词判命中、人守望按其所含地方判命中（SPEC-8.1「地方/人/主题守望共同驱动守望命中」「地方（圆域）／主题（关键词，匹配 title+summary）」+ SPEC-8.6①）；分类与守望正交叠加——关闭某分类开关后该类别事件即使命中守望也不呈现、重开后其守望命中项恢复，体现分类控类别显隐、守望控命中二者自然叠加（显示类别内的守望命中）（SPEC-8.1「分类过滤与守望命中为正交筛选轴…二者自然叠加（显示类别内的守望命中）」）；M3 最小边界——全部模式下命中项 inline 视觉强调、多守望条目组合复杂过滤属 M4，不在本场景（SPEC-8.2 M4 精化）。**机位与 R5 对齐注记（REV-022 §4.1/§5.6，R3/R5 生效后适用）**：核对事件流面板列表呈现/隐藏须先展开「今日刻痕」抽屉（SPEC-2.2 默认收起）；仅守望命中态下若命中集为零（如构造事件全部为非命中），因本场景已设定 ≥1 守望对象，应落入 SPEC-2.2a 安好态分支「你守望的世界，此刻安好」而非旧「暂无事件」——此为安好态典型触发场景（SPEC-2.2a「典型触发为『仅守望命中』模式零命中」），零命中子情形断言纳入本场景判据 | 🔲 | - | - |
| M3-13 | M3 | 守望第一问引导 + 开屏锚定回落/正向（e2e，D31 R7 守望最小闭环）：首次打开展示首启引导，第一问语义指向守望对象采集（「你守望哪里？牵挂谁？」，采集地方/人守望，承接 SPEC-1「守望」词根），而非以「你想看什么分类」为首问（SPEC-8.6「第一问 =『你守望哪里？牵挂谁？』…不再以『你想看什么分类』为首问」；断言首问语义指向守望对象采集、非分类选择，具体文案措辞属实现自由度）；垂类分流（冲突观察/航天/灾害等主题类别、驱动默认分类过滤 SPEC-2.4①）作为次级维度呈现于守望第一问之后、非首问（SPEC-8.6②「垂类分流（次级维度，退居守望第一问之后）」）；引导可跳过（SPEC-8.6「可跳过」）；开屏锚定回落（负向）——跳过引导或未选地方守望时冷启动开屏锚定回落 SPEC-3.1 默认视角，即相机位于 lat 0/lon 0 正上方、距球心 3.2、视线指向球心（几内亚湾）（SPEC-8.6「跳过或未选地方守望时，开屏锚定回落 SPEC-3.1 默认视角（几内亚湾）」+ SPEC-3.1「未选定时沿用默认」）；开屏锚定正向——引导中选定地方守望时冷启动初始视角改为近景该地方区域、覆盖默认几内亚湾（相机朝向该地方经纬度而非 lat 0/lon 0；近景具体距离/角度属实现自由度）（SPEC-3.1「若…守望选定关注区域，则冷启动初始视角改为近景该区域」+ SPEC-8.6「地方守望同时驱动开屏锚定…为 M3 开屏锚定的唯一数据源」；人所含地方本质为带标签地方守望 SPEC-8.1，同属地方锚定源） | 🔲 | - | - |
| M3-14 | M3 | 表盘默认态——冷启动静息核心（e2e，D31 R3 叙事净化批，FM-29）：冷启动首屏即为静息态——「今日刻痕」抽屉呈收起态、无常驻事件流面板占据画面（SPEC-2.2「表盘默认态（冷启动静息态）以地球为视觉主体…事件流面板不常驻画面，退位为默认收起的『今日刻痕』抽屉」+「抽屉冷启动即收起态」）；静息核心仅由三要素构成——地球 canvas、事件光柱标记（SPEC-3.7）、顶栏 UTC 时钟（SPEC-2.1「时间」）（SPEC-2.2「静息核心 = 地球 canvas + 事件光柱标记（SPEC-3.7）+ 顶栏 UTC 时钟（SPEC-2.1「时间」）」）；主区地球 canvas 占满视口宽高（DOM 量测，SPEC-2.2「主区为全屏地球 canvas，占满视口宽高」）；顶栏（SPEC-2.1）与基础分类过滤六开关（SPEC-2.4①）首屏仍可见，本条默认态改动不影响其呈现（SPEC-2.2 末句「顶栏…与基础分类过滤六开关…首屏呈现不由本条改动」，歧义1-A 六开关不退位）。M3 边界——六开关是否退位属独立产品决策，本批 defer（REV-022 §3 遗留 2），本场景仅断言当前六开关首屏可见现状、不预判其未来变动 | 🔲 | - | - |
| M3-15 | M3 | 今日刻痕抽屉展开/收起双向切换与展开态形态（e2e，D31 R3 叙事净化批，FM-29）：收起态下点击展开控件后抽屉进入展开态、呈现事件流列表（SPEC-2.2a 列表行）；展开态下点击收起控件后抽屉恢复收起态——双向切换均须验证（SPEC-2.2「提供展开/收起双向切换控件」）；展开态沿用现有事件流面板形态，悬浮于 canvas 之上、不挤占 canvas，展开宽度约 300px（DOM 量测，SPEC-2.2「展开态悬浮于 canvas 之上、不挤占 canvas，展开宽度约 300px」）；抽屉对外名「今日刻痕」——若断言呈现名称文本，允许最终措辞随 aes 定稿微调、不锁定精确字符串（SPEC-2.2「抽屉对外名『今日刻痕』（product-vision 基准，最终措辞属 aes 制定项）」）。抽屉展开态具体底色/边框/圆角/质感、收起态触发控件形态与位置、展开/收起动效不做判据（SPEC-2.2「属 UI 实现自由度」）；展开态底亮度 cap 判据不重复本场景，归 M3-05（同一 cap，机位注记见 M3-05） | 🔲 | - | - |
| M3-16 | M3 | 事件流空状态——安好态触发二分（e2e，单测辅助，D31 R5 叙事净化批，FM-29）：展开抽屉后事件流列表呈现集为空时，依守望对象有无二分呈现（SPEC-2.2a「事件流列表呈现集为空时的空状态依守望对象有无（SPEC-8.1/8.6）二分呈现」）——① 已设定 ≥1 守望对象且列表空（如仅守望命中零命中）时，显示安好态文案「你守望的世界，此刻安好」（SPEC-2.2a「有 ≥1 守望对象时显示安好态文案『你守望的世界，此刻安好』」；旗舰句本体已由 product-vision 基准给出，最终措辞属 aes 可微调项，本场景断言精确基准句或其 aes 微调后的等价句）；② 未设定任何守望对象（引导跳过/未选）且列表空时，显示不预设守望叙事的中性空态——断言先锚「渲染文本非旧文案『暂无事件』+ 非安好态旗舰句『你守望的世界』+ 中性空态元素存在」，中性空态精确文案本体 gate on aes 定稿后补精确断言（SPEC-2.2a「无守望对象时…显示不预设守望叙事的中性空态…具体文案本体属 aes 制定项」，REV-022 遗留 6）；两分支互斥——同一空列表场景按当前守望对象集合有无取其一，不同时呈现两种空态文案（SPEC-2.2a 二分语义）。搜索无匹配空态不入本场景，独立归 SPEC-2.5（SPEC-2.2a「搜索无匹配的空态由 SPEC-2.5 独立承接…不并入本条安好态」） | 🔲 | - | - |
