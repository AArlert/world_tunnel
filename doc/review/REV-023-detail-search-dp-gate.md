# REV-023 · FM-14〔详情卡 + 相机飞行 + 搜索〕design-prompt 交付门禁

- **审查对象**：`doc/design-prompt/M3-detail-search.md`（arch 新交付）
- **裁决基准**：`doc/spec.md`（v0.3.6 pin）SPEC-2.1/2.2/2.2a/2.3/2.4①/2.5、SPEC-5.10、SPEC-6.1/6.3①、SPEC-7.1/7.2/7.3/7.4、§9；REV-015 R-1/R-2、REV-018 F-3、REV-022 §6.1；兼容面 `doc/design-prompt/M3-marker-pillar.md §3.1`
- **门禁范围**：切片①（详情卡）+ 切片②（相机飞行）派单门禁。切片③（搜索）已知阻塞于 §7 GAP-1/GAP-2，按任务卡不在本门禁阻断范围内，仅核其定性与边界。
- **结论**：**通过**——切片①② design-prompt 门禁过，orch 可据此派 dev；切片③维持 §7 门禁（GAP-1+GAP-2 co-resolve 前不开卡）。附 4 项非阻断观察项（O-1~O-4），不动摇放行结论。

---

## §1 行为泄漏专查（门禁核心）——逐节 SPEC 锚点核对

判据：DP 中每个对外可见取值须有 spec 锚点，否则 = 行为泄漏、打回。逐条核对如下，**全部落锚，无泄漏**。

| DP 节 | 对外可见取值 / 行为 | SPEC 锚点核验 | 结论 |
| --- | --- | --- | --- |
| §2.1 详情卡浮层 | 点击触发 L1 浮层、一次一张、可关闭 | SPEC-2.3「点击…弹出浮层」+ SPEC-7.4「点击标记/列表条目…弹详情卡」；精确 z-index/尺寸/动效 DP 明标实现自由度 | ✅ 锚定 |
| §2.1 层级 | 渲染层级在 canvas 与「今日刻痕」抽屉之上 | L1（点击聚焦）在 L0/抽屉之上属浮层内在语义（D23 分层 + SPEC-2.3「浮层」），DP 标「浮层语义的实现，非新对外行为」；精确层级留自由度 | ✅ 非新行为 |
| §2.2 标题 | `event.title` 原文 | SPEC-6.1 `title` | ✅ |
| §2.2 分类徽章 | 主体 = 分类色 `CATEGORY_COLORS`；文字标签复用既有单一源、不另立 | SPEC-3.7（色）；文字标签见 O-1（M2 遗留、非本卡引入） | ✅（色）/ O-1（标签） |
| §2.2 severity | 渲染数值 `1\|2\|3`，**禁**新造「轻/中/重」文字档位 | SPEC-2.2a「其数值展示属详情卡 SPEC-2.3」+ SPEC-6.1 + SPEC-2.3 | ✅ 且主动堵泄漏 |
| §2.2 时间 | 取 `ts`，相对+绝对并陈；不展示 `lastSeen`；**不得断言 ts=真实发布时刻** | SPEC-2.3 时间语义 + SPEC-6.1 `ts` + SPEC-6.3①；**REV-015 R-1/R-2 口径逐字守住**（见 §1.1） | ✅ 合 R-2 |
| §2.2 地点 | 格式化 `lat`/`lon` 坐标；坐标格式自由度 | SPEC-6.1 `lat`/`lon`；无人读地名字段属直读，见 O-2 | ✅ |
| §2.2 摘要 | `event.summary` 原文，可空串 | SPEC-6.1 `summary`（明载可为空串） | ✅ |
| §2.2 信源名+等级 | **唯一取自** `getSourceTrust(event.source)`；禁另写字面量 | SPEC-5.10 单一源（`trust.ts`）+ SPEC-2.3 | ✅ 且主动堵泄漏 |
| §2.2 链接列表 | `urls` 逐条外链；>1 条**可**呈现条数「N 条链接/报道」，**不得**暗示「N 个信源」跨源 | SPEC-2.3 去重呈现句（REV-015 M-1 修正文本逐字对齐）+ SPEC-6.1 `urls` | ✅ |
| §2.3 纠错入口 | mailto:/issue 外链、预填事件 id/标题、**零 fetch/XHR、零服务端提交** | SPEC-2.3 纠错句 + §9 阶段一零服务器 + SPEC-8.7；渠道二选一自由度 | ✅ |
| §2.4 飞行时长 | **800ms（pin、非自由度）** | SPEC-7.4「相机 800ms 缓动飞行」——**无漂移**（§2.4/§3.2「durationMs 缺省 800」/§5.2「≈800ms」皆同源；≈ 系 e2e 量测容差非重定义） | ✅ pin 无漂移 |
| §2.4 仰角钳位 | polar clamp ±85° | SPEC-7.1「垂直视角限制在纬度 ±85°」 | ✅ |
| §2.4 目标距离 | 落在 `[1.8,6]`，具体值自由度 | SPEC-7.2「相机距离 ∈ [1.8,6]」；DP 正确指出 SPEC-7.4 未 pin 距离、只 pin 800ms+「上空」 | ✅ 未越界定值 |
| §2.4 飞行瞬态 | 800ms 后归静止、非常驻动画；输入可中断 | SPEC-3.11a 稳态零动画（稳态=无输入时，飞行是显式输入结果）+ SPEC-7.3「任何输入立即停」；DP 自洽调和到位 | ✅ 无冲突 |
| §2.5 搜索范围 | 本地缓存事件 + 本地 gazetteer、零网络、非全文检索 | SPEC-2.5 + §9 | ✅（地名半部见 GAP-2） |
| §2.5 缓存匹配 | 标题 + 信源名子串 | SPEC-2.5 + SPEC-6.1 `title` + SPEC-5.10 显示名 | ✅ |
| §2.5 空态 | 「当前缓存内无匹配」，**独立于** SPEC-2.2a 安好态 | SPEC-2.5 空态句 + SPEC-2.2a 末句「搜索无匹配的空态由 SPEC-2.5 独立承接…不并入本条安好态」 | ✅ 边界正确 |
| §2.6 L0 负向 | trust 表任一取值只现于 `DetailCard.tsx`；`EventPanel`/`markers` 不 import 不渲染 | SPEC-5.10「L0…不编码信源/分级」+ D23 L0 三变量 | ✅（结构保证见 §3） |

**§1.1 REV-015 R-1/R-2 强制口径专核（任务卡重点）**：
DP §2.2 时间字段行断言口径明文写「仅可断取值来源为 `ts` 且相对/绝对两呈现皆存在，**不得断言 `ts`=真实发布/更新时刻（REV-015 R-1/R-2）**」，§5.2 M3-08 检查点复述同一约束。逐字比对 REV-015 R-2（本文 line 106）「不得断言『`ts`＝真实发布时刻』（依赖 R-1 未 pin 的 provider 映射，属判据向未定契约看齐）」——**完全一致，未向 SPEC-5.4 未 pin 的前向映射看齐**。R-1（SPEC-5.4 `ts` 映射前向 pin）非本卡处置项，DP 未违反、正确回避。**通过**。

**行为泄漏专查结论：通过。** 全部对外可见取值落 spec 锚点或经 §7 缺口挂起（不默选）；无一处在 DP 内定义 spec 之外的对外可见行为。DP 且主动堵三处泄漏隐患（severity 文字档位、信源名字面量、跨源「N 个信源」暗示），符合 arch.md 行为泄漏禁区口径。

---

## §2 接口自洽核验（对照 src 实测）

- `getSourceTrust(source: SourceId): SourceTrustInfo` / `SOURCE_TRUST` / `TrustTier`（`src/data/trust.ts` 实测存在）——DP §3.1 签名与 §2.2/§2.6 消费口径**与实现一致**；`TrustTier` 字面量即 SPEC-5.10 用户可见文案，DP「唯一取自 getSourceTrust」自洽。
- 现 trust 表 importer 仅 `trust.ts` + `tests/trust.test.ts`；`EventPanel.tsx`/`markers.ts` 实测**未 import trust**——DP §2.6 断言 `DetailCard.tsx` 将成唯一 UI 消费方**成立**，L0 负向结构保证当前为真、DP 要求「维持」而非「新建」，口径准确。
- `MarkerLayer.pick(raycaster: THREE.Raycaster): string | null`（markers.ts:81/186 实测存在）+ `GlobeScene.onMarkerHover`（GlobeScene.ts:40）+ `pick` 经 raycaster 求值（:211）——DP §3.3 新增 `onMarkerClick?`（对称于 `onMarkerHover`）、复用 `pick`、`GlobeControls.flyTo`/`GlobeScene.flyTo`（新增，向后兼容）**接口自洽、不破坏既有调用方**。
- BUG-021 置空处理实测在 markers.ts:165——DP §4.3/§4.5 兼容面提示准确。

**接口自洽结论：通过。**

---

## §3 场景可测性核定（M3-08 / M3-09 / M3-10）

对照 `doc/testplan.md` 三行现判据（均 🔲）：

- **M3-08**（信源名+信任等级+时间语义+去重呈现）：DP §3.1 组件契约（`DetailCard(event, onClose)`）+ §2.2 逐字段口径使其可测——两路径（标记/列表条目）弹同一卡、卡内信源名+等级对 `trust.ts`/SPEC-5.10 表、时间取 `ts` 且相对/绝对两呈现（守 R-2 不断言真实发布时刻）、`urls.length>1` 条数文案不暗示跨源。**可测。**
- **M3-09**（纠错入口零服务端）：DP §2.3 定为纯导航（mailto/issue 外链，无 fetch/XHR），可断入口存在 + 目标外链 + 预填含 id/标题 + 点击过程零请求。**可测。**
- **M3-10**（L0 不编码信源/分级，负向）：DP §2.6 提供**双重可测保证**——(a) 结构：`getSourceTrust`/`SOURCE_TRUST` 唯一 UI 消费方为 `DetailCard.tsx`，`EventPanel`/`markers` 不 import；(b) 行为：未开卡时列表行/标记文本不含 trust 表任一取值，开卡后方可见（对照 M3-08 验「仅 L1」边界）。负向结构保证使 M3-10 可测且可复核。**可测。**

**场景可测性结论：通过——三场景判据均可由 DP 的组件结构 + L0 负向结构保证落地验证。**

---

## §4 缺口清单定性核定（GAP-1 / GAP-2）

- **GAP-1（搜索入口 co-resolve）定性成立。** 依据：SPEC-2.5「入口为事件流面板顶部搜索框」在 SPEC-2.2 v0.3.6 重铸后有 (a) 名称漂移（→「今日刻痕」抽屉）+ (b) 首屏可达性张力（默认收起抽屉顶部→首屏不可见）——入口呈现位置/首屏可达性**是对外可见行为**，DP 判其「不能只在 DP 定」正确、不越权定死。与 REV-022 §6.1（本文 line 153）登记的应用期钩子**逐点一致**。候选 A/B/C 并陈、明标「不默选」。**定性核定：成立（spec 级缺口）。**
- **GAP-2（gazetteer 数据源 + M3 搜索范围）定性成立。** 独立复核资产自查：`public/assets/` 仅 `textures/`（earth_day/night.jpg）+ ASSETS.md，`src/data/` 无 geo/gazetteer 目录，全库 grep 无 gazetteer/GeoNames 数据文件——**DP「资产不存在」自查属实**。M3 搜索是否含地名搜索 = 对外可见能力边界，须 spec 定，与 REV-018 F-3（本文 line 172）「R4 应用期 §7 co-resolve、二选一避免 M3 悬空子句」**一致**。候选 (i) 收窄为缓存事件搜索 /(ii) M3 建最小 gazetteer 并陈、不默选；(ii) 依 D29 开源优先给出 GeoNames CC BY 4.0 出处，合规。**定性核定：成立（spec 级缺口）。**
  - 说明：DP 候选 (ii)（M3 提前建最小 gazetteer）超出 REV-018 F-3 原「二选一」的字面（F-3 假定 T2 已后移 M4），但属对 §7 co-resolve 选项空间的**合法补全**（co-resolve 的本题正是「M3 是否具地名搜索能力」），且明标不默选、交 §7 裁。不构成越权。
- **切片③阻塞边界划准。** DP §6 将整条切片③（含 `searchCachedEvents` 纯函数、`SearchBox` 落点、命中分发、空态、gazetteer 半部）gate on GAP-1+GAP-2，两缺口裁定前不开卡——与任务卡一致。§2.5 对缓存匹配标「无门禁」仅指**匹配规则**由 SPEC-2.5 已定、无需 §7 裁决，**非**指该半部可先于 §7 派单落地（切片作为整体仍受 GAP-1 入口 gate）；此区分正确、无默选风险（另见 O-3 提请 orch 留意口径）。
- **切片①②无缺口依赖复核：成立。** 切片①依 SPEC-2.3（已定）+ SPEC-5.10（`trust.ts` 已实现，M3-07 ✅），纠错 URL 为配置常量（SPEC-2.3 渠道属自由度、非 spec 缺口）；切片②依 SPEC-7.4（800ms pin）+ SPEC-7.1/7.2（钳位）。二者均不触搜索入口/gazetteer/地名，**确无 GAP-1/GAP-2 依赖**。门禁过即可派。

**缺口定性结论：GAP-1/GAP-2 定性均成立，候选并陈不默选，切片③阻塞边界与切片①②无依赖判断均准。**

---

## §5 兼容面对接核验

- **与光柱线 `M3-marker-pillar.md §3.1` pick 兼容面：一致。** 该 DP §3.3「命中判定复用 `MarkerLayer.pick(raycaster)`——与 marker-pillar §3.1 保持的 pick 契约同一面，不新增拾取通道」；实测 markers.ts `pick(raycaster): string \| null` 签名与 marker-pillar §3.1 表逐字一致，本卡仅复用、不新增拾取通道。BUG-021 兼容面（§4.3 引 marker-pillar §4.4、§4.5 引 §3.1）交叉引用准确。光柱重写「在途」，DP §7 遗留风险已声明「若光柱重写改 pick 行为则本卡飞行/点击拾取随之复跑（M2-14 家族）」——对接声明**自洽、不冲突**。
- **与 SPEC-2.2 v0.3.6 抽屉语境层级关系：自洽。** 详情卡 L1 浮层在「今日刻痕」抽屉（SPEC-2.2 展开态悬浮于 canvas 之上）之上，符合 D23 分层（L0 球面/从属列表 → L1 点击聚焦卡）。§3.5 列表点击 path（自抽屉展开态列表条目 → DetailCard）与既有 `onSelectRow` 对接、hover 联动（M2-14）原样保留、只新增点击→飞行+卡路径。层级关系连贯。

**兼容面结论：通过。**

---

## §6 极简纪律 + 切片可验证性

- **极简（CLAUDE.md §1）：符合。** DP §1「不做什么」明确回绝全文检索/跨源聚类/AI 摘要/T4-RSS 抽象/新增 GeoEvent 字段/severity 文字档位/第二份分类标签源——外科手术式、无过度设计。FLYING 态（§3.2）为 800ms 缓动所必需、`searchCachedEvents` 纯函数为最小可测面，均非冗余抽象。`getSourceTrust` barrel 导出、相对时间共享 util 均给等价自由度、不过度规定。
- **切片可验证性：符合。** 切片①（卡+trust+L0 负向，不含飞行）闭环于 M3-08/09/10；切片②（飞行）闭环于新登飞行场景 + M2-14 复跑确认；切片③（搜索，gated）闭环于新登搜索场景。三片各为一段可独立验证的活、建议逐片 `/closeout`，符合 §1.5 小步快跑。

---

## §7 遗留观察项（非阻断，供 orch 知悉，不阻断切片①②）

- **O-1 · 分类徽章文字标签的 spec 锚点（认同 DP §7 观察项）**：详情卡徽章最小实现只用分类色（SPEC-3.7，已锚）即完全达标、无泄漏；若 dev 附文字标签，复用既有 `App.tsx CATEGORY_LABELS`（M2 六开关自 M2 即用户可见），该中文标签串**是否需独立 spec 锚点属 M2 遗留的先在条件、非 FM-14 引入的新行为**——独立核验 SPEC-2.4① 仅定「六 category 开关」未 pin 标签串，确为 M2 遗留。本卡不处置、不阻断；建议 orch 后续单独评估六开关分类标签串的 spec 锚点（与 FM-14 正交）。
- **O-2 · 「地点」= 坐标读法（认同 DP §7 观察项）**：M3 GeoEvent 无人读地名字段，详情卡地点渲染格式化 `lat`/`lon`（SPEC-6.1，已锚）——rev 核定此为无字段可依时的合法直读、**非缺口**；人读地名解析属 T2/T3（FM-13，M4）。rev 不认为 M3「地点」须人读地名（须 T2 gazetteer，已后移 M4），故不并入 GAP-2。
- **O-3 · 切片③派单口径提请**：§2.5 缓存匹配标「无门禁」仅指匹配规则已由 SPEC-2.5 定、无需 §7 裁；**整条切片③（含缓存事件半部）须待 GAP-1+GAP-2 §7 co-resolve 后作为整体派单**，不得据「无门禁」字样先行派缓存搜索半部。建议 orch 同批 co-resolve GAP-1+GAP-2（承 REV-022 §6.1）。
- **O-4 · 列表条目路径的抽屉展开前置（qa 面提示）**：触发源②（事件流列表条目点击）需先展开「今日刻痕」抽屉（SPEC-2.2 默认收起），qa 验 M3-08/M3-10 列表路径时须先展开抽屉使条目可见（比照 M2-14 机位 re-anchor 口径，判据不变）。非 DP 缺陷，交 qa 登记场景时知悉。

---

## §8 结论

**通过。** `M3-detail-search.md` 无行为泄漏（全部对外可见取值落 spec 锚点或经 §7 缺口挂起、不默选）、接口自洽（对照 src 实测）、M3-08/09/10 可测、GAP-1/GAP-2 定性成立且候选并陈不默选、切片③阻塞边界与切片①②无依赖判断均准、兼容面对接一致、极简与切片可验证性符合。REV-015 R-1/R-2 强制口径逐字守住、SPEC-7.4 800ms pin 无漂移。

- **可放行**：orch 据此派 dev **切片①（详情卡）+ 切片②（相机飞行）**。
- **维持门禁**：**切片③（搜索）**在 §7 GAP-1（入口 co-resolve）+ GAP-2（gazetteer/范围 co-resolve）裁定前不开卡；建议同批 §7 处置。
- **无须新登 BUG**：O-1~O-4 为非阻断观察项/派单口径提示，随应用/派单携入，不动摇放行。

— rev（REV-023），2026-07-22
