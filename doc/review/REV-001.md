# REV-001 — M0 基建审查记录

- 日期：2026-07-19　审查对象：M0 全量交付（相对 Initial commit 的全部新文件）
- 方式：/code-review medium——8 个独立查找角度（逐行/删除行为/跨文件契约/复用/极简/效率/深度/规范）各自独立实例执行，orch 汇总去重后逐条对照源码行核实。
- 流程说明：本次为 M0 首轮（角色体系本身是本里程碑交付物），签核汇总由 orch 完成；自 M1 起按 CLAUDE.md §0 派独立 rev 实例签核。

## 确认并已整改（8 项，全部在收口回归前修复）

| # | 位置 | 问题 | 整改 |
| --- | --- | --- | --- |
| 1 | scripts/regress.mjs | spawnSync 默认 maxBuffer 1MiB：输出多的绿色套件被 ENOBUFS 杀掉误判 FAIL；p.error 不入 log | maxBuffer 64MiB + 记录 spawn error |
| 2 | scripts/evidence.py | sanitize() Python \w 含 CJK、JS \w 不含，中文测试名两侧算出不同 log 名 | 显式 ASCII 字符类 + 两侧契约注释 |
| 3 | scripts/evidence.py | --bug 关单不校验修复commit列 → CLOSED 行立即被 docs-check 拦截（死锁） | 关单前置校验 + 报错指引 |
| 4 | playwright/vite config | 5173 未 strictPort、reuseExistingServer 恒 true → 证据可能针对漂移端口/陌生 server | vite strictPort + reuse 仅限非 CI |
| 5 | scripts/docs.py | spec_sha 对原始字节哈希，CRLF 落盘会在 normalize 后误报「悄改」 | 行尾归一化后哈希 |
| 6 | src/globe/GlobeScene.ts | SphereGeometry 分段 48 违反 SPEC-3.1 ≥64 | 改 64 并注 SPEC 锚点 |
| 7 | evidence.py / playwright.config.ts | 残留已删除的 regress.py 旧名（报错文案+注释） | 改 regress.mjs |
| 8 | src/globe/GlobeScene.ts | dispose 未 forceContextLoss，StrictMode/HMR 累积 WebGL context | dispose 补 forceContextLoss |

另修：backfill_table 列数守卫与 '|' 值拒写（防表结构损坏）、evidence.py 用法示例与实际 log 路径配对说明、eslint 死配置 'ios' 移除、@types/node 补齐。

## 审查通过但明确接受不改（理由）

- README 与 CLAUDE.md 路线图双写：受众不同（导读 vs 纪律），接受漂移风险，改动时同步。
- Makefile `$(if)` 传参链与 evidence.py argparse 双写：make 透传的固有形态。
- tsconfig noUnusedLocals 与 eslint 规则重叠：双保险保留。
- GlobeScene 构造中 observe() 后的同步 resize()：承载首帧尺寸，非冗余（efficiency 角度核实）。
- docs.py/evidence.py 各自读 version.json、geo.ts 自带 DEG 常量：极简优先下不抽象。
- testplan/bugs 列号契约仍为魔数双写：已加双侧注释与列数守卫；表头名寻址方案留待真实痛点触发（iverif「无痛不建设」原则）。

## 结论

**通过**。整改后 `make regress` 100% PASS（lint+unit+e2e），证据全部重新机械登记。
