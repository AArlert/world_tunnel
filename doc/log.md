# log — 交接日志（新的在上；仓库内最多 4 块，超限 `make docs-archive`）

## [0.3.7] 2026-07-22 光柱切片①落地（形态替换+呼吸通道，D27/动效批 dev 首片）+ FM-14/FM-28 双 DP 出件
- **做了什么**：**光柱切片①完整微循环**：dev 依 REV-021 放行的 DP 重写标记层——新增 `src/globe/shaders/markers.ts`（轴锁 billboard 光柱 shader：instanceMatrix 解根位置/柱高、锥收束 r1=0.35r0、径向软边、尖端软消散、GPU 纯函数朝向）、`markers.ts` dots+rings 双层→单层 pillar InstancedMesh（删脉冲全家、`SEVERITY_BASE_SIZE`→`SEVERITY_PILLAR_HEIGHT`=SPEC-3.7a 柱高、pick 改手动包围球求交、稳态零 GPU 写入）、`GlobeScene.ts` reduced-motion 接线（matchMedia 透传 SPEC-3.11a）。DP 三不变式（children[0]/instanceAlpha/**instanceMatrix 平移=根位置**）全保。qa×2 独立实例重推：单测卡（markers.test.ts 常量/子节点 2→1，M2-11/M2-25 证据刷新）+ e2e 卡（M2-21 断言 2→1+注释净化、M2-14 复跑验 pick 重写无回归），全量 26 files/196 tests + 两 e2e 全绿。**并行 DP 线**：arch×2 出 FM-14 详情卡 DP（`M3-detail-search.md`，REV-023 门禁**通过**——切片①②可派、切片③搜索阻塞于 GAP-1 入口/GAP-2 gazetteer 两 spec 缺口）+ FM-28 守望 DP（`M3-watch-loop.md`，REV-024 门禁在途，含默认过滤模式/关键词大小写两裁项）。批⑤提案（R4+GAP co-resolve）在途。
- **证据**：`doc/evidence/v0.3.6/M2-11.log`/`M2-25.log`/`M2-21.log`(+png)/`M2-14.log`（均 evidence.py 机械回填，✅ 判据文字全程未动）；`doc/review/REV-021-marker-pillar-dp-gate.md` 附录（整改重审放行）、`REV-023-detail-search-dp-gate.md`（通过）。
- **问题**：① M2-10/M3-03 仍 🔲：M2-10 判据文字还是旧脉冲语义、须 qa 从新 SPEC-3.7/3.7a 重写+重测（可在切片①后做）；M3-03 依赖切片②辉光。`e2e/globeDebug.ts sampleMarkerRings` 与 `marker-category-severity.spec.ts` 旧拓扑引用留 M2-10/M3-03 卡一并处理；`marker-initial-snap.test.ts` 旧「dots」注释顺带净化（qa 建议折入）。② e2e 动画类未显式钉 `reducedMotion:'no-preference'`（DP §5.2 预置风险，未发作）——折入 reduced-motion 新场景登记卡。③ FM-14 切片③（搜索）待批⑤仲裁解阻；REV-023 O-1 六开关分类标签串锚点属 M2 遗留先在条件，orch 择期评估。④ BUG-031 维持 OPEN：切片②③落地+M2-10/M3-03 重测后机械关单。
- **下一步**：收 REV-024（守望 DP 门禁）与批⑤提案→仲裁→应用。并行铺 dev：光柱切片②（辉光/新鲜度/足印）+ FM-14 切片①（详情卡+trust 消费，文件域不交叉）。其后 qa：M2-10 判据重写+重测、M3-08/09/10 场景实施、R3/R5 dev 小改（抽屉默认收起+安好态骨架）+ 机位刷新（M2-14/M3-01/M3-05 旧证据在 R3 落地后会失效，REV-022 §4.3）。

## [0.3.6] 2026-07-22 叙事净化批·默认态与安好态 spec 化（D31 per-R ④R3+R5，REV-022 整改后通过）
- **做了什么**：世界表默认态与护城河文案落 spec。链路 arch 提案（proposal-r3-r5-default-state）→ rev 仲裁 REV-022（两 spec diff 语义裁准照原文、三歧义全采读法 A、**涟漪层打回并给修正版**）→ orch 携修正应用 + pin。**SPEC-2.2 重铸**：表盘默认态（冷启动静息态）= 球 + 光柱（SPEC-3.7）+ UTC 钟（SPEC-2.1）；事件流面板退位为**默认收起的「今日刻痕」抽屉**（展开/收起双向、展开态沿用 ~300px、亮度 cap 平移展开态、视觉细节交 aes）；顶栏/六开关不动（歧义 1-A：六开关=透明收据）。**SPEC-2.2a**：删「暂无事件」——空态依守望对象有无二分：有守望→安好态「你守望的世界，此刻安好」（分类全关亦套用，歧义 3-A）、无守望→中性空态（文案本体 aes 制定项）；搜索空态归 SPEC-2.5。FM：新增 **FM-29〔静谧默认态与安好态〕**（M3）、FM-07 加交叉注、FM-10 不动。**✅ 撤销（REV-022 §4）**：M2-13/M2-19/M2-22 三列回退 🔲（rev 纠出提案漏判——M2-19 空态断言 ×2 属判据变更、M2-22 整场景遗漏）。qa 涟漪同批：三场景判据文字重写 + M2-14/M3-01/M3-05/M3-12 机位注记（✅ 不动）+ **新登记 M3-14（表盘默认态）/M3-15（今日刻痕抽屉）/M3-16（安好态二分）**回填 FM-29。
- **证据**：`doc/design-prompt/proposal-r3-r5-default-state.md`、`doc/review/REV-022-r3-r5-default-state-arbitration.md`（§4.1 修正版涟漪清单 + §5 应用清单）、`doc/spec.sha256`（重钉）、`make docs-check` OK。本批无 src/tests 改动。
- **问题**：① **M2-14/M3-01/M3-05 的 ✅ 存证滞后风险**（REV-022 §4.3）：三场景判据不变故未撤 ✅，但其旧证据复跑命令在 R3 抽屉默认收起落地后会 FAIL（机位变化）——dev 落 R3 后 qa 须以新机位重跑刷新证据。② 空态/安好态精确文案断言 gate on aes（当前判据口径=「语义+非旧文案」），aes 批次定稿后补。③ 「零 chrome」字面与六开关保留的 gap 已 surface 给用户（歧义 1-A 附翻案通道:用户若要彻底零控件,另起提案重裁 SPEC-2.4①/FM-10)。④ 光柱 dev 切片①在途(形态替换,REV-021 附录放行后开工)。
- **下一步**：收 dev 光柱切片① → qa 重推卡(markers 单测子节点数/常量名 + M2-11/M2-25 复验 + M2-21 helper 兼容验证)。R3/R5 dev 小改卡(REV-022 §5.8 认可无 DP:翻默认 collapsed + 重命名 + 空态二分骨架;安好态守望分支依 FM-28 store,先落无守望中性分支)。FM-28 守望 DP(arch)与 FM-14 详情卡 DP(R3 已落定,解锁)可开。续批 ⑤R4(GDELT/T2/T3 挂 M4 + SPEC-2.5 gazetteer co-resolve)。

## [0.3.5] 2026-07-22 M3-07 信源信任映射闭环（D22 信任层第一收口）
- **做了什么**：M3 信任层首个场景完整微循环收口。dev（sonnet）实现 `src/data/trust.ts`——SPEC-5.10 表驱动纯函数：`SOURCE_TRUST: Record<SourceId, SourceTrustInfo>` 六源全表（usgs/eonet/gdacs/ll2/opensky→权威事件源，gdelt→新闻报道（待验证））+ `getSourceTrust()` 查表；不入 GeoEvent 模型、不新增字段（SPEC-5.10 信源级常量子句），风格比照 markers.ts `CATEGORY_COLORS` 先例。qa（独立实例）从 SPEC-5.10/6.1 独立誊写期望值写 `tests/trust.test.ts` 17 断言（六源逐行核对/key 集合完整性/两级枚举封闭/幂等一致/GeoEvent 无 tier 字段），首跑即绿，`make evidence SCEN=M3-07 TEST=trust` 机械置 ✅。
- **证据**：`doc/evidence/v0.3.4/M3-07.log`（复跑 `make test TEST=trust`）；全量 `make test` 26 files/196 tests PASS 无回归；docs-check OK。
- **问题**：① qa 报覆盖缺口（可疑未定性）：「GeoEvent 不含 tier 键」反向断言仅在 trust.test.ts 范围验证,provider 归一化路径无同款负向守卫(M2-01 geoevent-model.test.ts 亦无)——留待触及 provider 改动时一并补,不单开卡。② gdelt 条目仅映射表常量,provider 本体未实现(FM-12,R4 批⑤裁 GDELT 挂 M4 后再定)。③ M3-08/09/10(详情卡消费/纠错入口/L0 负向)仍 🔲,依赖 FM-14 详情卡——其 DP 受 R3 面板退位影响,排 R3+R5 落定后。
- **下一步**：收两条在途线:R3+R5 默认态提案(arch)→rev 仲裁;光柱 DP 重审(rev)→放行则 dev 切片①(形态替换+呼吸通道,连带 BUG-031 收口路径)。其后:FM-28 守望 DP(arch)、FM-14 详情卡 DP(R3 落定后)、⑤R4 GDELT 挂 M4 提案。
