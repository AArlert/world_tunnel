# design-prompt — <模块名>

> arch 撰写，rev 门禁通过后 orch 据此派 dev。只准约束实现，不准定义 spec 之外的对外可见行为（行为泄漏禁区，见 .claude/agents/arch.md）。

## 1. 目标与范围
一句话说清模块做什么、不做什么。

## 2. 约束（每条标注 SPEC 锚点）
- <约束>（SPEC-x.y）

## 3. 接口
导出的函数/类/类型签名；与相邻模块的数据流向。对外可见行为只引用 spec，不在此定义。

## 4. 实现提示（可选，不构成强约束）
性能预算、推荐库/模式、已知陷阱（引用 BUG-ID）。

## 5. 验收判据
dev 自检标准（lint/相关单测）；建议 QA 覆盖的检查点清单（只列检查点，不写断言推导）。
