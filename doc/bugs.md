# bugs — 缺陷台账

规则见 CLAUDE.md §4.3：登记（最小复现+现象+期望+spec 依据）→ 修复回填 → 复现命令复跑 → `make evidence BUG=<ID>` 机械关单。
状态：OPEN / FIX_READY / CLOSED / WONTFIX。复杂调试开 `doc/bugs/<BUG-ID>.md` 详情页。

**台账书写约定（BUG-005 未修期间的硬要求）**：本表任何单元格内**禁止出现字面竖线字符**，无论是否用反斜杠转义、是否包在反引号里——回填脚本的分列逻辑会把它当列分隔符，静默写坏整行。需要表达竖线时一律用文字描述（如「竖线字符」）。详情见 BUG-005。

| 编号 | 状态 | 疑似归属 | 摘要 | 复现 | 修复commit | 复验证据 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | CLOSED | 工具环境 | PyManager(Store) Python 跑在 MSIX 容器内，AppData 被虚拟化，python 子进程看不到 ms-playwright 浏览器，经 make→python 的 e2e 必挂（直跑 npx 则绿）。根因证据：python os.listdir(AppData/Local/ms-playwright) FileNotFound。修复：regress 层由 regress.py 改为 regress.mjs（node 无容器） | `make e2e` | v0.0.1（M0 基建提交） | doc/evidence/v0.0.1/BUG-001.log |
| BUG-002 | CLOSED | scripts/evidence.py | backfill_table() 按字面竖线字符裸切列定位列号，未区分「列分隔符」与「单元格内容里的字面竖线」，导致列错位：对 M1-02 场景行回填时，状态勾/证据路径/复跑命令被写进描述中段与原状态列，描述被截断，行膨胀出多余列。列数下限校验 len(cells) 小于等于 max(update_cells) 不会对「列数偏多」报错，故静默通过。无 SPEC 依据（工具链契约缺陷）。期望：含字面竖线的单元格要么正确解析、要么显式报错拒收，不得静默错位覆盖。**首轮修复（转义感知分列 split_row，只豁免反斜杠转义）已交付但复验 FAIL**：真实数据里还存在反引号代码片段内的未转义竖线，同构故障复现，另立 BUG-005。本条待 BUG-004、BUG-005 修复后重新复验。 | 对 testplan 中描述列含字面竖线字符的场景行执行 evidence 回填 | 首轮修复见 scripts/evidence.py split_row（未通过复验） | doc/evidence/v0.1.0/BUG-002.log |
| BUG-003 | CLOSED | scripts/docs.py | 与 BUG-002 同源、位于读取端：parse_table() 仍按字面竖线裸切列，未做转义感知，与 evidence.py 的写入端列号契约不一致。只读路径不会覆盖数据，但表格出现含字面竖线的行时，docs-check 的列数校验与状态位读取可能误判（幽灵引用误报、状态统计失真）。无 SPEC 依据。期望：读写两端共用同一分列逻辑，抽为共用函数。发现于 BUG-002 修复交付的 dev 遗留风险汇报，orch 登记。**根因确认**：scripts/docs.py 的 parse_table() 直接对整行做 strip 竖线字符后再按竖线字符切分，未做转义或行内代码块感知，读取端与 evidence.py 写入端 split_row 的分列口径不一致。**修复**：抽取共用函数到新文件 scripts/mdtable.py 的 split_row()，scripts/docs.py 的 parse_table() 改为调用该函数，与 evidence.py 共用同一分列实现（详见 BUG-005 修复）。**验证**：写等价对比脚本，对 doc/testplan.md、doc/bugs.md、doc/feature-matrix.md 现有数据，新旧分列结果逐行一致；另在 scratchpad 构造含反引号代码片段与转义竖线字符混用的测试行，确认 parse_table 列数与状态列、证据列定位均正确、不再错位。 | 构造描述列含字面竖线字符的 testplan 行后跑 make docs-check，观察列数校验与状态统计 | scripts/docs.py（parse_table 改用 scripts/mdtable.py 的共用 split_row）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.1.0/BUG-003.log |
| BUG-004 | CLOSED | Makefile | evidence 目标用 LINT 作参数名判断是否追加 --lint，而 GNU Make 3.81 存在同名内置默认变量 LINT（内置值恒为字面量 lint，供隐式 C lint 规则用），故不显式传 LINT= 空值覆盖时该变量恒非空，条件恒真——**任何 make evidence 调用都被静默追加 --lint**。evidence.py 中 --lint 分支优先于 unit/e2e 分支，会把复跑命令钉成 make lint、日志源钉成 test-results/lint.log，使证据与实际所跑测试不符。这是证据链的系统性失真风险（CLAUDE.md §5.2 防验证造假的直接威胁）。已审计现有全部 9 个证据文件，source log 均与场景匹配、未受污染（历史调用或直接跑 python 脚本、或显式传 LINT= 规避）。无 SPEC 依据。期望：改用不与 Make 内置变量同名的参数名（如 DO_LINT），或在 Makefile 顶部 override LINT 置空后再按调用方传参判断。**根因确认**：用 make -n 展开验证，Makefile 里 LINT 变量在未显式赋值时恒等于 GNU Make 3.81 内置默认值 lint（供隐式 C lint 规则用），故 evidence 目标里判断该变量是否非空的条件恒真，任何调用都会被追加 --lint 分支。**修复**：evidence 目标改用 DO_LINT 作为参数名（不与 Make 内置变量重名），判断逻辑随之改为对 DO_LINT 取值。**验证**：make -n evidence 不传 DO_LINT 时展开结果不再含 --lint；显式传 DO_LINT=1 时仍正确展开为含 --lint。**遗留**：CLAUDE.md 文档表中原写法 LINT=1 需同步更新为 DO_LINT=1，超出本次 dev 改动授权范围（该文件不在本次任务相关文件列表内），已在交付汇报中登记，待 orch 处理。 | `make -n evidence SCEN=M0-01 TEST=geo` 展开含 --lint；追加 LINT= 空值覆盖后展开不含 --lint | Makefile（evidence 目标参数名 LINT 改为 DO_LINT）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.1.0/BUG-004.log |
| BUG-005 | CLOSED | scripts/evidence.py | BUG-002 首轮修复引入的 split_row() 只豁免反斜杠转义的竖线，未豁免 markdown 行内代码（成对反引号）里同样表示字面内容的未转义竖线，仍会计入分列，产生与 BUG-002 完全同构的「列数偏多、回填值写入错位列、描述被截断」静默覆盖。真实复现：bugs.md 里 BUG-002 行自身摘要含反引号代码片段，QA 复验执行 make evidence BUG=BUG-002 时把该行写坏两次（QA 已逐字复原）。期望：split_row 整体跳过成对反引号内容再分列，或在文档规范中明确「单元格内任何字面竖线一律禁止」并配报错分支拒收，二选一，不得再静默错位覆盖。orch 已在本表头部加书写约定并清除全表字面竖线，作为修复前的止血措施。**根因确认**：split_row() 原实现只用正则表达式排除反斜杠转义位置的竖线字符，未识别成对反引号包裹的行内代码片段——代码片段内部即便是未转义的竖线字符也应视为内容而非分隔符，原逻辑仍会在该处断列，复现与 BUG-002 完全同构的列错位。**修复**：抽取共用函数到新文件 scripts/mdtable.py，split_row() 改为逐字符扫描，遇反引号切换“行内代码中”状态，仅在非该状态且字符未被反斜杠转义时才断列；evidence.py 删除本地实现改为从 mdtable 导入（与 docs.py 共用，一并解决 BUG-003 的契约不一致）。**验证**：在 scratchpad 临时表格副本构造一行，摘要列同时含反引号包裹的代码片段（内部有未转义竖线字符）与反斜杠转义竖线字符两种情况，跑分列后列数为预期的 7 列且各列内容对齐；再用 backfill_table 对该行回填状态与复验证据列，确认描述列完整未截断、其余列未被覆盖错位。**QA 复验 FAIL（新边界，未覆盖）**：在 scratchpad 直接调用 mdtable.split_row 复核，构造摘要列含单个未闭合反引号（反引号数量为奇数）的行——split_row 逐字符扫描的 in_code 状态在行尾未复位，该反引号之后本应作为列分隔符的全部竖线字符都被误判为代码内容，行内后续解析被整体合并偏移：一个应有 7 列的行被解析成 4 列，第 4 列吞并了原本第 5、6、7 列的全部内容。写入端 backfill_table 因该表列数恰好落在 max(update_cells) 阈值上而拦截报错退出（本次未发生静默覆盖，属该阈值算法针对当前表结构的巧合，并非专门针对此边界设计的防御）；但读取端 docs.py 的 parse_table 靠 min_cols 判断，会把这类畸形行直接静默跳过、不报错，docs-check 因此可能对含此类畸形行的证据/状态一致性检查失效而不自知，与期望「正确解析或显式报错拒收，二选一」不符。判定：修复不完整，未覆盖「单元格内反引号未闭合（奇数个）」边界，退回 OPEN，待重新修复 split_row（如遇行尾仍处于 in_code 状态时报错拒收，或改用更严格的代码片段配对识别）后再复验。**二次修复确认**：split_row() 整行扫描结束后新增校验——若 in_code 仍为真（反引号未闭合，奇数个），显式抛出新增的 MalformedRowError，不再返回任何列结果。调用方（docs.py 的 parse_table、evidence.py 的 backfill_table 与 main 内关单前置扫描、新增的 scenario_needs_shot，见 BUG-008）均补充：先判断该行是否以竖线开头再调用 split_row（此前 evidence.py 曾对文件全部行不加区分地调用，理论上会误伤含未闭合反引号的非表格文字行），并捕获该异常后以 sys.exit 显式报错拒收，报错信息含文件名、行号与原始行内容，不再有任何静默路径。与 BUG-009（parse_table 对列数不足的畸形行静默跳过）合并处理，详见该条。 | 在 scratchpad 临时表格副本构造一行，描述列含反引号包裹且内部有未转义竖线的代码片段，对该行跑 evidence.py 的 backfill_table，观察列数与回填落位；另需补充验证：构造摘要列含单个未闭合（奇数个）反引号的行，观察 split_row 列数与 in_code 状态是否跨单元格污染 | scripts/mdtable.py：split_row() 追加行尾状态校验，反引号未闭合时显式抛出新增的 MalformedRowError（不再返回结果）。scripts/docs.py：parse_table() 捕获该异常并 sys.exit 拒收（与 BUG-009 合并修复，一并把列数不足 min_cols 的畸形行由静默 continue 改为 sys.exit 拒收）。scripts/evidence.py：backfill_table() 与 main 内关单前置扫描改为先判断竖线开头再分列并捕获该异常拒收；新增 scenario_needs_shot()（服务于 BUG-008）同样捕获拒收。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.1.0/BUG-005.log |
| BUG-007 | CLOSED | 工作流调度（orch） | 并发派单写同一共享台账导致数据损坏与编号冲突：两个 qa 实例同时被派发、均需写 doc/bugs.md，结果各自独立登记了编号同为 BUG-005 的条目（其一自行改名 BUG-006 避让），且其中一次 evidence 回填在另一实例两次读取之间损坏了 BUG-002 行。根因是 orch 调度未对共享 doc 台账文件做写串行化，误以为「回填目标文件不同」即可并行（实际 BUG 模式与 SCEN 模式都可能触及同一文件，且两卡都要登记新缺陷）。期望：涉及写 doc/bugs.md 或 doc/testplan.md 的任务卡一律串行派发；orch 派单前显式核对目标文件是否重叠。合并处理：原 BUG-006 与 BUG-005 同根因，内容已并入 BUG-005，编号作废不再使用。**修复**：规则落地到 .claude/skills/dispatch/SKILL.md 的「派单前自查」清单，新增三条——① 写共享台账（bugs.md/testplan.md）的卡一律串行，判断依据是「这张卡是否可能写该文件」而非「回填目标是否不同」，只读该文件的卡不受限；② 改 scripts/ 的卡与用 scripts/ 的卡不并行（避免 qa 跑 evidence 时拿到中间状态）；③ 单卡范围上限约 2 个场景或 2 条缺陷，超出即拆（5 场景卡曾整卡空转 55 分钟无产出，拆为 2 场景后 3.6 分钟完成）。流程类缺陷无自动化复现手段，复验方式为核对规则文本已落地且后续派单实际遵守。**QA 复验（关单人非修复人）**：逐条比对 .claude/skills/dispatch/SKILL.md「派单前自查」清单第 3 节，三条规则均已按本条登记的期望原文落地（①串行判断依据改为「是否可能写该文件」而非「回填目标是否不同」；②改 scripts 的卡与用 scripts 的卡不并行；③单卡上限约 2 场景或 2 缺陷），文字比对未见出入，此项判定通过。但操作性评估发现一处漏洞：SKILL.md 落地文本在规则①末尾新增了登记原文没有的举例「只读该文件的卡（如 rev）不受此限」，该举例与「按卡实际写入面判断、而非按角色/回填目标判断」的原则本身矛盾——据 .claude/agents/rev.md 明确列出 rev 的 Edit 与 Write 权限含「bugs.md 的裁决与状态列」，即 rev 在做「仲裁」这一类任务（裁决 spec 歧义类缺陷）时确实要写 doc/bugs.md，并非只读；只有门禁、代码审查、里程碑签核三类任务才是只读。若未来 orch 依这句举例的字面意思，把一张 rev 仲裁卡当作「只读、不受限」而与另一张写 bugs.md 的卡（如 qa 登记新缺陷的卡）并行派发，会复现本条报告的同一故障模式（并发写同一台账导致编号冲突或内容损坏），只是触发角色从两张 qa/dev 卡换成了 rev 仲裁卡加另一张写卡，规则③新增的这句举例并未真正堵住同类问题。判定：核心三条规则文本落地属实（对应本条登记内容一致），但该处新增举例存在按角色刻板印象而非按卡实际写入面判断的漏洞，修复不完整，退回 OPEN。建议下次修复二选一：a) 把该举例改为按 rev 四类任务区分——门禁/审查/签核只读不受限，仲裁需写 bugs.md 裁决列应纳入串行范围；b) 直接删除「如 rev」这一举例，只保留「这张卡是否可能写该文件」本身作为唯一判断依据，不再给任何角色开无条件豁免。回归结果（与本条判定无直接因果，一并留痕）：`make test` 全量 PASS（test-results/unit_all.log）；`make lint` PASS（test-results/lint.log）；`python scripts/docs.py --check` 仍是 status.jsonl 与 log.md 首块 TODO 两条已知 FAIL（orch closeout 待办，非本次新增）。**二次复验（关单人非修复人、与首轮复验不同实例）**：核对对象为 .claude/skills/dispatch/SKILL.md「派单前自查」现三条规则文本、doc/bugs.md 本条登记的期望/修复/首轮复验三段、.claude/agents/rev.md 全文、.claude/agents/arch.md 全文。逐条结论——① 现三条规则文本与本条「**修复**」段登记的①②③原文逐字比对一致，通过；② 首轮指出的漏洞（规则①末尾曾有「只读该文件的卡（如 rev）不受此限」这句举例）核实已不再出现于现文本，现文本改为区分 rev 四类任务——门禁/审查/签核只读不受限，**仲裁**需写 bugs.md 裁决与状态列应纳入串行范围，与 rev.md 原文列出的 Edit/Write 权限范围逐条对得上，采用首轮建议方向 a 且保留对错误举例的反面引用（不会被误读为现行豁免条款），判定该具体漏洞已消除，通过；③ 排查 arch 的 spec 提案卡是否也可能写 doc/bugs.md——核对 arch.md 职责段「登记 bugs.md 或附在交付里」确认 arch 确实可能直接写该文件，且 arch.md 未像 rev.md 那样设「禁区」限定 Edit/Write 可写文件范围；现规则①开篇「按卡的实际写入面判断、不按角色」与段末「不给任何角色开无条件豁免」均为无角色限定的通用表述，逐字检查未发现任何字面豁免 arch 或暗示"仅三类角色需纳入判断"的措辞，qa/dev/rev 三例是例证而非穷举式限定，与首轮"肯定式错误断言"的漏洞性质不同（本处是"举例未穷举"，规则本身无错误断言），判定该项已被现规则字面覆盖，通过，但登记为非阻塞的加固建议——后续可补一句将 arch 纳入例证列表以进一步降低误读风险。三项核对均通过。**复验通过，待 orch 依核对记录置 CLOSED**（核对记录见 doc/evidence/v0.1.0/BUG-007-review.md，含核对对象、逐条结论、遗留建议、署名日期；本次关单口径依 CLAUDE.md §5.3 第 4 条——流程/规则类缺陷复验证据为人工核对记录而非测试 log，工具侧 make evidence 尚不支持此类挂载，已登记 BUG-015 待 M2 处理）。回归留痕（与本条判定无直接因果）：`make test` 全量 PASS（test-results/unit_all.log，6 files / 25 tests）；`make lint` PASS（test-results/lint.log）。 | 同时派发两张均需写 doc/bugs.md 的任务卡；本次复验新增复现路径：同时派发一张「rev 仲裁」卡与一张「写 doc/bugs.md」卡；二次复验新增排查点：核对 arch 的 spec 提案卡是否也可能写 doc/bugs.md | .claude/skills/dispatch/SKILL.md（派单前自查新增三条）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.1.0/BUG-007-review.md（人工核对记录，非机械测试 log，按 CLAUDE.md §5.3 第 4 条口径出具） |
| BUG-010 | CLOSED | e2e/idle-spin.spec.ts（测试自身） | 疑似 flaky：全量 e2e 并发（8 worker）高负载下，M1-11 的空闲自转用例曾出现一次精度断言失败（对自转起始时刻的近似比较），此后连续 7 次全量复跑未复现，无稳定复现命令。**观察面扩充（2026-07-21，orch 依 qa M2-08 卡交付汇报登记）**：BUG-019 复验的全量回归中，8-worker 高并发下 day-night-calibration 与 zoom-range 亦出现同模式间歇失败（计时精度/超时类，单独复跑即绿），抖动范围可能不止 idle-spin；后续处置时应一并评估三个用例的负载敏感断言。**观察面再扩充（2026-07-21，qa M2-15/M2-13 重测卡）**：全量 make e2e（本机默认并发）下 marker-breathing-transition（M2-21）出现同族间歇失败——旧标记渐隐透明度多帧采样只捕获到 1 个中间值（实测序列 0.93、0.19、0.00 之后恒 0），期望 ≥2 个中间衰减采样；单独复跑该文件即 PASS，系高负载下逐帧采样墙钟间距变宽、把约 500ms 渐隐在更少采样点内跨过所致，与 idle-spin/day-night-calibration/zoom-range 同属负载敏感断言家族，非本卡改动引入（本卡仅改 vector-earth-style 与 event-panel 两 spec，未触 markers.ts 或 marker-breathing-transition.spec.ts）。发现于 M1-05/M1-07 卡的全量回归过程，由 qa 如实记入交付汇报，orch 转登记入台账（避免只在会话内口头传递，CLAUDE.md §5.3）。**观察面三度扩充（2026-07-21，qa FM-11 M2-23/M2-24 卡）**：全量 make e2e（本机默认并发）下新增两例同族间歇失败——① marker-breathing-transition（M2-21）再次复现（与上条同一断言点）；② marker-severity-tri-channel 出现像素采样错读（实测某通道值 12.0，参考值 179，容差 ±6，偏差量级远超正常噪声，疑似高并发下读到了错误/陈旧帧或被其他 worker 的渲染争抢干扰）。两例单独/低并发复跑（`npx playwright test marker-breathing-transition marker-severity-tri-channel`，2 worker）均 PASS，与既有家族同一特征（单独绿、全量并发下偶发红）。本卡改动仅新增 tests/build-budget.test.ts、e2e/cold-start-perf.spec.ts、tests/data-layer-early-stop-cache-loss.test.ts 三个自包含新文件与 vite.config.ts 的 reporters 配置项，未触及 markers.ts/GlobeScene.ts/globeDebug.ts 或任一既有 e2e spec，判定非本卡引入。风险：偶发红会侵蚀证据链可信度——一旦 CI 上出现无法复现的失败，团队会开始习惯性无视红灯，这比稳定失败更危险。期望：确认该断言的容差是否对机器负载敏感（墙钟采样在高并发下抖动），必要时改用对负载不敏感的采样方式或重新论证容差依据；若判定为测试自身问题由 qa 修，不得通过放宽 spec 判据来消除。无 SPEC 依据（测试稳定性问题，非实现缺陷）。**治理与修复（qa BUG-010 治理卡，2026-07-21，改动仅限 e2e/、不动 src 与 spec 判据）**：逐用例定位负载敏感点并改采样机制（判据强度不降，期望值仍全部从 SPEC 推导）——① marker-breathing-transition（M2-21，SPEC-3.11）改为页面内 requestAnimationFrame 逐帧记录 instanceAlpha 轨迹（注入与记录同一 evaluate 原子完成，采样密度=真实渲染帧率、与外部往返延迟解耦），「≥2 中间衰减采样」断言保持不降；② idle-spin（M1-11，SPEC-7.3/7.5）速率用例改多段短窗取中位数、抗被 dt 上限钳位的卡顿帧拖低，「空闲期不自转」用例加不产生位移的点击把空闲计时基准归零、与 setup 负载解耦；③ marker-severity-tri-channel（M3-03，SPEC-3.7）每枚色采样前重置空闲计时并钉零自转、findColorInRegion 改重试稳定版，消除 9 枚采样累计越 10s 阈值触发自转致像素错读（即第三轮登记的通道值 12 对参考 179）；④ day-night-calibration（M1-05，SPEC-3.2/3.6）前置零自转自检前加空闲重置+钉零自转（卫星 JPEG 加载可超 10s 阈值漂移 earthRotY），两用例超时预算放宽；⑤ zoom-range（M1-10，SPEC-7.2）超时预算 30s→60s（断言均为边界夹紧/收敛、与墙钟无关，仅完成时间需余量）。**本卡压力测试新发现两名同族成员并一并修复**：drag-inertia（M1-09 惯性用例，SPEC-7.1）——累计 ~8.2s 等待加往返在负载下逼近 10s 空闲阈值、空闲自转改写 earthRotY 破坏 10 位精度断言，earthRotY 断言前移到惯性已收敛且距阈值有余量的 t2，相机漂移检查（不受自转影响）保留末端；panel-marker-linkage（M2-14 list→marker，SPEC-7.4）——基线 dot 像素读取抢跑真实 dataLayer 异步上屏链路致读到 0，改为轮询到标记渲染可见为止。**压力验证**：全量 make e2e（8-worker 默认并发）连续 5 轮全绿（另单跑受影响用例集亦绿），make test（179 tests）与 make lint 零回归。 | 无稳定复现；观察方式为高负载下反复跑全量 make e2e，留意 idle-spin 用例；本卡复现/复验为全量 make e2e（8-worker）连续多轮观察 | e2e/globeDebug.ts（新增 findColorInRegionStable 与 injectAndRecordBreathing/waitBreathingTrace 逐帧记录器）、marker-breathing-transition.spec.ts、marker-severity-tri-channel.spec.ts、idle-spin.spec.ts、zoom-range.spec.ts、day-night-calibration.spec.ts、drag-inertia.spec.ts、panel-marker-linkage.spec.ts（均在 e2e/，尚未提交，随下次 closeout 一并入库） | doc/evidence/v0.2.22/BUG-010.log |
| BUG-008 | CLOSED | scripts/evidence.py + scripts/docs.py | 视觉类场景的截图证据无任何机械拦截：evidence.py 置 ✅ 时不检查场景是否声明了截图/视觉判据，--shot 纯属可选参数，docs-check 也不做交叉校验。「视觉类场景必须附截图」目前只是 .claude/agents/qa.md 里的文字约定，靠角色自觉。M1-07 是该缺口首次兑现——行文明写截图判据，实际只交了材质代理单测即置 ✅ 并通过全部守卫，由 REV-003 人工审查才发现。M1-05、M1-08 及新登记的 M1-14 同在敞口内。无 SPEC 依据（工具链契约缺陷）。期望（设计交 arch/dev 定）：场景描述含截图或视觉标记时，未传 --shot 则拒绝置 ✅；或把截图路径写进证据文件头注供 docs-check 交叉校验。发现来源 REV-003 发现 2，orch 登记。**设计选择与修复确认**：采用前者（写入端前置拦截），舍弃「截图路径写进证据头注供 docs-check 交叉校验」——置 ✅ 的入口只有 evidence.py 一处，在此处前置拦截即可堵住漏洞，无需再新增证据文件头注字段与 docs-check 侧的交叉校验规则、也不必变更已归档证据文件的格式约定，改动面明显更小。判定「是否声明截图判据」的规则：testplan.md 场景描述列（第 3 列）子串包含"截图"或"视觉"任一字样即判定为声明了视觉判据，该规则写死在 scripts/evidence.py 的 VISUAL_MARKERS 常量注释中；若未来出现误判（例如描述含"非视觉"一类否定表述），需再细化标记语法而非放宽判断。实测验证：对含截图判据描述、不传 --shot 的场景执行 evidence 回填，被显式拒绝且不产生任何文件写入；传入合法 --shot 后可正常置 ✅ 且截图随证据归档；不含视觉标记的场景不受影响、无需 --shot 仍可正常置 ✅。 | 对一条描述中声明了截图判据的场景行，不传 --shot 执行 evidence 回填，观察是否照常置 ✅ | scripts/evidence.py：新增 VISUAL_MARKERS 常量与 scenario_needs_shot()，main() 在 --scen 分支写入任何文件之前判断该场景是否声明视觉判据且未传 --shot，若是则 sys.exit 拒绝置 ✅。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.1.0/BUG-008.log |
| BUG-009 | CLOSED | scripts/docs.py | parse_table() 对列数少于表结构约定的畸形行静默跳过（min_cols 判断），既不报错也不计入统计——docs-check 会对这类行失去校验能力而不自知，属守卫无声失效。发现于 BUG-005 复验：未闭合反引号导致一行被解析成 4 列（应为 7 列）时，读取端直接跳过该行。与 BUG-005 同源，期望一并处理：畸形行应显式报错拒收，而非静默跳过。**修复确认**：与 BUG-005 合并修复——parse_table() 对 scripts/mdtable.py 新增的 MalformedRowError（split_row 遇反引号未闭合时抛出）显式捕获并 sys.exit 拒收；len(cells) 小于 min_cols 的分支由原先的静默 continue 改为 sys.exit 拒收，报错信息含文件路径、行号、实际列数与表结构约定的列数。两处改动使 docs-check 及其调用方（handover/next）在遇到畸形行时都会显式中止，不再有静默降级路径。 | 构造一行单元格内含奇数个反引号的表格行，跑 make docs-check，观察该行是否被静默忽略 | scripts/docs.py：parse_table() 新增对 MalformedRowError 的捕获与 sys.exit；列数不足分支由 continue 改为 sys.exit。与 BUG-005 同一提交处理，文件改动详见该条修复commit列。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.1.0/BUG-009.log |
| BUG-011 | OPEN | scripts/evidence.py | 证据文件无法覆盖判据跨单测与 e2e 两层的场景：evidence.py 的 scen 模式只接受单一 log 源，一行只能登记一条复跑命令，判据分布在两层的场景只能归档其中一层，另一层的断言虽真实存在却不在证据文件内、复跑列不忠实。已实际兑现两次——M1-07 的材质代理断言（REV-003 判定为达标的判据 c）与 M1-08 的 SPEC-3.5 单测半（点数下限与分布半径下限）均未进各自证据文件。发现来源 REV-004 的 NEW-1，rev 已明确定性：**不构成未挣得的 ✅、不要求回退**（断言真实存在且被全量回归覆盖，无 spec 子句掉出场景之外），仅须登记留痕，M2 修工具后重跑补齐。无 SPEC 依据（工具链契约缺陷）。期望：支持一个场景挂多条 log 与多条复跑命令，或支持追加式补登记。 | 对判据跨单测与 e2e 两层的场景（如 M1-08）执行 evidence 回填，观察证据文件与复跑列是否只覆盖其中一层 | - | - |
| BUG-012 | OPEN | doc/testplan.md 数据 | M1-04 行文标注为单测，实际证据与复跑命令均为 e2e（相机参数需真实 WebGL 上下文，只能走 e2e）。属层级表述不符，非覆盖缺口——判据本身已被 e2e 真实覆盖。发现来源 REV-004 的 NEW-2。期望：修正行文的测试层级表述使其与实际证据一致。注意此修改属「判据表述与实际所测对齐」方向，且不使任何 spec 子句掉出场景之外，与 CLAUDE.md §5.2 禁止的「判据向实现看齐」不同——后者是下调判据强度，本条只是更正测试层级标注。**同类新增（2026-07-21，qa 于 M2-17 卡交付时发现）**：M2-17 行文同样标注为单测，实际证据与复跑命令均为 e2e（网络请求断言须真实浏览器网络层与 DOM+WebGL 上下文触发 GlobeScene 完整 boot 流程，vitest 的 node 环境无 WebGL/fetch 网络层可用，只能走 e2e）。同一根因、同一处理方式，一并登记于本条待 orch 统一修正两行标注。 | 比对 doc/testplan.md 的 M1-04 行文标注与该行证据文件首行的复跑命令；同法比对 M2-17 行文标注与其证据文件（doc/evidence/v0.2.4/M2-17.log）首行的复跑命令 | - | - |
| BUG-013 | OPEN | tests 头注 | 两处测试文件头注已过期失真：tests/earth-night-gain.test.ts 与 tests/atmosphere.test.ts 的头注仍写着「保持 🔲，留待 orch 决定路径」，而对应场景 M1-06 与 M1-07 均已 ✅ 闭合。过期注释会误导后续维护者对场景状态的判断。发现来源 REV-004 的 NEW-3。期望：更新为与当前场景状态一致的表述。 | 读 tests/earth-night-gain.test.ts 与 tests/atmosphere.test.ts 头注，对照 doc/testplan.md 中 M1-06 与 M1-07 的当前状态 | - | - |
| BUG-014 | CLOSED | doc/feature-matrix.md 覆盖面 | SPEC-2.1 的「UTC 时钟」不落在任何 FM 行、无任何 testplan 场景，src/App.tsx 中连占位都没有——正是 REV-002 的 G-1 所警示的「spec 子句无人认领而静默蒸发」模式，只是这次发生在 feature-matrix 层而非 testplan 层。不影响 M1 签核（SPEC-2.x 不在 M1 范围）。发现来源 REV-004 的 NEW-5。期望：orch 定归属——指定承接该子句的 FM 行与目标里程碑，M2 开卡时一并核对 SPEC-2.x 其余子句是否也存在同类失落。 | 用 SPEC-2.1 的子句逐条比对 doc/feature-matrix.md 全部 FM 行的交付物与场景列，检查是否存在无人认领的子句 | 路线图重排（提案 design-prompt/proposal-roadmap-v2.md §3.1，REV-006 门禁⑤核验）：SPEC-2.1 UTC 时钟落 FM-10（M2，格式判据已入 SPEC-2.1 正文）；SPEC-2.x 全子句逐条核对有主，未见其他失落。随 v0.1.3 closeout 入库。复验口径：核对记录型（CLAUDE.md §5.3 第 4 条），待独立 qa 于 M2 开卡时出具 | doc/evidence/v0.2.11/BUG-014-verification.md（独立 qa 核对记录，orch 依 CLAUDE.md §5.3-4 流程类口径置 CLOSED，2026-07-21） |
| BUG-020 | CLOSED | e2e 与 FM-08 交互 | 卫星依赖的 M1 e2e 在 FM-08 摘除卫星默认加载后将回归失败：M1-05（day-night-calibration）与 M1-14（day-side-no-lights）渲染默认 GlobeScene 且首句 waitForRealEarthTexture，矢量默认下该等待永不 resolve，破坏 M2 §5.1 条件② 全量回归。发现来源 REV-010 §3.1。期望（rev 已列二选一）：FM-08 卡内提供 DEV-only 卫星实例化钩子供两 e2e 显式走卫星路径,或 orch 与 qa 将两场景重登记为卫星风格包专属;**FM-08 交付前须闭合,不阻塞 FM-07**。**根因**：矢量默认球的底面材质是矢量 shader（uColorDay/uColorNight/uSunDir/uTwilight/uGlow*），不含卫星专属的 uDayMap uniform；e2e 助手 waitForRealEarthTexture 靠读底面 SphereGeometry 材质的 uDayMap.value.isDataTexture 判断纹理是否就绪，矢量默认下该 uniform 恒不存在，waitForFunction 永不 resolve 至超时。**修复（方案 a）**：卫星资产与 M1 shader 路径全部保留，仅退出默认 boot 加载路径——GlobeScene 构造新增可选入参 satellite（默认 false 走矢量 createVectorEarth，true 走 M1 卫星 createEarth+loadEarthTextures）；App.tsx 仅在 import.meta.env.DEV 下识别 URL query style=satellite 并下传该入参，生产忽略、默认矢量。默认矢量不请求任何大纹理（headless 核验 earth_day/earth_night 零请求、无 console 错误），style=satellite 时两纹理照常加载、uDayMap 变真实纹理、waitForRealEarthTexture 可 resolve。DEV 不改 e2e/，两条 e2e 在 qa 把 goto 指向 style=satellite 前暂红。**观察扩充（实跑 make e2e 另发现）**：atmosphere-glow（SPEC-3.4）同样在 globeDebug.ts:184 经 line 44 调 waitForRealEarthTexture 作稳定门，矢量默认下同因超时——属 BUG-020 同一机制但登记时未列入的第三条 e2e；该测断言大气辉光、与风格无关，qa 适配范围应一并纳入（指向 style=satellite 或改稳定门任一）。全量 e2e 其余 13 用例（globe-init/idle-spin/starfield/drag-inertia/zoom-range/smoke）保持绿。 | 摘除卫星默认加载后跑 make e2e TEST=day-night-calibration 与 day-side-no-lights,观察超时 | src/globe/vectorEarth.ts、src/globe/shaders/vectorEarth.ts、src/globe/coastline.ts、src/globe/coastline-110m.json、scripts/build-coastline.mjs（新增）；src/globe/GlobeScene.ts（矢量默认+satellite 入参）、src/App.tsx（DEV style=satellite 钩子）、tsconfig.json（resolveJsonModule）、public/assets/ASSETS.md。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.2.4/BUG-020.log |
| BUG-017 | FIX_READY | spec 契约（SPEC-5.6 OpenSky） | CORS 独立探测（2026-07-20，orch 以带 Origin 头的 GET 观察响应头，记录存 scratchpad）：OpenSky states/all 的 Access-Control-Allow-Origin 钉死为其自有域，web/PWA 端浏览器直连被同源策略封死；同轮探测的其余六源（USGS/EONET/GDACS/LL2/GDELT/CoinGecko）均返回通配 ACAO 可直连。SPEC-5.6 航班图层的 web 端直连可行性假设不成立，属上游契约与 spec 不符（CLAUDE.md §5.3 第 5 条），走 spec 修改路径。期望：M3 FM-12 开卡前经 rev 仲裁定向——航班图层改原生端专属（挂 M6）、另择 CORS 开放航班源、或降级；仲裁 pin 前 dev 不得按 SPEC-5.6 现文实现浏览器直连。**仲裁（REV-016，2026-07-22，独立复验非采信转述）**：curl 带 Origin 头实测 opensky states/all 返回 Access-Control-Allow-Origin 为 https://opensky-network.org（钉死自有域、非通配），对照源 USGS/EONET/CoinGecko 均返回通配星号，opensky 独异成立；curl 请求本身 200 成功证明纯属浏览器同源策略、非服务端封禁，原生端不受此限。§9 阶段一零服务器不得自建 CORS 代理绕过。**裁定方向①——航班图层改原生端（Capacitor）专属、挂 M6**，比照 SPEC-5.9「CORS 受限能力归原生端」先例；OpenSky 保留为源（源地址/视口 bbox/60s 轮询/severity 恒 1/关图层立即停拉不变，原生请求不受 CORS 约束）。web/PWA（M5）不呈现航班开关或呈禁用态并明示受同源策略限制。被否方向：②另择源——实测 airplanes.live 返通配 ACAO 且无 key 免费、技术可行，但 D29 商用许可存疑 + 社区源 CORS 稳定性风险 + 切源改动面大（须改 SPEC-5.10 信任表与 fixtures），不采（adsb.fi 无 ACAO 不可用、ADS-B Exchange 需 key 违无 key 约束）；③降级——被①支配，原生端可完整交付故否。SPEC-5.10 opensky「权威事件源」分级不变（信任分级与解析/平台归属正交）。SPEC-5.6 改法指引与全部证据见 doc/review/REV-016-opensky-cors-arbitration.md。**涟漪**：SPEC-6.3① flight-60s 随图层移出 M3、改挂 M6；FM-12 须摘除 OpenSky 与 flight-60s 承接（保留 GDELT/CoinGecko 于 M3）；BUG-016（现 FIX_READY，此前依 REV-007 F-1 改挂 M3 FM-12）复验前提失效，需 orch 将 flight-60s 再改挂 M6 并把复验条件改为 M6 native 航班图层开卡时核对，M2-02 的同 id 去重与 48–72h 过期窗判据不受影响。待 orch 应用 SPEC-5.6 改法 + 修改记录 + pin-spec + 同步 testplan。 | 浏览器侧对 opensky states/all 发跨域 fetch 观察 CORS 拦截；或 curl 带 Origin 头观察 ACAO 非通配（本仲裁复验：curl -sS -H "Origin: https://example.com" opensky states/all，实测 ACAO=https://opensky-network.org；对照 USGS/EONET/CoinGecko 通配） | - | 待 orch 应用 SPEC-5.6 改法 + pin（裁决 REV-016 方向①）；复验：M6 native 航班图层开卡时由 qa（≠仲裁人）核对 flight-60s 场景已按新平台归属登记 |
| BUG-016 | FIX_READY | doc/testplan.md 数据 | M2-02 登记的 flight（opensky）不套用长期过期窗子句在 M2 不可满足——M2 无 opensky 源（FM-12 属 M3），统一清扫语义下该子句无实现承接，属场景判据与里程碑范围失配。发现来源 REV-007 门禁 F-1。期望：该子句改挂 M3 FM-12 由 opensky provider 场景承接 SPEC-6.3①「flight 60s」，M2-02 保留同 id 去重与 48–72h 过期窗判据，不得静默蒸发 | 比对 doc/testplan.md M2-02 行文与 FM-05/FM-12 里程碑范围（裁决 REV-007 F-1） | orch 依 REV-007 F-1 应用：M2-02 删 flight 子句并留痕改挂、FM-12 交付物列补承接锚点，随 v0.2.0 closeout 入库。复验：M3 FM-12 开卡时由 qa 核对 flight-60s 场景已实际登记。**再改挂（REV-016/BUG-017，2026-07-22，v0.3.1）**：opensky 航班图层裁为原生端专属移出 M3、归 M6 FM-27，flight-60s 承接点从 M3 FM-12 改挂 **M6 FM-27**，复验条件相应改为「M6 native 航班图层开卡时由 qa 核对 flight-60s 场景已登记」；M2-02 的同 id 去重+48–72h 过期窗判据与 flight 无关、维持现状。 | - |
| BUG-015 | OPEN | scripts/evidence.py + 规则空白 | 流程/规则类缺陷无法机械关单：evidence.py 的 bug 模式要求挂一份带机械判定尾行的测试 log，而流程类缺陷（如 BUG-007 的派单调度问题）根本不存在对应的自动化测试，其复验形式是「核对规则文本已落地且可被遵守」。当前只有两条出路，都不可接受——挂一份与该缺陷无关的回归 log 冒充复验证据（rev 在 REV-004 的 C2 明确禁止），或让此类缺陷永远无法关单。发现来源 REV-004 的 C2 与 R-7。无 SPEC 依据（工具链与规则的双重空白）。期望：evidence.py 的 bug 模式支持「核对记录型」复验证据（由独立 qa 出具、写明核对对象与逐条结论、署名），并在 CLAUDE.md 明确该口径与其适用边界，避免被滥用为绕过测试证据的后门。 | 对一条无对应自动化测试的流程类缺陷（如 BUG-007）尝试执行 make evidence BUG 关单，观察是否只能挂无关 log 或无法关单 | - | - |
| BUG-021 | CLOSED | src/globe/markers.ts | SPEC-7.4「列表 hover/选中与球面标记高亮双向联动」的 marker→list 方向（hover 球面标记使面板对应列表行进入强调态）在真实运行环境中确定性 100% 失效，不是偶发。根因定位（QA 于 M2-14 卡直接调试确认，非猜测）：GlobeScene 构造完成后的首帧 render() 发生时，事件尚未经异步 dataLayer 链路注入，此刻 MarkerLayerImpl 的 dots（InstancedMesh）count 为 0；three.js 的 WebGLRenderer 默认开启 sortObjects，对透明物体排序前会在 object.boundingSphere 为 null 时调用一次 object.computeBoundingSphere()（node_modules/three/src/renderers/WebGLRenderer.js 约 1892-1894 行，此检查不受 mesh.frustumCulled=false 影响，markers.ts 里 dots/rings 均设了该标志也挡不住）。count=0 时算出的是一个 makeEmpty() 空球体（radius=-1），从此被永久缓存进 dots.boundingSphere（不再是 null）。此后 setEvents() 持续更新 instanceMatrix 与 count，但从未使 dots.boundingSphere 失效（既不重算也不置回 null）。InstancedMesh.raycast()（同一 three.js 源码文件 258-276 行）同样用「boundingSphere===null 才重算」的懒加载判断，命中已被钉死的空球体，raycaster.ray.intersectsSphere 恒为 false，函数提前 return，根本不进入逐实例求交循环。结果：GlobeScene.updateHover→MarkerLayer.pick() 对任意已加载标记、任意鼠标位置恒返回 null，onMarkerHover 回调永远不会以非 null id 触发——该 GlobeScene 实例存续期间，marker→list 联动从未生效过。期望（SPEC 依据）：SPEC-7.4「列表 hover/选中与球面标记高亮双向联动」，M2 范围已由 REV-010 §2.4 裁准取全双向（doc/review/REV-010-M2-globe-gate.md）；hover 球面已加载事件的标记时，面板应有对应行进入强调态。list→marker 方向（面板 hover/选中 使标记高亮放大）不受本缺陷影响，已由 e2e/panel-marker-linkage.spec.ts 的对应用例验证通过。**根因确认**：核对 node_modules/three 源码逐字复核 QA 的定位——InstancedMesh.raycast()（node_modules/three/src/objects/InstancedMesh.js 第 270 行 `if (this.boundingSphere === null) this.computeBoundingSphere();`）与 WebGLRenderer 的 sortObjects 前置检查（同仓库 renderers/WebGLRenderer.js 约 1892-1894 行）确为同一套「boundingSphere===null 才懒重算」判断；`dots` 在 GlobeScene 首帧渲染时 count=0，被 sortObjects 抢先算出并永久缓存一个 makeEmpty() 空球体（不受 frustumCulled=false 影响），而 markers.ts 的 setEvents() 更新 instanceMatrix/count 后从未使该缓存失效，pick() 由此恒命中陈旧空球提前 return。QA 定位与三方库源码行为完全吻合，非猜测。**修复**：src/globe/markers.ts 的 setEvents() 末尾新增一行 `this.dots.boundingSphere = null`，在每次事件集更新（增/删/改）后主动失效缓存；下一次 raycast()/render 会按当时最新的 instance 数据懒重算出正确球体。此单点修复同时覆盖两条路径：①首帧空球场景——真实事件经 setEvents() 落地即失效重算，不再恒空；②ensureCapacity 触发的 >256 扩容重建场景——扩容时 this.dots 已被 buildMeshes() 整体替换为全新 InstancedMesh 实例，本行在替换后仍执行一次失效（对新实例是幂等的 null→null），确保该路径同样被明确覆盖，而非依赖「新对象构造时天生 boundingSphere=null」的隐式行为。未动 pick()/setHighlight() 契约签名与其余逻辑；rings 不参与 raycast（pick() 只 intersectObject(this.dots)），未一并处理，超出本缺陷范围。修的是 raycast 懒重算本身而非绕过，M3 复用同一拾取路径的点击-飞行同样受益。 | `npx playwright test panel-marker-linkage.spec.ts -g marker`（e2e/panel-marker-linkage.spec.ts，QA 于 M2-13/M2-14 卡新增）：boot 单个/两个标记后，将真实鼠标移动到标记的精确屏幕投影坐标并等待多帧，.event-row--active 恒不出现，超时 FAIL。根因复现（独立于测试基建，任意已加载 ≥1 个标记后即可在浏览器控制台对 window.__globeDebug.globe 执行）：先记录 markerLayer.pick(raycaster) 在标记精确 NDC 下返回 null；再显式调用 markerLayer.object.children[0].computeBoundingSphere() 强制刷新后同一 pick() 调用即返回正确事件 id，证明问题出在缓存的空 boundingSphere 从未失效。 | src/globe/markers.ts（setEvents() 末尾新增 `this.dots.boundingSphere = null`）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.2.5/BUG-021.log |
| BUG-019 | CLOSED | spec 契约（SPEC-5.5 severity 方向性） | SPEC-5.5「severity：以 net 相对当前时刻，T-1h 内 3，T-24h 内 2，其余 1」未明说 net 已过去（发射已发生，记录仍存在，如源尚未清理或短暂延迟同步）时的时间距离计算方向——现实现（src/data/providers/ll2.ts severityFromNet）取 Math.abs(netTime-now) 双向对称计算，把「刚发射完 5 分钟」与「5 分钟后即将发射」同判为 severity 3；但 spec 文本同样能读出另一种合理解读——「T-1h 内」语义上更贴近「还有不到 1 小时就要发射」，仅未来方向计入，net 已过去时应直接归「其余」档甚至需要独立于该三档的语义（如「已发射」不再体现紧迫度）。发现于 M2-08 GDACS/LL2 provider 映射测试编写阶段（QA 从 spec 原文独立判断，未采信实现的 Math.abs 双向对称行为作为期望），按任务卡指示未替 spec 拍板，相关断言（tests/ll2.test.ts）已限定在无歧义的「net 严格晚于 now」区间，不覆盖 net 已过去的方向性。期望：走 CLAUDE.md §7 spec 修改路径——arch/rev 仲裁 net 已过去时的计算方向（双向对称 vs 仅未来方向 vs 另立語义），pin 后 qa 视裁决结果补齐或维持现断言范围。**仲裁与修复**：REV-009 §2 裁决二拍板——仅未来方向计入，net 已过去（net ≤ now）归其余档=1，不取绝对值双向对称（doc/review/REV-009-expiry-and-severity.md）；SPEC-5.5 severity 子条已按该裁决补句并 pin（v0.2.3）。**根因**：severityFromNet 用 Math.abs(netTime-now) 抹去了时间方向，把「已发射」与「即将发射」混同判高档，与倒计时紧迫度语义相悖。**修复**：src/data/providers/ll2.ts 的 severityFromNet 去掉 Math.abs，改用有向差 diff=netTime-now；diff≤0（net 已过去或等于 now）直接返回 1，否则按原三档阈值（≤1h→3，≤24h→2，其余→1）判定。仅动此一处函数体与其上注释，未动字段映射与其余逻辑。 | 构造 net 早于 now 的 LL2 result（如 net=now-30min），观察 normalizeLl2 输出的 severity——修复前按 Math.abs 判 3，修复后应为 1（SPEC-5.5 修订条款） | src/data/providers/ll2.ts（severityFromNet 去 Math.abs 改有向差）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.2.2/BUG-019.log |
| BUG-018 | CLOSED | spec 契约（SPEC-6.3 过期语义）+ src/data/store.ts | SPEC-6.3 过期清扫按事件 ts 判定（store.isExpired 为 now 减 ev.ts 大于 expiryMs），对仍被上游持续返回的长寿命事件产生误清与闪烁：源每轮仍返回该事件，upsertMany 以其原始（陈旧）ts 重写，随后 sweepExpired 又因 now 减 ts 超窗将其删除，下轮再现再删，稳定态下用户看不到该事件。实证（EONET 已 live，非未来边界）：eonet_events.json（status=open 且 days=7，抓取 2026-07-20T14:18:49Z）26 条全为 open，其中 24 条最新 geometry 日期距抓取时刻已超 72h（野火最旧达 171h）；eonet.ts 第 104 行映射 ts 为最新 geometry 日期，接入 EventStore(72h) 后首轮 sweepExpired 即删除 24 条、EONET 近乎全失。GDACS 侧同根因：REV-008 裁定 ts 取 datemodified，样本内最旧 47.4h 未触发，但生产中持续数月的干旱洪水一旦 datemodified 超 72h 未刷新即同样被误清。当前潜伏未被发现，因 M2-01（eonet 归一化与清扫真龄交互）尚为待办、M2-02 用合成事件不含真实年龄。根因为 SPEC-6.3① 「48 至 72h 无更新移除」语义歧义：无更新指(a)事件 ts 未推进（现实现）还是(b)事件未再被源返回或未再 upsert（按最后见到时刻续期）。REV-008 裁决三仲裁倾向 (b)：源仍返回即视为已见到，应续期不过期。期望：走 CLAUDE.md §7 spec 修改路径——arch 出 SPEC-6.3 语义澄清提案（Design Y：清扫按最后 upsert 墙钟时刻，事件仍在源内则每轮续期；ts 仍保留事件时间用于展示与排序），rev 仲裁 pin，dev 改 store.isExpired 语义，qa 补一条真实年龄场景（eonet fixture 全 open 事件经一轮清扫仍在快照内）并复测 M2-02。GDACS/LL2 字段映射 pin 不被本缺陷阻塞（ts 取 datemodified 独立正确，改它无益且更差）。建议在 M2 签核前解决——EONET 为 M2 源，带此缺陷对真实数据近乎不可用。发现来源 REV-008 裁决三（R-1 独立核验）。**仲裁**：REV-009 §1 裁决一确认 Design Y 成立（doc/review/REV-009-expiry-and-severity.md），SPEC-6.3① 已按提案 design-prompt/proposal-expiry-semantics.md §3 文本重写并 pin（v0.2.3）：过期基准改为 lastSeen（事件最后一次被 upsert 的墙钟时刻），ts 不参与过期判定；冷启动回填沿用持久化 lastSeen，离线时段计入无更新时长；SPEC-6.1 不改（lastSeen 不进 GeoEvent，作存储层内部记帐）。**根因**：store.isExpired 原以事件时间 ts 为过期基准，而 ts 是展示/排序用的事件时间、非存活性信号，导致「事件时间陈旧」与「已不再被源返回」两个正交概念被混为一谈。**修复**：① src/data/types.ts 新增 StoredRecord（event+lastSeen）类型，供 store/cache 间传递持久化专用记录，不进 GeoEvent。② src/data/store.ts：内部主索引由 Map<id,GeoEvent> 改为 Map<id,StoredRecord>；upsertMany 新增 now 入参（缺省 Date.now() 保持旧调用点可编译），每条被 upsert 的事件以 now 刷新 lastSeen；load 改为接受携带持久化 lastSeen 的 StoredRecord[] 并原样恢复（不重置为 now）；isExpired 判定改为 now-record.lastSeen>expiryMs；新增 entriesForPersist() 供装配层落盘用；snapshot() 仍返回纯 GeoEvent[]，读口契约不变。③ src/data/cache.ts：persist/load 的落盘形态改为 GeoEvent 字段铺平+lastSeen（keyPath 仍 id），round-trip 持久化 lastSeen。④ src/data/index.ts：每轮刷新回调内取单一 now 同传 store.upsertMany(events, now) 与 store.sweepExpired(now)；两处 cache.persist(store.snapshot()) 改为 cache.persist(store.entriesForPersist())。外科手术式改动，未动 scheduler/http/providers 结构。**自验**：用 tests/fixtures/eonet_events.json 复现登记的复现命令（临时脚本，未入库），归一化 26 条 → upsertMany(evts, now) → sweepExpired(now) → snapshot 仍为 26 条、0 条被清（修复前 Design X 下应为 2 条、24 条被误清）。**已知影响**：tests/store.test.ts 的「sweepExpired 在窗内保留事件，超窗后移除」一测转红——该用例调用 upsertMany 未传 now（缺省取真实 Date.now()），随后以构造的小整数（500/1500）调用 sweepExpired，Design Y 下 lastSeen 恒为真实墙钟大整数，导致该构造的小 now 值永远判定为未超窗，与旧 Design X（按 ts=0 计窗）语义不同，属判据经合法 spec 修改而变，非本次误改，需 qa 按新语义重写（不改 tests/，见交付汇报）。 | node 脚本读 tests/fixtures/eonet_events.json，用 normalizeEonet(raw, now=Date.parse 值 2026-07-20T14:18:49Z) 归一化，灌入 new EventStore(72h) 后先 upsertMany(evts, now) 再 sweepExpired(now)，观察 snapshot 条数——修复前降至 2 条（24 条 open 野火被误清），修复后应仍为 26 条；或对该 fixture 统计最新 geometry 日期距抓取时刻超 72h 的 open 事件数 | src/data/types.ts（新增 StoredRecord）、src/data/store.ts（lastSeen 基准 isExpired、upsertMany 增 now、load 接受并恢复 lastSeen、新增 entriesForPersist）、src/data/cache.ts（persist/load round-trip lastSeen）、src/data/index.ts（单一 now 同传 upsert+sweep，persist 改用 entriesForPersist）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.2.2/BUG-018.log |
| BUG-022 | CLOSED | src/globe/markers.ts | 标记脉冲环材质用 AdditiveBlending，密集区多环重叠时加色混合饱和成白色团块——白色不属于 SPEC-3.7 分类色表中任何分类，重叠产生了第三种颜色语义，破坏「分类色为唯一色语义」契约；视觉上呈污渍状白斑，观感差。实测必现于加州地热田（The Geysers 微震群十余条堆叠）与大西洋中脊多震点位，现象截图 doc/attachment/ux-review-20260721/pacific-microquake-smear.png（太平洋视角中部白圈即饱和斑）。期望：重叠标记的混合结果仍应可归属其分类色（如改普通透明混合、限制环透明度上限，或与聚合方案一并解决——聚合已属 D23 强制项）。orch UX 验收（v0.2.6）发现。**根因**：ringMaterial 用 THREE.AdditiveBlending，加色混合为 dst 加上 srcColor·srcAlpha 的无上界累加；同分类多环重叠时每环叠加约 0.55·分类色，RGB 三通道逐次逼近 1.0 即白，制造出不属 SPEC-3.7 色表的第三种色语义。dots 用 ShaderMaterial 默认 NormalBlending 不受影响，故白斑仅由脉冲环产生，与登记现象一致。**修复**：仅将 ringMaterial 的 blending 由 AdditiveBlending 改为 NormalBlending（普通透明混合），其叠加结果为 src·srcAlpha 加 dst·(1 减 srcAlpha) 恒收敛于该分类色本身、绝不越过其色域趋白——同分类密集区叠成实心分类色团、仍可归属，跨分类重叠得两色之间的中间色而非白；满足「重叠结果可归属分类色」期望（SPEC-3.7）。**观感取舍**：近黑矢量球面背景下 bg 近似 0，单环 additive（bg 加 0.55·环）与 normal（0.55·环 加 0.45·bg）结果几乎等同，单标记辉光基本不劣化，脉冲仍由环尺寸振荡表达（tick 不变）；仅在较亮昼面/海岸线背景上普通混合较加色略暗，属可接受取舍。opacity 维持 0.55、接口签名与标记体系未动，未做聚合（D23 另案）。**被否方案**：仅「限制环透明度上限」不足——加色饱和由叠加环数驱动，封顶单环 alpha 只延迟不消除白斑，故弃。 | make dev 后拖转到北美西海岸或大西洋中脊视角，观察多标记重叠处颜色 | src/globe/markers.ts（ringMaterial 的 blending 由 AdditiveBlending 改为 NormalBlending）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.2.8/BUG-022.log |
| BUG-023 | CLOSED | spec 契约（SPEC-5.1 未定展示阈值）+ src/data/providers/usgs.ts | USGS 接 all_day 全量 feed 且无震级下限，M0.x 微震全部入球入流：实测快照 250+ 条事件中绝大多数为 M<2 微震（多为加州地热田、内华达同点位重复），球面一屏 200+ 红点、事件流被淹没，M5.6 有感地震与 M0.35 仪器级微震同屏同权。直接违背 D1 体验核心「低噪」（doc/product-decisions.md）与 D23 L0 层「少而准」原则。属 spec 缺陷——SPEC-5.1 只定了 severity 映射未定入球门槛，走 CLAUDE.md §7 spec 修改路径：arch 提案展示阈值（如 M≥2.5 或分层淡显/聚合策略），rev 仲裁后 pin。现象截图同 BUG-022 附件。orch UX 验收（v0.2.6）发现。**根因与修复（dev，2026-07-21）**：根因=接入 all_day/all_hour 全量 feed 且无显著性门槛；spec 侧经 REV-012 裁准新增 SPEC-5.0a、改写 SPEC-5.1（v0.2.8 已 pin），实现侧 src/data/providers/usgs.ts 端点常量切换为 2.5_day/2.5_hour（源侧兑现 M≥2.5，无客户端过滤）。待 qa 按 M2-05 新判据测绿后复验关单。 | make dev 观察事件流面板中 M<2 条目占比与球面红点密度 | src/data/providers/usgs.ts（端点切换；随 M2-05 测绿的 closeout 一并入库） | doc/evidence/v0.2.10/BUG-023.log |
| BUG-024 | CLOSED | spec 契约（SPEC-2.2a 排序语义） | 事件流按 ts 倒序（最新在上）在混合未来/过去事件时产生反直觉排序：未来事件 ts 越大越靠上，导致「9 天后」的发射占据首屏第一行，而最紧迫的条目（T-12h 内发射、几分钟前的新地震）沉在列表中部——首屏黄金位被最不紧迫的信息占据，与 D23 L0「多新」编码意图相悖。属 spec 级设计缺陷，走 §7：arch 重提排序语义（如按「距now绝对时间差」双向收敛、或未来/过去分区呈现），rev 仲裁后 pin 并同步 SPEC-2.2a 与相关 testplan 条目。orch UX 验收（v0.2.6）发现。**根因与修复（dev，2026-07-21）**：根因=排序键为 ts 倒序、对未来事件方向反转；spec 侧经 REV-012 裁准改写 SPEC-2.2a 排序句（距 now 时间邻近度升序，v0.2.8 已 pin），实现侧 src/ui/EventPanel.tsx 比较器改三级键（ts 与 now 绝对时间差升序、等距未来优先、id 升序）。待 qa 按 M2-22 场景测绿后复验关单。 | make dev，LL2 存在多条未来发射时观察面板首行是否为最远未来条目 | src/ui/EventPanel.tsx（排序比较器；随 M2-22 测绿的 closeout 一并入库） | doc/evidence/v0.2.10/BUG-024.log |
| BUG-025 | CLOSED | spec 契约（SPEC-2.2a 行定义） | 事件流列表行仅定义「分类色圆点+标题+相对时间」，无任何 severity 表达：M5.6 中脊地震与 M0.35 地热微震的行视觉完全同权，用户无法从列表分辨事件轻重，违背「低价值条目不得与真正风险事件抢同等注意力」（D23 采纳的 grill §3 判据）。属 spec 缺陷，走 §7：arch 提案行内 severity 编码（如字重/行高/色点尺寸三档），注意与 BUG-023 的入球门槛方案联动设计、避免重复解决同一噪声问题。orch UX 验收（v0.2.6）发现。**spec 与实现均已落地(2026-07-21 orch 回填)**:spec 侧 REV-012 裁准 SPEC-2.2a 行内 severity 三档单调非色相编码(v0.2.8 pin),REV-013 填 D25 造型值——标题明度三档 `#eef2f7`/`#c2ccd8`/`#8794a3`+行首圆点镜像分级(v0.2.13 pin);实现侧视觉批次一交付(EventPanel.tsx 行 severity class+index.css 三档明度,v0.2.15)。机械复验以 M3-01 场景(行明度三档单调+WCAG AA)测绿为准。 | make dev 对比面板中高低震级两行的视觉差异;机械复验=M3-01 场景测绿 | spec:v0.2.8+v0.2.13 pin;实现:视觉批次一 v0.2.15(19ed077) | doc/evidence/v0.2.19/BUG-025.log |
| BUG-026 | CLOSED | src/ui（App 头部） | 标题栏灰字「行情 ticker（M3）」为未来功能占位符，对用户可见——内部里程碑代号泄漏进产品 UI。期望：M3 交付前该位不渲染（不是禁用态展示）。orch UX 验收（v0.2.6）发现。**修复（dev FM-10，2026-07-21）**：src/App.tsx 顶栏按 SPEC-2.1 重写时移除该占位（不渲染），随 v0.2.11 入库。待 qa 复验关单。 | make dev 看首屏头部左侧 | src/App.tsx（FM-10 顶栏重写，v0.2.11） | doc/evidence/v0.2.11/BUG-026.log |
| BUG-027 | CLOSED | src/ui（App 头部）+ index.html | 对外可见品牌用内部代号：头部标题「World Tunnel」、document.title「World Tunnel — 新闻地球仪」。依 D4（doc/product-decisions.md）产品对外名为 Worlens（备选序列见 D4 修订）。上架（M5）前必须统一对外名；提前登记防遗忘，属低优先级、可与 M5 打包卡合并处理。orch UX 验收（v0.2.6）发现。**orch 裁定（2026-07-21，依 REV-011 整改项 2）**：以 spec 为准——SPEC-2.1 品牌名标注（M2），本条并入 FM-10 缺口于 M2 内解决，前句「低优先级、可与 M5 合并」作废；不走 §7 改里程碑标注。**修复（dev FM-10，2026-07-21）**：src/App.tsx 顶栏品牌名与 index.html document.title 均统一为「Worlens」（SPEC-2.1），随 v0.2.11 入库；M2-18/M0-02 已按新判据 ✅。待 qa 复验关单。 | make dev 看头部与浏览器标签页标题 | src/App.tsx + index.html（FM-10，v0.2.11） | doc/evidence/v0.2.11/BUG-027.log |
| BUG-028 | CLOSED | scripts/docs.py | make next 的里程碑核对只统计 testplan 已登记场景,对「feature-matrix 中该 M 的行零场景」不可见——M2 在 FM-09/10/11 未开卡时即提示进入签核,REV-011 认定为误报,系 BUG-014/REV-002 G-1「静默蒸发」模式在 feature-matrix 层的放大。期望:里程碑完成核对新增「该 M 的全部 FM 行至少各有一条已登记 testplan 场景」检查,不满足时列出缺卡 FM 行而非提示签核。来源:REV-011 整改项 4(建议级,不阻塞签核,但宜在 M2 重签核前修好以免再误报)。**根因确认（2026-07-21，dev）**：next_action() 判断「是否可进入签核提示」只依据 testplan_rows() 过滤出的当前里程碑场景状态（bad/todo）,从未读取 feature-matrix.md，对「FM 行场景列仍是占位、尚未开卡」这一状态零可见——即便某些 FM 行从未登记任何 testplan 场景，只要已登记的那部分场景全 ✅，仍会误判"该 M 场景全 ✅"并推进到归档/签核提示。**修复**：next_action() 在原有"当前里程碑场景全 ✅"分支内新增前置检查——用既有 parse_table 读取 feature-matrix.md，筛出里程碑列等于当前版本里程碑的行，对每行场景列做场景编号正则提取，提取结果为空即判定该 FM 行零场景（覆盖「(M<n> 开卡登记)」占位与空值两种写法）；存在零场景 FM 行时列出其编号并直接结束该分支（不再进入证据目录扫描与签核三级判断），全部 FM 行均已有场景编号引用时行为与原先完全一致。**验证**：① 真实台账（M2 全部 FM 行已开卡）跑 python scripts/docs.py --next，输出与修复前一致（仍为"三条判据齐备"提示），未回归；② scratchpad 构造 doc/ 副本，将 feature-matrix.md 副本中 FM-11 场景列清空为「（M2 开卡登记）」占位，monkeypatch docs.ROOT/DOC 后跑 next_action()，输出改为列出缺卡行"FM-11"，不再出现归档/签核/三条判据齐备任一提示；未触碰真实 doc/feature-matrix.md。 | 于某 FM 行零场景时跑 make next,观察是否仍提示进入签核 | scripts/docs.py（next_action() 新增 fm_missing 检查，位于原「场景全 ✅」判断分支内）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.2.22/BUG-028-033-verification.md（独立 qa 核对记录，正反两向 PASS，orch 依 CLAUDE.md §5.3-4 口径置 CLOSED，2026-07-21） |
| BUG-029 | CLOSED | doc/testplan.md 覆盖面 | SPEC-2.2「全屏地球 canvas」子句无任何场景以 SPEC-2.2 为引用显式判定——唯一涉 canvas 尺寸的 M0-02 仅断言非零、未断言占满视口。BUG-014 同构模式（spec 子句无人认领），发现于其核对记录 doc/evidence/v0.2.11/BUG-014-verification.md 顺带核对节。期望：M2-13 重测卡一并补「canvas 占满视口」判据与断言（引 SPEC-2.2），或 qa 另登场景承接；属场景覆盖补齐，不涉 spec 修改。**关单（2026-07-21，orch 依 signoff-M2.md 复核记录）**：M2-13 重测卡（v0.2.16）已按期望补齐「canvas 占满视口、面板悬浮不挤占」判据与断言（e2e/event-panel.spec.ts，引 SPEC-2.2），场景 ✅ 且证据在档（doc/evidence/v0.2.15/M2-13.log）；rev 于 M2 签核复核确认子句已被显式承接、无静默蒸发风险，本条属台账卫生滞后，据其书面记录关单。 | 检索 doc/testplan.md 全部场景描述对 SPEC-2.2 canvas 子句的引用，确认无人认领 | qa M2-13 重测卡（v0.2.16，e2e/event-panel.spec.ts 补断言） | doc/evidence/v0.2.15/M2-13.log + doc/evidence/signoff-M2.md 复核节 |
| BUG-030 | CLOSED | spec 契约（SPEC-3.11 过渡范围歧义） | SPEC-3.11「冷启动即渲染缓存…首次刷新完成后以呼吸式过渡收敛;旧标记渐隐、最新事件渐亮」未明说**全新安装无缓存**时首个非空批次(首轮网络事件)是否也应淡入。现实现(FM-09,markers.ts hasPopulated 标志)取「首个非空 setEvents 直接置 alpha=1(snap),其后增量才过渡」——依据是「即渲染」语义与避免整屏淡入;另一种合理解读为「无前态也应渐亮」。dev 于 FM-09 交付如实上报,未自行改判据。期望:走 §7——rev 仲裁两种解读,pin 后 qa 视裁决补齐或维持 M2-20/21 断言范围(现登记判据未涉此边界,不阻塞其测试)。**仲裁与应用(2026-07-21)**:REV-014 三维度一致裁定读法 a——无前态(全新安装无缓存/缓存空/读取失败)时首个非空批次直接 snap 上屏不淡入,呼吸只表达对前态的增量(doc/review/REV-014-cache-snap-arbitration.md);orch 已按 §3 前后对照应用 SPEC-3.11 消歧句并 pin(v0.2.16)。现实现与裁决一致,零代码改动,属消歧澄清。M2-20/21 维持现范围与 ✅(REV-014 §4)。 | 清空 IndexedDB 缓存后冷启动,观察首轮网络事件上屏是 snap 还是淡入;机械复验以 qa 新登「首批 snap 对照增量淡入」标记层单测场景为准(REV-014 §4-3) | orch 应用 REV-014 §3 修订并 pin(v0.2.16),无 src 改动 | doc/evidence/v0.2.16/BUG-030.log |
| BUG-031 | OPEN | spec 契约（SPEC-3.7 常驻脉冲） | 用户实测否决常驻动画:现行每标记带脉冲环持续脉动(全 severity 档幅度>0,且 SPEC-3.7 硬性要求 sev3 持续脉冲环),观感「带环的土星一直动,很吵闹」,违 D3 宁静与 D27(常驻点静默、呼吸只用于增量登离场)。期望:走 §7 动效批(REV-013 遗留 + D27)——aes 调研设计动效体系(常驻态表达、增量呼吸、reduced-motion P1、光柱形态候选一并),arch 提案改 SPEC-3.7 动效子句(含「sev3 必须持续脉冲环」存废),rev 仲裁。**仲裁(2026-07-22,REV-017)**:动效批提案 doc/design-prompt/proposal-batch2-motion.md 经仲裁**整改后通过**——删「sev3 必须持续脉冲环」硬句 + 新增 SPEC-3.11a reduced-motion 获准(REV-013 两项强制论证义务兑现,见 REV-017 审查 1/2),色值零漂移、呼吸无回退、arch 忠实转写 aes 取值均核实通过;唯一整改点 R-1(SPEC-3.7c 标记 LOD 近档 d<1.8 撞 SPEC-7.2 相机距离下限 1.8 不可达)与本 BUG 核心「否决常驻脉冲」正交、不阻塞关单。**维持 OPEN**(非零代码消歧,不得由仲裁直接 CLOSED):待 orch 应用删脉冲 spec+pin → dev 实现动效批(删常驻脉冲动画)→ qa 重测 M2-10/M3-03(稳态两帧无差异 + 三档静态辉光)+ reduced-motion 稳态验证,make evidence BUG=BUG-031 机械关单(关单人≠修复人)。 | make dev 观察任一常驻标记的持续动画 | - | - |
| BUG-032 | OPEN | 视觉资产/几何精度 | 近景失真三件(用户实测):①海岸线 110m 档拉近锯齿失真;②大气壳近景可见折线(几何分段不足);③经纬网格仍默认显示——③属 v0.2.13 SPEC-3.2a 已改、实现未跟,归视觉批次一 dev 卡承接;①②依 D29 开源优先修复:海岸线升 Natural Earth 50m/10m 分档(公版可打包,map-lod 调研 §2),球体/大气分段取成熟参数,不自研算法;分档加载与国界数据一并入视觉第二批(LOD)提案,近景档体积入 SPEC-3.10 预算核对。**③已兑现(2026-07-21 orch 复核)**:网格默认隐藏随视觉批次一实现落地(v0.2.15),qa M2-15 重测以结构断言确认唯一可见线层为海岸线、网格层 visible=false 不入画面(doc/evidence/v0.2.15/M2-15.log);本条余下①②,随视觉第二批(LOD)处置。 | make dev 拉近观察海岸线锯齿与大气边缘折线 | - | - |
| BUG-034 | CLOSED | src/data/index.ts（createDataLayer 生命周期） | 早停的 dataLayer 实例会用空快照清空共享持久化缓存：createDataLayer().stop() 无条件执行 `cache.persist(store.entriesForPersist())`（DP §3.6 落一次持久化），即便其自身 start() 触发的 cache.load()→store.load() 尚未完成——此时 store 必为空，这次持久化即用空快照覆盖 IndexedDB 里可能已存在的、来自上一会话或其他实例的真实数据。发现于 M2-24 卡（e2e 冷启动基线量测）：真实浏览器（dev server，React 18 StrictMode 生效）下，App.tsx useEffect 内 `void dataLayer.start()` 不等待、StrictMode 的挂载后立即卸载 cleanup 紧接在同一同步块执行（早于 start() 内部任何微任务/Promise resolve，含 IndexedDB 回调），首个（很快被卸载的）dataLayer 实例的 stop() 几乎必然抢在其自身 cache.load() 完成前执行；预置的种子缓存事件写入后立即读回确认存在，但触发一次 reload（真实冷启动路径）后再读回 IndexedDB 已变为空数组，标记层实例数在 10s 轮询窗口内恒为 0，SPEC-3.11「冷启动即渲染上次本地缓存事件」的前提（缓存本身完整）被破坏。已用最小 Node 级 fake IndexedDB 复现同一根因（不依赖浏览器/React/StrictMode，纯 createDataLayer 实例生命周期契约问题，证明并非测试基建或 StrictMode 本身的偶然产物）：tests/data-layer-early-stop-cache-loss.test.ts，先经真实 EventCache.persist 落盘种子事件，实例 A 的 start() 触发但不 await 即调用 stop()，flush 若干微任务后实例 B 正常 start 并 await，断言 B 应读到种子事件——现行为读到空数组，断言失败，证明数据丢失。**已知影响面**：production 构建下 React StrictMode 的双调用效应不生效（React 运行时行为，仅 dev 模式），故该竞态目前只在 `npm run dev`（本项目全部 e2e 测试基建的运行环境，playwright.config.ts webServer）下确定性触发；理论上任何导致 start() 未完成即被 stop() 的早停场景（如未来快速 remount、M5 Capacitor 生命周期 pause/resume 若接类似模式）都可能复现同一根因，不限于 StrictMode。期望（SPEC 依据）：SPEC-3.11「缓存……升格为启动路径的一部分」+ SPEC-8.4「事件缓存……仍不承诺离线数据完整性」——「不承诺完整性」不等于「允许把已有持久化数据清空」；stop() 不应在 start() 自身的 cache.load() 未完成时，用必然为空的快照覆盖式持久化共享存储（如改为：load 未完成则 stop() 跳过持久化，或 persist 改用合并写而非 clear+put 全覆盖，具体方案交 dev 判断）。M2-24 场景已改用生产 preview 服务器（`vite preview`，无 StrictMode 双调用）规避本缺陷进行冷启动基线量测，量测方法与本缺陷相互独立、不互相阻塞。**根因（2026-07-21 dev）**：stop() 无条件调用 `cache.persist(store.entriesForPersist())`，而 EventCache.persist 是 clear+put 全覆盖式落盘（src/data/cache.ts）；初始 cache.load() 未 settle 前 store 恒为空，这次空快照落盘先 clear 掉 IndexedDB 里已有的真实缓存又不 put 任何记录，即清空。dev 下 React 18 StrictMode 挂载即卸载，使首实例的 stop() 抢在其自身 start() 的 load 回调（含 IndexedDB 异步）之前同步执行，故 100% 触发。**修复**：src/data/index.ts 引入 cacheLoaded 闸门——初始 cache.load() 成功 settle 后置 true（读取失败走 catch 保持 false，避免读挂时误清），stop() 仅在 cacheLoaded 为 true 时落盘，未完成前跳过持久化；对外接口签名不变、cache.ts/store.ts 零改动。复现测试 `make test TEST=data-layer-early-stop-cache-loss` 转绿；`make lint` PASS；全量 `make test` 176 单测 PASS，cache-first-start/store 等数据层既有测试零回归。 | `make test TEST=data-layer-early-stop-cache-loss` | 待 orch closeout（src/data/index.ts） | doc/evidence/v0.2.20/BUG-034.log |
| BUG-035 | CLOSED | src/data/index.ts（createDataLayer 生命周期，BUG-034 修复引入的残留缺口） | BUG-034 修复给 stop() 加了 cacheLoaded 闸门（未完成初始回填前跳过持久化），但闸门的翻转（`try { ...; cacheLoaded = true } catch {...}`）不受 stopped 状态约束——一个已被早停的实例（stop() 抢在其自身 cache.load() 完成前执行，store.load 因 !stopped 判断被跳过、store 仍为空），其自身 load 晚到 settle 后仍会把 cacheLoaded 悄悄翻为 true；此时若该实例被再次调用一次 stop()（二次 stop，如宿主重复调用 dispose/cleanup），闸门已开，会用这份必然为空的 store 快照第二次落盘，清空共享缓存——BUG-034 本欲杜绝的破坏经「二次 stop」这条路径复活。发现于 BUG-034 复验卡评估「早停后晚到 load 无竞态复活」回归检查点，扩大探测边界到「二次 stop」时命中。已用最小 Node 级 fake IndexedDB 复现：tests/data-layer-double-stop-cache-loss.test.ts——先经真实 EventCache.persist 落盘种子事件，实例 A 早停（start 触发不 await 即 stop），flush 微任务令 A 自身 load 晚到 settle，再对 A 调用第二次 stop()，随后另起实例 B 应仍读到种子事件——现行为读到空数组，断言失败，证明数据经二次 stop 再次丢失。期望（SPEC 依据）：与 BUG-034 同源——SPEC-3.11「缓存……升格为启动路径的一部分」+ SPEC-8.4「事件缓存……仍不承诺离线数据完整性」——「不承诺完整性」不等于「允许把已有的持久化数据清空」，该原则不因触发路径是首次 stop 还是同一实例的后续 stop 而改变；一个从未真正完成回填（store 从未装入真实数据）的实例，不论被 stop() 多少次，都不应用空快照覆盖共享存储（具体方案交 dev 判断，如改用「store 是否曾经真正 load 过非空数据」而非单纯 cacheLoaded 布尔位做闸门，或 stop() 增设幂等标志防止同一实例重复落盘）。真实场景触发面：doc/bugs.md BUG-034 条目已记「M5 Capacitor 生命周期 pause/resume 若接类似模式」可能复现同一根因不限于 StrictMode——二次 stop 属该推测的具体化之一，当前无已知生产触发路径确证，risk 层面属防御性缺口。**根因（2026-07-21 dev）**：BUG-034 的 cacheLoaded 闸门翻转（`cacheLoaded = true`）写在 `if (!stopped && cached.length > 0) store.load(cached)` 之后、脱离了 `!stopped` 约束——早停实例的 store.load 因 `!stopped` 为假被跳过、store 从未装入持久层数据，但其晚到 settle 的 load 仍无条件把 cacheLoaded 翻为 true；闸门一旦被这样悄悄打开，同一实例的二次 stop() 便通过 cacheLoaded 判断获得落盘资格，用必然为空的 store 快照执行 EventCache.persist 的 clear+put 全覆盖，把共享缓存清空——BUG-034 本欲杜绝的空快照覆写经「二次 stop」复活。**修复**：把 `cacheLoaded = true` 收拢进同一个 `if (!stopped)` 块内（回填与开闸同受 !stopped 约束）——早停实例的 load 晚到 settle 时 stopped 已为真，既不回填也不翻转闸门，其 store 从未反映持久层，故任意次数的 stop() 都因 cacheLoaded 恒为 false 跳过持久化，杜绝空快照覆写；正常实例（未早停）仍在 load settle 后照常开闸，BUG-034 语义不变。闸门语义收敛为「本实例的 store 确已反映持久层」，与 BUG-034 自洽。对外接口签名不变、cache.ts/store.ts 零改动。复现测试 `make test TEST=data-layer-double-stop-cache-loss` 转绿（1 test）；BUG-034 回归 `make test TEST=data-layer-early-stop-cache-loss` 保持 PASS（3 tests）；`make lint` PASS；全量 `make test` 179 单测 PASS，零回归。 | `make test TEST=data-layer-double-stop-cache-loss` | 待 orch closeout（src/data/index.ts） | doc/evidence/v0.2.20/BUG-035.log |
| BUG-033 | CLOSED | scripts/evidence.py（视觉标记误判） | evidence.py 的 VISUAL_MARKERS 守卫（BUG-008 引入）按 testplan 场景描述列是否含"截图"或"视觉"子串判定视觉判据，未传 --shot 即拒绝置 ✅。M2-13（事件流面板基础结构，e2e DOM/版式场景，本身非截图判读类）行文经 REV-013 改写后含对 M3-01/M3-03「视觉批次场景」的交叉引用（原文"其单调性验证归 M3-01/M3-03 视觉批次场景"）而误触守卫，逼迫 M2-13 必须附截图方能置 ✅。此即 BUG-008 修复注记预警的残留风险（"若未来出现误判…需再细化标记语法而非放宽判断"）实际兑现。本次以捕获全屏 canvas 加悬浮面板版式截图（M2-13 含 SPEC-2.2 全屏 canvas 版式子句，截图为相关真实产物、非无关 log）满足守卫、未绕过测试证据。无 SPEC 依据（工具链契约缺陷）。期望：细化标记语法——仅当场景描述自声明视觉判据（如整词"（视觉，需附截图）"或行尾"需附截图"）才触发，对指向其他场景编号的"视觉批次场景"等交叉引用不触发；不得靠删改场景交叉引用文字规避（削弱可追溯性）。**根因确认（2026-07-21，dev）**：scenario_needs_shot() 原实现对描述整列做一次性子串包含判断，不区分该子串出现的语境是"本场景自声明视觉判据"还是"提及其他场景编号的交叉引用"，M2-13 行文中"M3-01/M3-03 视觉批次场景"属后者却被同等计入，产生误判。**修复**：改为先按中文/英文标点把描述切成分句，逐分句判断——分句内含视觉/截图字样时，再检查该分句内是否同时出现除本场景自身编号外的其他场景编号引用；若有则判定为交叉引用、跳过该分句不计入，否则判定为自声明、触发守卫；任一分句判定自声明即返回需要截图。VISUAL_MARKERS 常量本身未动（仍是"截图"/"视觉"两词），只细化了触发语境，不构成放宽 BUG-008 的守卫本意。**验证**：① 对真实 doc/testplan.md 逐场景调用 scenario_needs_shot 核对，M2-13 由 True 转为 False（误判已消除）；M0-02/M1-05/M1-07/M1-08/M2-10/M2-15/M2-21/M3-01/M3-02/M3-03/M3-04/M3-05/M3-06 等全部真实视觉/截图场景仍为 True（守卫未被削弱），非视觉场景（如 M2-01/M2-03/M2-25）仍为 False；② scratchpad 构造 doc/testplan.md 副本，monkeypatch evidence.ROOT/DOC 后跑通 CLI 全流程——对 M2-13 执行 python scripts/evidence.py --scen M2-13 --test event-panel --e2e（不传 --shot）不再被拦截、正常置 ✅；对 M2-15、M3-01 同法执行仍被显式 SystemExit 拒收；两向行为均符合期望，未污染真实台账与证据目录。 | 对 doc/testplan.md 中描述含"视觉批次场景"等交叉引用但自身非视觉的场景（如 M2-13）跑 python scripts/evidence.py --scen M2-13 --test event-panel --e2e（不传 --shot），观察是否被守卫拒收 | scripts/evidence.py（scenario_needs_shot() 改为分句+交叉引用检测判定自声明视觉判据）。本次改动尚未提交，随下次 closeout 一并入库。 | doc/evidence/v0.2.22/BUG-028-033-verification.md（独立 qa 核对记录，正反两向+判别力抽查 PASS，orch 依 CLAUDE.md §5.3-4 口径置 CLOSED，2026-07-21） |
| BUG-036 | OPEN | scripts/docs.py | make next 里程碑完成核对的判据②③检查(REGRESS 归档证据与签核记录)用 any() 扫描全部历史证据目录,不校验该证据是否属当前签核轮次——历史某轮的 regress 证据/签核文件会让「三条判据齐备」在本轮未重跑 REGRESS 时提前亮起,与 BUG-028 同族(核对盲区)但独立成因。实际影响:orch 若轻信该提示可跳过本轮全量回归,证据链出现时效性假绿。来源:dev 于 BUG-028 修复卡交付汇报中观察上报(超出该卡授权范围未自行登记),orch 依 §5.3 转登记。期望:判据②③的证据检查加时效约束(如要求 REGRESS 证据晚于当前里程碑最近一次场景状态变更,或绑定当前版本号),不满足时提示重跑而非齐备;修复前 orch 签核一律显式重跑 make regress+evidence REGRESS=1,不采信该提示。 | 存在历史轮次 regress 证据但本轮未重跑时执行 make next,观察是否仍提示三条判据齐备 | - | - |
